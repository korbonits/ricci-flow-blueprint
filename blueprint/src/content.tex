  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

H  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

'  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

1  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

9  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

8  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

3  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

G  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

j  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

0  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

^  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

1  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

^  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

+  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

J  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

0  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

6  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

^  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

H  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

₃  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

^  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

~  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

^  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

V  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

W  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

;  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

U  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

N  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

F  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

4  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

#  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

3  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

6  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

8  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

4  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

5  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

K  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

0  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

6  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

U  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

K  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

;  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

0  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

6  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

C  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

/  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

N  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

X  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

Y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

U  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

H  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

₃  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

U  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

G  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

'  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

P  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

"  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

;  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

L  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

E  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

I  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

_  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

=  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

O  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

0  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

(  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

D  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

T  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

'  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

N  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

H  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

'  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

)  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

N  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

M  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

[  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

H  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

1  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

9  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

8  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

2  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

]  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

:  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

3  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

$  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

-  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

R  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

b  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

B  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

k  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

A  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

y  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

q  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

x  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

u  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

p  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

S  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

'  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

s  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

,  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

v  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

g  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

c  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

a  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

i  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

z  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

   \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

f  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

l  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

w  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

.  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

\  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

n  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

d  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

{  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

t  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

h  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

o  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

r  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

e  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

m  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

}  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.


  \textbf{Mathematics complete; packaging outstanding.} The trace needs $\Riem$
  to descend from vector \emph{fields} to vectors, and the criterion for that is
  $C^\infty(\M)$-linearity in each slot, which is Lemma~\ref{lem:tensoriality}.
  What remains is not mathematics but construction: assembling a pointwise tensor
  from the linearity and tracing it, for which \texttt{TensorialAt} supplies only
  first-order hypotheses and \texttt{mkHom₃} does not exist. This was previously
  recorded here as the blocker on the mathematics; that was wrong.

