# Ricci flow blueprint — working notes

A `leanblueprint` for Ricci flow. Live: https://korbonits.github.io/ricci-flow-blueprint/
Milestone: Hamilton 1982. Terminal node: Perelman's spherical space form theorem.

## State (2026-09-16)

`lake build` clean, **no `sorry`**, axiom-free — `#print axioms` shows only
`[propext, Classical.choice, Quot.sound]`. Tracks mathlib `master` (pinned rev
in `lake-manifest.json`) on Lean `v4.34.0-rc2`, because mathlib4 #36845
(Levi-Civita) merged after the last release and `LeviCivita.lean` builds on it.

| File | Contents |
| --- | --- |
| `Curvature.lean` | `curvature`, `curvature_antisymm`, `bianchi_first`, `curvature_smul_*` and `curvature_add_*` (tensoriality, all three slots), `tensorialAt_curvature_fst/snd`, `neg_apply`, `sub_apply`, `mdiffAt_cov_apply` |
| `LieBracketDerivation.lean` | `lieBracket_apply_fun`, `mlieBracket_apply_fun` (any manifold, corners allowed), `jacobi_mlieBracket_apply`, model-space collapses |
| `Ricci.lean` | `ricci` — **defined**; `ricci_sub_ricci_swap` |
| `Sectional.lean` | `sectionalCurvature`, `sectionalCurvature_basis_change` |
| `LeviCivita.lean` | `exists_leviCivita`, `leviCivita_unique` (on differentiable sections — the `∃!` form was never provable), `curvature_eq_of_isLeviCivita`, `ricci_eq_of_isLeviCivita`, `sectionalCurvature_eq_of_isLeviCivita` |
| `LeviCivitaSmooth.lean` | `contMDiffCovariantDerivative_leviCivitaConnection` — **Levi-Civita is `C^k` for a `C^{k+1}` metric** (Mathlib leaves this to "future PRs"); criteria `contMDiffAt_clm_of_basis`, `contMDiffAt_section_of_inner_localFrame`, `contMDiffAt_mvfderiv_apply`, `contMDiffAt_koszul`; instance for `k = 1`; `ricciOfMetric`, `sectionalCurvatureOfMetric` |
| `Flow.lean` | `IsRicciFlowAt/On`, `isRicciFlowAt_const_iff`, `isRicciFlowAt_iff_of_isLeviCivita`, `isRicciFlowOn_iff_ricciOfMetric` — **the flow is `∂g/∂t = -2 Ric(g t)` with `Ric` a function of `g`**; `ricciFlow_shortTime_existence` (`proof_wanted`); the analytic-frontier survey lives in its header |
| `Hamilton.lean` | `hasConstSecLC_tensor`/`_ricci`/`_scalar` — **constant sectional curvature delivers the full `(0,4)` tensor, `Ric = (n−1)k g` and `scal = n(n−1)k`** at the canonical Levi-Civita connection (`ConstantCurvature.lean` instantiated through `hasConstSecLC_iff_mul`); `hamilton_1982` — **stated**, `proof_wanted`, no sorry, no axiom. Predicates require a `C¹` witness and `C²` test fields (corrected 2026-09-04: the old `HasConstSecLC` quantified over arbitrary fields, i.e. over junk). `admitsPositiveRicciMetric_iff` / `admitsConstPositiveSecMetric_iff` restate them via `ricciOfMetric` / `sectionalCurvatureOfMetric` |
| `Pinching.lean` | Hamilton's curvature ODE in dimension 3 — ordering, positive Ricci, `λ ≤ C(μ+ν)` preserved; `pinching_antitone` (Hamilton Thm 10.1, ODE half). Linear Grönwall helpers `nonpos_of_deriv_le_mul` etc. No manifold. **Reopened for Hamilton–Ivey**: `iveyF` (= `x(log x - 3)`), `hasDerivAt_iveyF`, `iveyF_le_neg_three`, `IsIveyPinched` (Hamilton's set with no `f⁻¹`), `isIveyPinched_of_neg_one_le`, `le_of_isIveyPinched`, `iveyE`/`iveyE_nonneg` (the unified boundary polynomial), `iveyPsi`/`iveyPsi_nonneg` (the Grönwall), `nonneg_preserved`, `shift`, `restrict`, `neg_of_neg`, and **`hamiltonIvey`** (the ODE half, complete); `hasDerivAt_scal`, `le_scal` (`Ṙ = ½[(λ+μ)²+(λ+ν)²+(μ+ν)²] ≥ 0`, so any lower bound on `R` is preserved — the first inequality of Hamilton's set `K`), `hamiltonIvey_boundary_of_nonneg`/`_of_neg` (the boundary check with the log eliminated; pure polynomial, Cao–Zhu Cases (i)/(ii)). **Normalisation**: `λ,μ,ν` are *twice* the sectional curvatures, `R = λ+μ+ν`, Ricci eigenvalues `(μ+ν)/2` etc. — the header said `μ+ν`, off by 2; every proved statement is a sign or ratio claim so none moved |
| `GramSchmidtOrtho.lean`, `OrthonormalFrame.lean` | **Not ours.** Ported from unmerged mathlib PR #26221 (grunweg), Apache-2.0, see `NOTICE.md`. Gives `Module.Basis.orthonormalFrame` and `contMDiffAt_orthonormalFrame_of_mem`: a `C^k` **orthonormal** local frame of a Riemannian bundle, by pointwise Gram–Schmidt on `Trivialization.localFrame`. Mathlib has `IsLocalFrameOn` but no orthonormal version; it names the planned file in `LocalFrame.lean`'s header. **Delete when #26221 lands.** Upstream's one `sorry` (`contMDiffOn_iff_coeff'`, marked unused) was dropped |
| `GlobalExtension.lean` | **proved, for an ARBITRARY `C^k` vector bundle** (`section Bundle`: `exists_contMDiff_eventuallyEq_of_bundle`, `exists_contMDiff_extension_of_bundle`, `forall_contMDiff_iff_forall_fiber`), the tangent-bundle statements being corollaries — nothing in the bump argument is about `TangentSpace`, mathlib's `FiberBundle.exists_contMDiffOn_extend` and `ContMDiffOn.smul_section_of_tsupport` being already general. **This is what `lem:pinching` needs**: the curvature operator is a section of `Sym²(Λ²TM)`, so a maximum principle for it cannot quantify over a constant direction vector. Gotchas: the general conclusion must be `∀ᶠ y in 𝓝 x, τ y = σ y`, not `τ =ᶠ[𝓝 x] σ` (`Filter.EventuallyEq` wants a non-dependent codomain, and `Π y, V y` is non-dependent only for `TangentSpace`, where it reduces to `E`); `I` is determined by nothing in a general-bundle statement, so call sites need `(I := I)`; and the bump is only `C^∞`, so the level must be `{n : ℕ∞}` with `(n : ℕ∞ω)` — a bare `{k : ℕ∞ω}` fails on `k ≤ ∞`. `exists_contMDiff_extension` (a global `C^k` section through any prescribed `v ∈ T_xM`, via `FiberBundle.exists_contMDiffOn_extend` plus a bump), `exists_contMDiff_two_extension`, and `forall_contMDiff_iff_forall_tangent` — quantifying over globally `C²` fields **is** quantifying over tangent vectors. This is what makes the `Hamilton.lean` predicates non-vacuous |
| `CurvaturePointwise.lean` | **proved**: `curvature_smul_third`, `curvature_sum_smul_third`, `curvature_congr_third`, `curvature_pointwise_third` (curvature depends on the third slot only through its value at the point), `ricci_congr_of_eq`, **`ricciAt`** (Ricci as a genuine function of two tangent vectors) + `ricciAt_eq`, `sectionalCurvature_congr'`, `inner_curvature_eq_zero_of_dep` |
| `Hessian.lean` | `hessian` (∇²), `hessian_sub_hessian_swap` (Ricci identity), tensoriality + `hessianAt`, `hessianFun` + `hessianFun_symm`, `laplacian` + `laplacian_eq_sum` + `laplacian_eq_sum_frame` (basis-independent metric trace, `OrthonormalBasis.sum_apply_self_eq` — which traces bilinear maps into **any topological `ℝ`-module**, no norm needed in the codomain (every step is an algebraic rearrangement of a finite sum; nothing is estimated) — this is what lets `BundleHessian.lean` be norm-free on `V` and hence specialise to `TM`; so the vector-valued trace needs no pairing against a covector, unlike `scal`). **This file already is `∇²` on sections and the rough Laplacian** — `hessian X Y Z x` is `∇²_{X,Y}Z`, `hessian_sub_hessian_swap` is the Ricci identity, and `hessianAt` is the bilinear map via `TensorialAt.mkHom₂`, which applies because `hessian_smul_right` needs only `MDiffAt`. Do not rebuild it: no frame-and-globalise argument is needed here, unlike the third slot of `curvature` |
| `BundleHessian.lean` | **proved**: **`∇²` and `Δ` for a section of a general vector bundle.** `hessianSection covT X Y σ x = ∇_X(∇_Yσ) − ∇_{∇^T_XY}σ` with **two connections** — `cov` on `V`, which is differentiated, and `covT` on `TM`, which supplies the correction; tensoriality in `Y` is the cancellation between the *two* Leibniz rules and needs both. Then `hessianSectionAt` (`mkHom₂` with `A = V x`), `laplacianSection`, `laplacianSection_eq_sum`/`_eq_sum_frame`. The only new brick is **`mdiffAt_cov_apply_section`**: `y ↦ ∇σ(y)` is a section of `Hom(TM,V)`, which is exactly what `ContMDiffCovariantDerivative` asserts to be `C¹` — the tangent proof transfers with `F` in place of the second `E`. **NOTHING here asks `V` for a norm**, and that is what lets the whole file specialise to `TM`: `hessianSection_eq_hessian`, `hessianSectionAt_eq_hessianAt` and `laplacianSection_eq_laplacian` are all **`rfl`**. The fibres carry only mathlib's standard general-bundle binders (`AddCommGroup`, `Module`, `TopologicalSpace`, `IsTopologicalAddGroup`, `ContinuousSMul`). **A fibre norm would break the bridge outright** — see corrected belief 4 |
| `BundleBochner.lean` | **proved**: **the Bochner identity for a section of a general bundle** — `Δ⟪σ,τ⟫ = ⟪Δσ,τ⟫ + ⟪σ,Δτ⟫ + 2∑ᵢ⟪∇_{eᵢ}σ,∇_{eᵢ}τ⟫` (`laplacianFun_inner_section_eq`), plus the squared-norm form and **`Δ|σ|² ≥ 2⟪Δσ,σ⟫`**. **The argument does not change at all**: metric compatibility twice, the Hessian's correction expanding into exactly the two corrections that turn the iterated derivatives into `∇²σ`, `∇²τ`. Mathlib's `IsMetricCompatible.mvfderiv_inner_eq` is already general, and the `(V := …)` annotations `Bochner.lean` needs against lean4#14949 are **unnecessary here** — the fibre instances are binders, so there is nothing to see through. `covT` need be neither metric nor torsion-free. **`IsMetricCompatible` DOES elaborate in this setting**, contrary to what the `RiemannianBundle` rule below might suggest — the rule bites only for `TangentSpace` + a `letI`-introduced `RiemannianBundle`, not for an ambient binder with `cov` an explicit variable. **The fibre metric arrives as `[RiemannianBundle V]`, NOT as plain `[∀ x, NormedAddCommGroup (V x)]` binders**, and that choice is load-bearing: it is what lets the file specialise to `TM` (corrected belief 4). **`laplacianFun_inner_eq_of_bundle` derives `Bochner.lean`'s theorem from this one**, with `example : @laplacianFun_inner_eq_of_bundle = @laplacianFun_inner_eq := rfl` forcing the two *types* to be defeq — **an unfalsified general theorem is a liability**, and until that bridge existed the general identity had no instantiations at all. Gotcha: `Metric.lean` is **not** transitively imported — `import Mathlib.Geometry.Manifold.VectorBundle.CovariantDerivative.Metric` explicitly, or the error is "environment does not contain `CovariantDerivative.IsMetricCompatible`" |
| `BundleNormalSection.lean` | **proved**: **a normal section of a general bundle** — for every `v ∈ V x` a global `C²` `N` with `N x = v`, `∇N(x) = 0` **and** `ΔN(x) = 0` (`exists_contMDiff_section_normal_of_bundle`), on top of `exists_contMDiff_section_cov_eq_zero_of_bundle`, `cov_sum_smul_section_apply_of_bundle`, `contMDiff_cov_apply_section`, `hessianSection_smul_of_vanishing`, `hessianSection_add`/`_sum`. Still **no parallel transport and no `exp`**. **The general case is SIMPLER than the tangent one, in one specific way**: there the correction sections and the trace frame are the *same* family, so the second-order step is `∑ⱼ⟪bᵢ,bⱼ⟫² = 1`; here they live in different bundles (corrections through an orthonormal basis of `V x`, trace over one of `T_xM`), and what replaces it is that **one function `g` suffices** — tracing `∇²(½a g²)(x) = a·dg⊗dg` gives `a·∑ⱼ dg(Eⱼ)²`, which is `a` as soon as `dg(x)` is a **unit covector**, so `dg(x) = ⟪E_{j₀},·⟫` corrects every component at once. **New edge case**: `T_xM = 0` admits no unit covector — but then `Δ` is an empty sum, `ΔN₁(x) = 0` already and `N₁` is the answer (`rcases isEmpty_or_nonempty (Fin (finrank ℝ (TangentSpace I x)))`). **`exists_contMDiff_section_normal_of_bundle'` derives `NormalSection.lean`'s theorem from this one**, with the same `rfl` type check as in `BundleBochner.lean`. Gotchas: `Finset.sum_empty` leaves `AddCommMonoid ?m` stuck — go through `laplacianSection_eq_sum` and `simp`; and `have := hemp`, not `haveI`, for an `IsEmpty` (the `haveILetI` linter is a CI error) |
| `BundleMaximumPrinciple.lean` | **proved**: **the touching-point step of the maximum principle on a general bundle** — the piece that transposes `ManifoldMaximumPrinciple.lean` from a *trivial* bundle, where the tested direction is literally the same vector everywhere, to one where `n ∈ V x` only. `laplacianFun_inner_normal_eq`: at a **normal** section (`∇N(x) = 0`, `ΔN(x) = 0`) the Bochner expansion of `Δ⟪N,u⟫` collapses to `⟪N x, Δu x⟫` — **both** corrections die on `∇N(x) = 0` (the gradient pairing term by term, `⟪ΔN,u⟫` by the second-order condition), so `BundleNormalSection.lean`'s second stage is load-bearing and not a luxury. Then `inner_laplacianSection_nonpos_of_isLocalMax`/`_nonneg_of_isLocalMin` (the second-derivative test on the resulting *scalar*) and **`exists_section_inner_laplacianSection_nonpos`**, the packaged form: for every `n ∈ V x` a global `C²` `N` through `n` such that a spatial max of `⟪N,u⟫` at `x` forces `⟪n, Δu x⟫ ≤ 0`. **The direction is a vector in one fibre and the conclusion is about that vector; the section is an artefact of the proof**, hence the existential. **No parallel transport and no `exp`** — only first- and second-order behaviour at one point is used. `[I.Boundaryless]` enters exactly once, through the second-derivative test; `covT` need be neither metric nor torsion-free. Tangent-bundle instantiations `laplacianFun_inner_normal_eq_tangent` and `inner_laplacian_nonpos_of_isLocalMax_tangent` are the falsification check. **What this does NOT do**: the cross-fibre half of the first-touching-time argument, which compares `dist(u(t,x), K_x)` between *different* fibres — that is still what a trivialisation (Uhlenbeck, or Hamilton's moving fibre metric) is for. Needs `[IsContMDiffRiemannianBundle I 2 F V]`, not just `1` — **tested 2026-09-16** by deleting the level-2 binders, which breaks all three touching-point theorems: `ContMDiffAt.inner_bundle` is indexed by the level the second-derivative test asks for. (The level-`1` binders are then redundant, level `2` implying them; they are kept only because the file reads better with both named.) |
| `SecondDerivativeTest.lean` | **proved**: `deriv2_nonneg_of_isLocalMin`, `fderiv2_nonneg_of_isLocalMin`, `fderiv_fderiv_apply_nonneg_of_isLocalMin` (chart-side core), `hessianFun_nonneg_of_isLocalMin` and `laplacianFun_nonneg_of_isLocalMin` on a **boundaryless manifold** (transport through `extChartAt`, same pattern as `mlieBracket_apply_fun`), plus the `*_model` versions, and **`laplacianFun_nonpos_of_isLocalMax`** (a max of `f` is a min of `-f` and `Δ` is odd, so nothing is reproved — added for `BundleMaximumPrinciple.lean`). The connection term `(∇_X X) f` dies at a critical point, so any `cov` works |
| `MaximumPrinciple.lean` | **proved**: the scalar maximum principle on a compact space with the differential inequality assumed at spatial minima (`le_of_deriv_ge_at_min`, `le_of_deriv_le_at_max`), and **with a time-dependent reaction** (`le_of_deriv_ge_at_min_time`, 2026-09-13 — the comparison ODE becomes the non-autonomous `φ' = F t (φ)`), the autonomous ones being corollaries. Same three-point argument as the tensor version. ε-perturbation `φ − ε e^{(2K+1)t}` + first touching time. No Laplacian |
| `TensorMaximumPrinciple.lean` | **proved**: Hamilton's tensor maximum principle abstractly. **The reaction may depend on time** (`mem_of_deriv_le_at_max_time`, 2026-09-13), with `mem_of_deriv_le_at_max` the autonomous corollary — `F` enters the proof at exactly **three** points (the hypothesis at the touching point, the Lipschitz comparison against the nearest point, subtangentiality) and all three are at the single time `t₀`, so the generalisation is mechanical; only the Lipschitz constant must be uniform in `t`. **This is the generality a moving background geometry needs** — Hamilton 1982 §9 makes the reaction non-autonomous, where Uhlenbeck's trick instead freezes the fibre metric. **`mem_of_deriv_le_at_max_set` (2026-09-15) moves the SET too** (monotone `K s ⊆ K t`, plus joint continuity of `(t,v) ↦ infDist v (K t)` — stated in the form the proof consumes, not as a Hausdorff condition). **Monotonicity is felt in exactly one place**: the nearest point and normal are taken in `K t₀` at the single touching time, so every step *at* `t₀` is verbatim; only the earlier times need `infDist v (K t₀) ≤ infDist v (K t)`, which `K t ⊆ K t₀` gives free. **The shrinking case is genuinely not free** — it needs a retreat-rate bound coupled to `F`, since a set closing faster than the reaction pushes inward is not preserved. **And the payoff is bigger than the `log(1+t)` improvement it was filed under: a moving INNER PRODUCT reduces to a moving SET** — `⟪·,·⟫_t = ⟪P_t ·,·⟫`, `S_t = P_t^{1/2}` is an isometry, and `u ∈ K` becomes `S_t u ∈ S_t(K)`; **Uhlenbeck's trick is exactly the choice of `S_t` making `S_t(K)` constant**, so the two are the same bill and the monotone half of it is now paid. What the time-dependent reaction does NOT buy on its own: a moving inner product on `V` itself. `infDist (·) K` and the nearest-point projection are taken in `V`'s own metric at every time and the first-touching-time argument compares them *across* times — that is the real content of Uhlenbeck's trick and it is untouched. **The time-dependent set `K_t` is DONE in the monotone case** (`mem_of_deriv_le_at_max_set`) — and it is not the `log(1+t)`-only side quest it was filed as: a moving inner product reduces to a moving set by the square root, so this and Uhlenbeck's trick pay the same bill. Statement (`mem_of_deriv_le_at_max`): `K` closed convex in a complete real inner product space, hypothesis `⟪n, ∂ₜu⟫ ≤ ⟪n, F(u)⟫` at spatial maxima of `⟪n,u⟫`, ODE-invariance in Nagumo form `⟪n, F p⟫ ≤ 0` for outward normals (`subtangential_of_invariant` derives it). Nearest point via `exists_norm_eq_iInf_of_complete_convex` + `norm_eq_iInf_iff_real_inner_le_zero`; distance via `Metric.infDist`, `le_infDist`. Same skeleton as the scalar one |
| `ManifoldMaximumPrinciple.lean` | **proved**: both principles on a closed Riemannian manifold (`le_of_laplacian`, `le_of_laplacian'`, `mem_of_laplacian`, and `mem_of_laplacian_time` for `∂ₜu = Δu + F t (u)`) for `∂ₜu = Δu + F(u)`, `u(t,·)` `C²`: the abstract theorems with the touching-point hypothesis discharged by `laplacianFun_nonneg_of_isLocalMin`. Tensor case is the trivial bundle, equation componentwise `⟪n, ∂ₜu⟫ = Δ⟪n,u⟫ + ⟪n, F u⟫`; a max of `⟪n,u⟫` is a min of `⟪-n,u⟫`, so no linearity lemma is needed. `hessianFun_neg`, `laplacianFun_neg` added to `Hessian.lean` |
| `Variation.lean` | **proved**: `covBilin` (∇ of a bilinear form field), `koszul_bilin_eq` (Koszul combination of a symmetric `h` through a torsion-free `∇` is `∇h`-terms `+ 2h(∇_X Y,Z)`), `leviCivitaOfMetric`, `inner_leviCivitaOfMetric_eq` (Koszul in `g.inner`), `hasDerivAt_inner_leviCivitaOfMetric` (Koszul differentiated in `t`), `inner_deriv_leviCivitaOfMetric_eq` (**first variation of ∇**). Hypotheses: `∂ₜ` commutes with `X(g(Y,Z))` for the fields at hand; differentiability of `t ↦ ∇ᵗ_X Y` (vector form only) |
| `CurvatureVariation.lean` | **proved**: `covEnd` (∇ of an `End`-valued one-form), `curvature_eq_add_covEnd` (curvature of `∇ + A`, `∇` torsion-free — algebraic), `hasDerivAt_curvatureE` (`∂ₜ Rᵗ = (∇_X Ȧ)(Y,Z) − (∇_Y Ȧ)(X,Z)` along `∇ᵗ = ∇ + Aᵗ`), `exists_hasDerivAt_clm_of_apply` (coordinatewise ⇒ CLM-valued derivative), `differenceE` (Mathlib's `difference` on `E`), `derivDifferenceE` (`Ȧ = ∂ₜ∇` as `deriv`, no existential), `inner_derivDifferenceE_eq`, `hasDerivAt_curvatureE_leviCivitaOfMetric` (**first variation of Rm along metrics**). Hypotheses: `CommutesWithMvfderiv` (the `Variation.lean` commutation, all fields) and `∂ₜ`/`∇_X` commuting on `Aᵗ(Y,Z)` |
| `Bianchi.lean` | **proved**: `contMDiff_cov_apply` (`C^k` connection, `C^{k+1}` section, `C^k` field ⇒ `C^k` covariant derivative), `mlieBracket_sub_left'`, `covCurvature` (`(∇_X R)(Y,Z)W`), `bianchi_second` (**second Bianchi**, `C²` connection, `C²` fields, `C³` argument; no metric). The proof is the first-Bianchi pattern: split the sections, rewrite `∇_X Y − ∇_Y X` as `[X,Y]` in both the direction slot and as sections, `linear_combination (norm := module)` with Jacobi |
| `CurvatureDeriv.lean` | **proved**: **`∇R` is tensorial in all four slots.** *Direction* (`covCurvature_smul_dir`, `_add_dir`, with `cov_smul_dir`): each of the four terms picks up one `f x`, since `∇_{f•X} = f∇_X` and `R` is already tensorial in the slot it lands in (the third via `curvature_smul_third`). *`Y`* (`covCurvature_smul_snd`, `_add_snd`): a cancellation, not a rescaling — `∇_X(f•R(Y,Z)W)` gives a Leibniz term that `R(∇_X(f•Y),Z)W` cancels; applying Leibniz needs `y ↦ R(Y,Z)W y` to be a *differentiable section*, which is **`contMDiff_curvature`** (also here: a `C³` triple has a `C¹` curvature section, two `contMDiff_cov_apply` plus mathlib's `ContMDiffAt.mlieBracket_vectorField`). *`Z`* (`_smul_thd`, `_add_thd`): free from `Y` by **`covCurvature_antisymm`**. *`W`* (`covCurvature_smul_fth`, `_add_fth`): the same cancellation with the other pairing — `R(Y,Z)(f•W) = f·R(Y,Z)W` as *sections*, so `∇_X` contributes `(Xf)·R(Y,Z)W` and `∇_X(f•W) = f∇_X W + (Xf)W` contributes the match. **`f` must be `C³` here**, not `C²`: the second Leibniz term is `R(Y,Z)((Xf)•W)` and third-slot tensoriality wants its coefficient `C²`. Needed `curvature_smul_third'` (in `CurvaturePointwise.lean`) — the third-slot linearity asking of slots 1–2 only the `C¹` its proof uses, since the fields there are `∇_X Y`, `∇_X Z`. With all four slots, `div Rm(Y,Z,W) = ∑ᵢ ⟪(∇_{eᵢ}R)(Y,Z)W, eᵢ⟫` is well defined and frame-independent. **`∇Rm` is also pointwise in all four slots** (`covCurvature_congr_snd`/`_thd`/`_fth`, with `_congr_dir` in `Divergence.lean`) — so `∇Rm` is a genuine tensor. Slots 2 and 4 are the `CurvaturePointwise` frame-and-globalise argument one level up; locality is cheap because every term but the leading one is `Rm` in a tensorial slot, so only `∇_X(Rm(Y,Z)W)` needs the germ. Slot 3 is free from slot 2 by `covCurvature_antisymm`. **Slot 4 runs one derivative higher**: `covCurvature_smul_fth` wants a `C³` coefficient, so the frame and the coefficients are globalised at `C³` and the frame comes from `IsOrthonormalFrameOn … 3` (needing `IsContMDiffRiemannianBundle I 3`). Towards `Δ Rm`: `contMDiff_curvature_two` (`C⁴` fields ⟹ `C²` curvature section) and `contMDiff_covCurvature` (`C³`/`C⁴` fields ⟹ `C¹` `∇Rm` section). Levels there are hard-coded: `ContMDiffAt.mlieBracket_vectorField` indexes by `ℕ∞` while `ContMDiff` indexes by `ℕ∞ω`, and a variable level costs more cast bookkeeping than the twin proof. Note `T%` wraps the section, so `rw` cannot see through `curvature`'s definition — use `show`; and a Pi-`add`/`smul` residue after `simp only [add_apply]` is closed by `rfl`, not by `Pi.add_apply` |
| `Bochner.lean` | **proved**: **the Bochner identity** `Δ⟪σ,τ⟫ = ⟪Δσ,τ⟫ + ⟪σ,Δτ⟫ + 2∑ᵢ⟪∇_{eᵢ}σ,∇_{eᵢ}τ⟫` for a metric connection. `hessianFun_inner_eq` is the pointwise Hessian form — metric compatibility twice, and the Hessian's correction `−(∇_XY)⟪σ,τ⟫` expands (again by compatibility) into exactly the two corrections that turn the iterated derivatives into `∇²σ`, `∇²τ`. The trace is over the frame `laplacian`/`laplacianFun` are *already* defined on (canonical extensions of `stdOrthonormalBasis`), so no frame-independence is needed as input; the factor `2` is the two cross terms coinciding at `X = Y`. `laplacianFun_inner_self_eq` (`Δ|σ|² = 2⟪Δσ,σ⟫ + 2∑|∇_{eᵢ}σ|²`) and `two_mul_inner_laplacian_le` (`Δ|σ|² ≥ 2⟪Δσ,σ⟫`, the form `thm:max-scalar` consumes). **`sum_inner_cov_congr`** — the gradient term is frame-independent *on its own*, with no metric compatibility and no Bochner: `(v,w) ↦ ⟪∇_vσ,∇_wτ⟫` is a continuous bilinear form because `cov σ x` is a CLM, so `sum_apply_self_eq` applies. Also `MDifferentiableAt.inner_bundle'` (lean4#14949 wrapper, upstream candidate). Hypotheses are weak: `σ,τ` `C²`, and the direction fields need nothing (`X`) or `MDiffAt` (`Y`) |
| `CurvatureLaplacian.lean` | **proved**: **`Δ Rm` exists.** `cov2Curvature` = `∇²Rm`, with **one correction per slot of `∇Rm` — five terms**. `cov2Curvature_congr_dir` (pointwise in the outer direction) needs **no frame argument**: it is the four-slot pointwise lemma of `CurvatureDeriv.lean` cashed in — every term is a CLM at `X x` or `∇Rm` in a pointwise slot applied to a field whose value at `x` is determined by `X x`. The inner slot is the usual Leibniz cancellation (`_smul_snd`/`_add_snd`, coefficient `C³` so that `(Xf)` is `C²`) plus `cov2Curvature_congr_snd`, the frame-and-globalise argument at `C³`. Then `cov2CurvatureAt` (on `exists_contMDiff_three_extension`, new in `GlobalExtension.lean`), `cov2CurvatureBilin` as a `T_xM →L T_xM →L T_xM`, and `curvatureLaplacian` as its trace with `_eq_sum_basis`/`_eq_sum_frame` — frame-independence free by `sum_apply_self_eq`, vector-valued with no pairing. Regularity: `X,Y,A,B` `C³`, `C` `C⁴`. **The last slot is the expensive one at every level of the tower**: `C` needs four derivatives because `covCurvature_congr_fth` needs `C³` coefficients because `curvature_smul_third` needs `C²`. `Δ Rm` is the right-hand side of `∂ₜRm = ΔRm + Q`; the geometry side is done, `∂ₜRm` is still model-space |
| `Divergence.lean` | **proved**: **`div Rm` exists.** `covCurvature_congr_dir` — `(∇_X Rm)(Y,Z)W` at `x` depends only on `X x`, and this needs **no new frame argument**: each of the four terms is separately pointwise in `X` (the first is a CLM at `X x`; the middle two are `Rm` in a tensorial slot via mathlib's `TensorialAt.pointwise` on `tensorialAt_curvature_fst`/`_snd`; the last is `curvature_congr_third`). With direction-slot linearity that gives `covCurvatureAt` (on `exists_contMDiff_two_extension`, as `ricciAt` is) and the endomorphism `covCurvatureEndo`; `divCurvature` is its **endomorphism** trace — no metric, unlike `scal`, which is a *metric* trace of a bilinear form. So `divCurvature_eq_sum_basis` (frame-independence) is just `LinearMap.trace_eq_sum_inner`, and `divCurvature_eq_sum_frame` reads it off any `C²` frame orthonormal at `x`. Also **`divCurvature_eq_sub_sum`** — the **traced second Bianchi identity**: `div Rm(Y,Z,W) = ∑ᵢ⟪(∇_Y Rm)(eᵢ,Z)W,eᵢ⟫ − ∑ᵢ⟪(∇_Z Rm)(eᵢ,Y)W,eᵢ⟫`, by pairing `bianchi_second` with `eᵢ` and turning the middle term round with `covCurvature_antisymm`. No derivative of the frame appears — the identity is pointwise in `eᵢ` and only summed. Note `omit [CompleteSpace E]` fails on it (`bianchi_second` references the instance). **`sum_inner_covCurvature_eq`** closes the gap that left: `∑ᵢ⟪(∇_X Rm)(eᵢ,Z)W,eᵢ⟫ = (∇_X Ric)(Z,W)` (`covRicci`), the first-slot trace commuting with `∇`. Same mechanism as `TraceCov.lean` — the frame is not parallel, so `⟪∇_X eᵢ, eⱼ⟫` appears and `inner_cov_antisymm` + `sum_bilin_of_antisymm` kill it — but the bilinear form has to be *produced*: `curvatureBilinFst = (innerSL ℝ).comp (mkHom …)`, available only because `Rm` is tensorial in its first slot. Hence **`divCurvature_eq_covRicci_sub`**: `div Rm(Y,Z,W) = (∇_Y Ric)(Z,W) − (∇_Z Ric)(Y,W)`, the **contracted second Bianchi identity, first contraction**. Both connection hypotheses are used and for different things — torsion-freeness for `bianchi_second`, metric compatibility for the trace. **`two_mul_sum_covRicci_eq_mvfderiv_scalarCurvatureAt`** is the **second contraction**, `2 div Ric = d scal`: sum the first contraction over `Z = W = eⱼ`; on the right `TraceCov` gives `Y(scal)` and `covRicci_symm` (from `ricci_symm`) gives `div Ric(Y)`, on the left pair symmetry of `∇Rm` plus `sum_inner_covCurvature_eq` gives `div Ric(Y)` again. **Pair symmetry of `∇Rm` is not inherited for free**: `inner_curvature_pair_symm` is about the `(0,4)` tensor, while `covCurvature` is an operator on fields. `inner_covCurvature_expand` bridges them — for a *metric* connection `∇_X` moves onto the pairing, so `⟪(∇_X Rm)(A,B)C,D⟫` is `X⟪Rm(A,B)C,D⟫` minus **five** corrections, one per slot plus the metric one `⟪Rm(A,B)C,∇_X D⟫` — and the two sides' corrections are the same five numbers reordered. Frame must be `C³` there so `∇_X eᵢ` is `C²`. **Typing gotcha, load-bearing**: `sum_bilin_of_antisymm` wants `B : E →L[ℝ] E →L[ℝ] ℝ`, so `curvatureBilinFst` must be *declared* at that type (body ascribed through `TangentSpace I x →L …`) — declared at the `TangentSpace` type it is defeq but every later `rw` fails with "did not find the pattern" on a goal that prints identically. Also `innerSL_apply_apply`, not `innerSL_apply`; and `mkHom_apply` is reached by `show`, not `simp only`. Gotchas: `open scoped Classical in` for the `dite`, and it goes **before** the docstring like `omit`; `simp only [d, h, and_self, ↓reduceDIte]` then `rfl` (naming `LinearMap.coe_mk` trips the unused-simp-arg linter, which is a CI error) |
| `MetricTrace.lean` | **proved**: `sharpE` (`g♯⁻¹ ∘ B♭`), `metricTraceE` (`tr(g♯⁻¹ B♭)`), `metricTraceE_eq_sum` (= `∑ᵢ B(eᵢ,eᵢ)` over any `g`-orthonormal basis, via `LinearMap.trace_eq_sum_inner`), `sharpE_apply_eq_sum`, `metricTraceE_comp_sharpE_eq_sum` (`⟨h,B⟩_g`), `hasDerivAt_inverse_innerE` (`∂ₜ g⁻¹ = −g⁻¹ h g⁻¹`, from `contDiffAt_map_inverse` plus differentiating `g ∘ g⁻¹ = id`), `hasDerivAt_metricTraceE` (**`∂ₜ tr_{g_t} B_t = tr Ḃ − ⟨h,B⟩`**). All at a point on `E`. `metricTraceE_innerE_comp`, `ricci_eq_metricTraceE` bridge to `ricci`. **Two inequalities for the pairing** (2026-09-11): `metricTraceE_comp_sharpE_self_nonneg` (`⟨B,B⟩_g ≥ 0` for symmetric `B` — a sum of squares over any `g`-orthonormal basis) and `sq_metricTraceE_le_card_mul` (**`(tr_g B)² ≤ n·⟨B,B⟩_g`**, Cauchy–Schwarz: Chebyshev `sq_sum_le_card_mul_sum_sq` on the diagonal, then the off-diagonal squares are discarded by `Finset.single_le_sum`). At `B = Ric` these are `|Ric|² ≥ 0` and `R² ≤ n|Ric|²`, the two facts the scalar-curvature estimates run on. Needs `import Mathlib.Algebra.Order.Chebyshev` — it is not transitively available, and the lemma is in the **root** namespace, not `Finset` |
| `Scalar.lean` | **superseded in scope, kept as the record.** The first pass at `scal`, with the metric a *fixed* `RiemannianBundle` instance and no quantification over metrics. Second-slot laws `ricci_zero_right`/`_add_right`/`_smul_const_right`/`_sum_right`, `tensorialAt_ricci_fst`, `ricci_congr_fst`/`_snd`, `ricci_combination_expand`, `ricci_eq_sum_inner_curvature`/`_frame`; `scalarCurvatureWith` and `scalarCurvatureWith_congr` — **frame-independence is an assumed hypothesis `h3` here**, and that is exactly what `RicciForm.lean` later discharges by making `Ric` bilinear. `scalarCurvature` itself, with `_eq_sum`/`_eq_sum_const`, is **model-space only** (`curvature_congr_third_model`), because the general third-slot pointwiseness did not exist yet. Its header states the two obstructions correctly and both are now closed — global `C²` extensions (`GlobalExtension.lean`) and the pointwise third slot (`CurvaturePointwise.lean`). Read it for why `RicciForm.lean` is shaped the way it is; do not build on it |
| `RicciForm.lean` | **proved**: **Ricci as an honest bilinear form and the scalar curvature on a general manifold**. `ricciAt_add_left`/`_smul_left`/`_add_right`/`_smul_right` (each field-level law applied to global `C²` extensions), `ricciForm x : E →L[ℝ] E →L[ℝ] ℝ` (`LinearMap.mk₂` + `toContinuousLinearMap` twice), `ricciForm_apply_field`; `scalarCurvatureAt` and `scalarCurvatureAt_eq_sum_basis` — **frame-independence is free** once `Ric` is bilinear (`OrthonormalBasis.sum_apply_self_eq`), so `scalarCurvatureWith_congr'` discharges the `h3` that `Scalar.lean` had to assume; `mvfderiv_scalarCurvatureAt_eq_sum_covBilin` = **`X(scal) = tr_g(∇_X Ric)`**, the trace lemma applied to `ricciForm` |
| `RicciSymm.lean` | **proved**: **Ricci is symmetric on a general manifold** for a metric torsion-free connection, with no hypotheses. `extendTwo`/`contMDiff_extendTwo`/`extendTwo_apply_self` (a *named* global `C²` extension — the anonymous `Exists.choose` of two vectors prints identically and `rw` cannot target one), `curvatureEndoAt` (the `hL` of `ricci_sub_ricci_swap`, now constructed: well-definedness from `curvature_congr_third`, linearity from `curvature_add_right`/`curvature_smul_const_right`), `trace_curvatureEndoAt_eq_zero` (skew-adjointness, via `LinearMap.trace_eq_sum_inner`), `ricci_symm`, `ricciAt_symm`, `ricciForm_symm`. Both hypotheses of `ricci_sub_ricci_swap` are now theorems |
| `CurvatureSymm.lean` | **proved**: **pair symmetry of the Riemann tensor** on a general manifold, `⟪R(X,Y)Z,W⟫ = ⟪R(Z,W)X,Y⟫` (`inner_curvature_pair_symm`) — the octahedron argument, four copies of first Bianchi linked by the two antisymmetries, closed by `linarith` in the six unknowns. `inner_bianchi_first` (first Bianchi paired against a vector; the fourth slot needs *no* regularity, the identity being a vector identity), `inner_curvature_left_skew`. This is what makes `R` an operator on `Λ²T_xM` — the form Hamilton's pinching and Uhlenbeck's trick need — and what the second contraction of second Bianchi will use |
| `CurvatureOperator.lean` | **proved**: **the Riemann tensor as a pointwise `(0,4)` form** — `curvatureTensorAt v₁ v₂ v₃ v₄ = ⟪R(v₁,v₂)v₃,v₄⟫` on four *tangent vectors*, with `curvatureTensorAt_apply_field`, additivity and homogeneity in **all four slots**, and the four symmetries pointwise (`_antisymm_fst_snd`, `_antisymm_thd_fth`, `_pair_symm`, `_bianchi`). **What was missing was a genuinely pointwise tensor**: the repo had `curvatureEndoAt` (two field slots fixed, a vector in the third) and `curvatureBilinFst` (last two fields fixed, a vector in the first), but nothing taking four vectors — which is what factoring through `Λ²` needs. **Three slots cost an extension, the fourth is free**: the first three are globalised by `extendTwo` and the fourth is paired against directly, so it needs **no regularity at all** (the observation `inner_bianchi_first` already rested on). Slots 1–2 are `tensorialAt_curvature_fst`/`_snd` through **`TensorialAt.pointwise`** — note `TensorialAt` exposes `.add`/`.smul` as *fields*, so use those rather than the raw `curvature_add_left` etc.; slot 3 is the `curvatureEndoAt` pattern one slot wider. **Antisymmetry in the second pair is NOT re-proved from metric compatibility** — it is two applications of pair symmetry around one sign flip, much shorter. Gotchas: `ContMDiff.const_smul_section`'s scalar is named **`a`**, not `c`; `TensorialAt.smul` wants a *function* `f : M → ℝ`, so the section is `(fun _ ↦ r) • σ`, which is defeq to `r • σ` but **`Pi.smul_apply` will not fire on it** — get the value equation by `show … = r • σ x` then `rw [extendTwo_apply_self]`, and get the differentiability by `exact` on the `r • σ` form. **The `Λ²` factorisation is done for the first pair** (`CurvatureLambda.lean`); the second is blocked on an elaboration wall, see that row. Mathlib supplies the target — `Analysis/InnerProductSpace/ExteriorPower.lean` puts an inner product on `⋀[ℝ]^n E` by the Gram determinant, with `OrthonormalBasis.exteriorPower` — and `exteriorPower.alternatingMapLinearEquiv` is the universal property; what it does **not** supply is a constructor for a rank-2 alternating map from a bilinear one, so the `MultilinearMap` over `Fin 2` must be hand-rolled (`map_update_add'`/`map_update_smul'` with a `Fin 2` case split) |
| `CurvatureLambda.lean` | **proved**: **the first pair of slots of `Rm` factored through `Λ²`** — `altOfBilin` (a **rank-2 alternating map from a bilinear one**, which Mathlib does not supply: `map_update_add'`/`map_update_smul'` with a `Fin 2` case split, the alternating condition reducing to `Rm(a,a,c,d) = 0`), `curvatureAltFst`, and `curvatureFunctional` = its image under `exteriorPower.alternatingMapLinearEquiv`, **characterised** by `curvatureFunctional_apply_ιMulti` (`Φ(a∧b) = Rm(a,b,c,d)`) — a definition is not landed here until it can be *applied*. Ten laws in the second pair (`_add_thd`/`_smul_thd`/`_add_fth`/`_smul_fth`/`_self`, at both levels), each proved on the `AlternatingMap` and carried across by `alternatingMapLinearEquiv` being a linear **equivalence** — **no induction over `Λ²` is used and none exists** (`exteriorPower.ιMulti_span` is there, a span-induction eliminator is not). **Everything is stated over `E`, never `TangentSpace I x`**: forming `⋀[ℝ]^2 (TangentSpace I x)` and unifying it against `⋀[ℝ]^2 E` sends `isDefEq` into the exterior algebra (a quotient of a tensor algebra) and it does not return, even at 4× heartbeats — the documented `E` vs `TangentSpace` wall one type former up, same remedy. No metric is used, so nothing is lost. **The second factorisation IS built, abstractly** (`sndAux`, `sndForm`, `sndForm_apply`): for any family `Φ c d : ⋀²V →ₗ ℝ` bilinear and alternating in `(c,d)`, the induced bilinear form on `Λ²V`, **defined, applied and characterised**. Linearity in the `Λ²` slot is carried **by hand**, so `alternatingMapLinearEquiv` is used only at codomain `ℝ`, never at a `LinearMap`-valued one. **`curvatureForm` is still absent, but the recorded diagnosis was WRONG and is corrected (2026-09-16)** — see corrected belief 5. Briefly: it is **not** the nested codomain (a variable of exactly type `⋀²E →ₗ (⋀²E →ₗ ℝ)` applies by `rfl` under the full instance pile; mathlib's own `LinearMap.BilinForm.exteriorPower` is that type and has a `simp` lemma applying it; and a *non*-nested `AlternatingMap`-valued map fails identically), and **not** `Λ²` (the abstract `sndForm` works end to end). `set_option diagnostics true` names the real cost: `isDefEq` unfolding **`RiemannianBundle.g` 202472 times and `RiemannianMetric.inner` 202476 times**, through `innerSL`/`mkContinuousOfExistsBound`, with the `f a =?= f b` heuristic on `toFun` 286390 times. **It is the METRIC** — `curvatureTensorAt` is `⟪Rm(a,b)c,d⟫` by definition, so every defeq comparison descends into the metric's construction. `irreducible` at three levels (inner defs, outer def, `curvatureTensorAt` itself) does not stop it. **Next attempt**: keep the metric out of the *term*, not just the construction — build the `(0,4)` tensor once as a bundled `E →L E →L E →L E →L ℝ` so `⟪·,·⟫` sits inside one opaque CLM, and factor that. Same lesson as `curvatureBilinFst` (declare at the `E` type) and `FlowKoszul` (`→ₗ` not `→L`). Gotchas: `decide` fails on `(1 : Fin 2) ≠ 0` because the `DecidableEq (Fin 2)` in scope is the `MultilinearMap` field's opaque instance-implicit — use `Fin.ne_of_val_ne (by norm_num)`; **`ω` is a reserved token** under `open scoped ContDiff`, so a bound `ω : ⋀[ℝ]^2 E` silently breaks elaboration; and a nested `namespace CovariantDerivative` inside `namespace RicciFlowBlueprint` creates a *different* namespace — close the outer one first |
| `ConstantCurvature.lean` | **proved**: **constant sectional curvature determines the whole `(0,4)` tensor** — `⟪R(X,Y)Z,W⟫ = k(⟪Y,Z⟫⟪X,W⟫ − ⟪X,Z⟫⟪Y,W⟫)` (`inner_curvature_eq_of_const_sec`, and `_norm` for the `‖·‖` form `hasConstSecLC_iff_mul` produces). `curvatureDefect` = `Rm − k·(model)`, with its four symmetries and four additivity laws; polarising slots 1&4 gives `T(A,B,B,C) = 0`, polarising 2&3 gives antisymmetry there, and a tensor antisymmetric in three consecutive slots is killed by first Bianchi (`3T = 0`). **Not a parity fix — a strengthening.** Their `admitsConstantPositiveSectionalCurvature` is the multiplied-out *sectional* identity `Rm(X,Y,Y,X) = c(g(X,X)g(Y,Y) − g(X,Y)²)`, which `hasConstSecLC_iff_mul` already matched; divergence 2 in `notes/hamilton-statement-comparison.md` was closed before this. What this adds is that the sectional identity determines the *whole* `(0,4)` tensor, so everything downstream of constant curvature can use `Rm` directly. Corollaries by tracing once and twice: `ricci_eq_of_const_sec` (`Ric = (n−1)k g`) and `scalarCurvatureAt_eq_of_const_sec` (`scal = n(n−1)k`), both over an arbitrary orthonormal basis with `n = Fintype.card ι`, so no `finrank` identification is needed |
| `IveyConvex.lean` | **proved**: **Hamilton's pinching set is closed and convex** — the input `thm:max-tensor` needs to carry Hamilton–Ivey from the ODE to the flow, and roadmap blocker (a). **`f⁻¹` is not needed and was never needed**: the disjunction in `IsIveyPinched` collapses to one inequality against `iveyG n = iveyF (max (-n) (e²))` (`isIveyPinched_iff_iveyG`), because `f` increases on `[e²,∞)` so the `-ν ≤ e²` branch gives `G = f(e²) = -e² ≤ -3`, which the *first* condition already supplies. `monotoneOn_iveyF`, `convexOn_iveyF` (via `MonotoneOn.convexOn_of_deriv` — `f' = log x − 2` is monotone), `convexOn_max_neg_exp_two`, `image_max_neg_exp_two`, `convexOn_iveyG` (`ConvexOn.comp`), `continuous_iveyG`; `iveyPinchedSet : Set (EuclideanSpace ℝ (Fin 3))` with `convex_iveyPinchedSet` and `isClosed_iveyPinchedSet` |
| `BilinDeriv.lean` | **proved**: **`∇h` is a tensor** for a bilinear form field `h`. Slot laws `covBilin_smul_dir`/`_add_dir`/`_congr_dir`, `_smul_snd`/`_add_snd`, `_smul_thd`/`_add_thd`; `tensorialAt_covBilin_dir`/`_snd`/`_thd`; `covBilinDir` (the direction as a linear form, by `mkHom`) and `covBilinForm` (`∇_X h` as a bilinear form, by `mkHom₂`) with `covBilinForm_apply`; `sum_covBilin_congr` (the metric trace of `∇_X h` is frame-independent *on its own*, by `sum_apply_self_eq`). **All three slots are cheap, unlike `∇Rm`'s four.** The direction slot needs *nothing*: `mvfderiv I f x` and `cov Y x` are already CLMs out of `T_xM`, so linearity and pointwiseness hold by construction. Slots `Y`/`Z` are the plain Leibniz cancellation on `MDiffAt` fields (the `hessian_smul_right` pattern), so no frame-and-globalise. One hypothesis is carried, `IsMDiffBilinAt h x` (`y ↦ h y (U y) (V y)` differentiable for differentiable `U`,`V`) — what makes Leibniz available. **Gotchas, all the `E` vs `TangentSpace` family**: `map_add`/`map_smul` will not `rw` when the `+`/`•` is `TangentSpace`-typed and the CLM is `E`-typed — state the fact at the `E` type (`key : ∀ c d v v' w : E, h x (c•v + d•v') w = …`) and close with `exact`, which is up to defeq. `(f • X) x` is not reduced by `Pi.smul_apply` on a dependent Pi — `have hfx : (f • X) x = f x • X x := rfl` then `simp only [hfx]`. And a hand-built `∘L` between `T_xM →L T_xM` and `E →L ℝ` will not unfold under `simp`; get the direction CLM from `TensorialAt.mkHom` instead |
| `BilinLaplacian.lean` | **proved**: **`∇²h` and `Δ_g h` for a bilinear form field.** `cov2Bilin` = `(∇²_{W,X}h)(Y,Z)`, with **one correction per slot of `∇h` — three terms**, against `cov2Curvature`'s five. The two derivative slots split the same way they do for `∇²Rm`: the **outer** direction `W` needs **no frame argument** (`cov2Bilin_congr_dir`, `_smul_dir`, `_add_dir`) — it is `BilinDeriv.lean` cashed in, every term being a CLM at `W x` or `∇h` in a slot it is already pointwise in; the **inner** direction `X` is the Leibniz cancellation (`_smul_snd`/`_add_snd`) plus `cov2Bilin_congr_snd`, the frame-and-globalise argument — but **at `C¹`**, not `∇²Rm`'s `C³`, because `cov2Bilin_smul_snd` asks its coefficient for one derivative. **The inner cancellation is cheap**: every step but the leading one uses only the *direction* slot of `∇h`, which carries no hypotheses. Then `cov2BilinAt` (on `exists_contMDiff_two_extension`), `cov2BilinForm` as a `T_xM →L T_xM →L ℝ`, and `laplacianBilin` as its trace with `_eq_sum_basis`/`_eq_sum_frame` — frame-independence free by `sum_apply_self_eq`. **`traceBilin`** (`tr_g h` as a function on `M`, frame-independent since `h y` is already a CLM) and **`hessianFun_traceBilin_eq`**: `∇²(tr_g h)(X,Y) = ∑ᵢ (∇²_{X,Y}h)(eᵢ,eᵢ)` — **the trace commutes with `∇²` too**, by the same mechanism as the divergence one level up: differentiating the frame sum twice gives `∇²h` **plus** exactly the `tr_g(∇_{∇_XY}h)` that the Hessian's connection correction subtracts. Same beta-redex hazard as `OneForm.lean`'s, same fix. One hypothesis beyond `IsMDiffBilinAt`: `IsMDiffCovBilinAt` (`y ↦ (∇_V h)(Y,Z)` differentiable for `C¹` `V`) — merely-`MDiffAt` `V` cannot supply it, since the leading term of `∇h` differentiates `h(Y,Z)` in the direction `V` and so sees the germ of `V`. That is exactly why the inner slot needs the frame argument and the outer one does not |
| `OneForm.lean` | **proved**: **`∇ω`, `div ω`, `div h` and `div div h`.** `covOneForm` (`(∇_X ω)(Y) = X(ω(Y)) − ω(∇_X Y)`) has **two** terms, against `covBilin`'s three and `covCurvature`'s four, so **both slots are free of any frame argument** — direction by construction, `Y` by the plain Leibniz cancellation on `MDiffAt` fields — and `mkHom₂` gives `covOneFormAt` directly; `divOneForm` is the trace, frame-independent by `sum_apply_self_eq`. For `ω = df` these are `Hessian.lean`'s `hessianFun`/`laplacianFun`. **The payoff is that `div div h` needs no four-linear packaging of `∇²h`**: it factors as `div(div h)`, and `div h` (`(div h)(Z) = ∑ᵢ (∇_{eᵢ}h)(eᵢ,Z)`) is the 1&2 trace of the `(0,3)`-tensor `∇h` — one `mkHom₂` over (direction, slot 2), `covBilinFstSnd` — which is tensorial in its remaining slot as a finite sum (`tensorialAt_sum`, a small general helper) and so an honest one-form field (`divBilinForm`, `divBilinOneForm`). `IsMDiffBilin` is the global form of `IsMDiffBilinAt`, needed because `div div h` differentiates `div h` as a *field*. **`hessianFun_eq_covOneForm` / `laplacianFun_eq_divOneForm` are `rfl`** — `∇²f(X,Y)` and `(∇_X df)(Y)` are literally the same expression — and that is load-bearing, not a remark: it **hands the Hessian its bilinear packaging for free**. `∇²f` on its own has no cheap route to one (a `TensorialAt` instance would need `y ↦ df_y(Y y)` differentiable for merely-`MDiffAt` `Y`, the same obstruction as `IsMDiffCovBilinAt`), but `∇ω` has two terms and needs no frame argument, so `laplacianFun_eq_sum_frame` falls out of `divOneForm_eq_sum_frame`. Hence **`laplacianFun_traceBilin_eq`**: `Δ(tr_g h) = tr_g(Δ_g h)`, the last identification the flow needs — both sides off the same frame, terms matched by `hessianFun_traceBilin_eq`, plus one `Finset.sum_comm`. Gotcha: `omit` needs the binder spelled out — `omit [RiemannianBundle (fun (x : M) ↦ TangentSpace I x)]`, not `fun x ↦ …`, or the omit silently fails and instance search sticks on `ChartedSpace H ?m`. **`covOneForm_divBilinOneForm_eq_sum`** — **the divergence commutes with `∇`**: `(∇_W(div h))(Z) = ∑ᵢ (∇²_{W,eᵢ}h)(eᵢ,Z)`, hence **`divDivBilin_eq_sum`**, `div div h = ∑_{j,i}(∇²_{eⱼ,eᵢ}h)(eᵢ,eⱼ)` — the iterated definition equals the double trace the flow produces. It is `TraceCov`'s lemma on `(v,w) ↦ (∇_v h)(w,Z)`: differentiating the frame sum gives `∇²h` **plus** exactly the `(div h)(∇_W Z)` that `∇_W` of a one-form subtracts, so no curvature term survives. **Gotcha, expensive**: matching `divBilinForm_apply` against a beta-redex (`(fun y ↦ cov Z y (W y)) x` vs `cov Z x (W x)`) sends the unifier through `mkHom` into the trivialisations behind `FiberBundle.extend` and it never returns — a `whnf` timeout that more heartbeats do not fix. Pass the value explicitly: `∀ v V, MDiffAt V x → V x = v → …`, applied with `rfl` |
| `KoszulSecondDeriv.lean` | **proved**: **differentiating the Koszul combination gives the same combination one level up, with no curvature terms.** `covTwoTensor` (`∇` of a `(1,2)`-tensor), `IsKoszulOf` (the characterisation `⟪A(P,Q),R⟫ = ½[(∇_Ph)(Q,R) + (∇_Qh)(R,P) − (∇_Rh)(P,Q)]` on `C¹` fields, which is what `inner_derivDifferenceE_eq` supplies for `A = ∂ₜ∇`), and **`inner_covTwoTensor_eq`**: `⟪(∇_U A)(P,Q),R⟫ = ½[(∇²_{U,P}h)(Q,R) + (∇²_{U,Q}h)(R,P) − (∇²_{U,R}h)(P,Q)]` for a metric connection. Metric compatibility moves `∇_U` onto the pairing; differentiating the germ identity gives the three `∇²h` terms **plus nine corrections**, which regroup three-by-three (one group per differentiated field) into exactly the three Koszul combinations that `∇_U A` and the pairing subtract — the regrouping is linear, so it is `linarith` once the four expansions are in hand. **This is what turns `∂ₜRm` into a statement about `∇²h` alone.** **`sum_inner_covTwoTensor_eq`** traces it twice: for symmetric `h`, `∑_{i,j}⟪Rm_A(eᵢ,eⱼ)eⱼ,eᵢ⟫ = div div h − tr_g(Δ_g h)` where `Rm_A(X,Y)Z = (∇_XA)(Y,Z) − (∇_YA)(X,Z)` is what `∂ₜRm` equals — **no curvature terms at any stage**; `sum_inner_covTwoTensor_eq_divDiv` names the two sums. **Symmetry of `h` is what collapses six double sums to two**: `cov2Bilin_symm` (from `covBilin_symm`, both in `BilinLaplacian.lean`) identifies `(∇²_{U,a}h)(b,c)` with `(∇²_{U,a}h)(c,b)`, merging the first two terms of each Koszul combination; the rest is one `Finset.sum_comm`. **`∇A` is also pointwise in both slots a traced vector can land in**: the *direction* is free (`covTwoTensor_congr_dir` — every term is a CLM at `U x`), and the tensor's *first argument* is the Leibniz cancellation, so `TensorialAt` applies and `covTwoTensor_congr_snd` needs no frame argument. One hypothesis, `IsMDiffTwoTensorAt` (`y ↦ A y (P y)(Q y)` differentiable for differentiable `P`,`Q`) — for `A = ∂ₜ∇` a regularity hypothesis on the first variation, carried alongside the commutation ones. Gotchas: `MDifferentiableAt.add` produces a *Pi*-add type, so `mvfderiv_fun_sub` picks the wrong higher-order unifier — ascribe `have hadd : MDiffAt (fun y ↦ a y + b y) x := hd₁.add hd₂` first; there is no `mvfderiv_fun_div_const`, so state the germ identity as `… = fun y ↦ 2 * ⟪…⟫` and use `mvfderiv_fun_mul mdifferentiableAt_const` with `mvfderiv_const` killing the other term |
| `FlowKoszul.lean` | **proved**: **`∂ₜRm` is the `Rm_A` of `KoszulSecondDeriv.lean`.** Two conventions had to be lined up: `covEnd` (which the curvature variation is phrased in) takes a `(1,2)`-tensor **argument first, direction second** — matching `differenceE` — while `covTwoTensor` takes the **direction first**, so the two agree on *flipped* tensors term for term (`covEnd_eq_covTwoTensor_flip`, a `rfl`). `derivDifferenceTensor` is that flip of `∂ₜ∇`; **`isKoszulOf_derivDifference`** says it is Koszul for `h = ∂ₜg` — `inner_derivDifferenceE_eq` states this on `FiberBundle.extend` fields and the `covBilin` congr lemmas transfer it to arbitrary `C¹` fields; **`hasDerivAt_curvatureE_covTwoTensor`** delivers `∂ₜRm` as `Rm_A` (also a `rfl` once the flip is right). **`derivCurvatureEndoE_apply_field`** moves `∂ₜ[v ↦ Rm(v,X)Y]` off the `extend` field the variation formula produces and onto an arbitrary differentiable field, which is what lets the double trace be read off a frame. **`metricTraceE_derivRicciFormOfMetric_eq`** then reads both traces off one frame — outer by `metricTraceE_eq_sum`, inner by `LinearMap.trace_eq_sum_inner` — and **`metricTraceE_derivRicciFormOfMetric_eq_sub`** combines with the double-trace theorem to give **`tr_g(∂ₜRic) = div div h − tr_g(Δ_g h)`** for the actual flow, with no curvature terms. **Two typing walls here, both with the same fix**: ascribing an `E →L[ℝ] E` to `T_xM →L[ℝ] T_xM` grinds `isDefEq` forever (a `whnf` timeout unaffected by 5× heartbeats) — ascribe to **`→ₗ[ℝ]`** instead and `change` the trace, exactly as `metricTraceE_eq_sum` does; and `⟪·,·⟫` will not elaborate on an `E`-typed value, so state the summand as an equation between *tangent vectors* (no inner product) and rewrite with it. Also: `isMetricCompatible_leviCivitaOfMetric` **cannot be stated** as a standalone lemma (CLAUDE.md's `IsMetricCompatible` rule — no ambient `RiemannianBundle` binder here); pass mathlib's `isMetricCompatible_leviCivitaConnection I (M := M)` directly at the application site, where the expected type is already fixed. **The `letI RiemannianBundle` hazard is real but survivable here**: the conclusion is written `letI : RiemannianBundle … := ⟨(g t₀).toRiemannianMetric⟩` before the `IsKoszulOf`, re-introduced in the proof by `let _ : … := …`, and `⟪·,·⟫` is then defeq to `(g t₀).inner y`. Gotcha: `.flip` on a `T_yM →L T_yM →L T_yM` needs a norm on `T_yM`, which only a `RiemannianBundle` supplies — so state the bridge with `A : M → E →L E →L E` and ascribe the flip to the `TangentSpace` type, as `covEndE` does |
| `ScalarEvolution.lean` | **proved**: **`div div Ric = ½ Δ scal`** — the second contraction of the second Bianchi identity with the divergence taken once more. `divBilin_ricciForm_eq` reads `2 div Ric = d scal` as an identity of **one-forms** rather than a frame sum: `divBilin` is the 1&2 trace of `∇Ric`, `covBilin_ricciForm_eq_covRicci` turns each term into `covRicci`, and `two_mul_sum_covRicci_eq_mvfderiv_scalarCurvatureAt` evaluates the sum. `divBilinForm_ricciForm_eq` upgrades that to a CLM identity on the frame's whole base set (so the germ is available), and **`divDivBilin_ricciForm_eq`** takes the divergence again, using `divOneForm_congr` (germ-locality) and `divOneForm_smul` (linearity in `ω`) plus `laplacianFun_eq_divOneForm`. Everything is stated for a fixed metric connection with an **ambient `RiemannianBundle` binder** — the `letI` hazard only bites at the final specialisation. Gotcha: writing `(c • (mvfderiv … : T_yM →L ℝ) : E →L ℝ)` gives the *TangentSpace* scalar action, so `divOneForm_smul` will not match; `obtain ⟨v, hv⟩ : ∃ v : M → E →L[ℝ] ℝ, v = … := ⟨_, rfl⟩` first and write `c • v y`, which is `E`'s. **`sum_cov2Bilin_neg_two_ricciForm_eq`**: at `h = −2Ric` the two canonical double traces are `div div h = −2·½Δscal = −Δscal` and `tr_g(Δ_g h) = Δ(tr_g h) = −2Δscal`, so their difference is **`Δ scal`** — i.e. `tr_g(∂ₜRic) = Δ scal` under the flow. Getting there needs **only** linearity of `∇²h` in `h` (`cov2Bilin_smul_form`, from `covBilin_smul_form` in `BilinDeriv.lean`): everything downstream is already proved for `Ric` itself, and `traceBilin ricciForm = scalarCurvatureAt` is `rfl` — the same frame sum by definition |
| `ScalarFlow.lean` | **proved**: **`∂ₜ scal = Δ scal + 2\|Ric\|²` on a general manifold** — the first evolution equation of the flow formalised here in full, and the close of roadmap item 2. `metricTraceE_derivRicciFormOfMetric_eq_laplacian` joins the two halves: `FlowKoszul.lean` gives `tr_g(∂ₜRic) = div div h − tr_g(Δ_g h)` (no curvature terms — they cancel at the Koszul step, before any Bianchi identity), `innerE_deriv_eq_of_isRicciFlowAt'` supplies `h = −2 Ric` as a *function* equality (`funext`), and `ScalarEvolution.lean`'s `sum_cov2Bilin_neg_two_ricciForm_eq` evaluates the difference to `Δ scal`. **`hasDerivAt_scalarCurvatureOfMetricAt_eq_laplacian_add`** then composes with `hasDerivAt_scalarCurvatureOfMetricAt_of_isRicciFlowAt`. **Every hypothesis is written under a two-step `letI`**: `RiemannianBundle … := ⟨(g t₀).toRiemannianMetric⟩` *and* `ContMDiffCovariantDerivative (leviCivitaOfMetric (g t₀)) 1 := contMDiffCovariantDerivative_leviCivitaOfMetric_one (g t₀)` — `ricciForm`, `scalarCurvatureAt`, `divBilinOneForm` and `IsMDiffCovBilinAt` all need the *instance*, not just the metric, and a hypothesis-position `letI` supplies only what it names. `ContMDiffCovariantDerivative … 2` is carried as an **explicit hypothesis** (`hlc2`): a `C²` metric gives only a `C¹` connection, so it is not derivable here, and `ScalarEvolution.lean`'s section requires it. `hsymm` and the `∇h`-differentiability hypothesis are derived from the `Ric` ones by `ricciForm_symm` and `covBilin_smul_form`; there is no `MDifferentiableAt.const_mul`, use `mdifferentiableAt_const.mul` |
| `CovariantAlongCurve.lean` | **proved**: **`D/dt`, the covariant derivative along a curve**, as a predicate — the first brick of the self-built comparison-geometry substrate. Mathlib's `CovariantDerivative` acts on **global sections over `M`**, so `∇_{γ'}γ' = 0` cannot even be *stated*; the missing primitive is the pullback connection on `γ*TM`. `velocity` (`γ'(t)` via `mfderiv` at `1 : TangentSpace 𝓘(ℝ,ℝ) t`), `MDiffAlongAt` (a section along `γ` is a lift of `γ` to `TM`, so regularity is ordinary `MDifferentiableAt` into the total space — **no new bundle structure needed**), and `IsCovDerivAlong` with the three axioms: additive, Leibniz over `f : ℝ → ℝ`, and `D(W ∘ γ) t = cov W (γ t) (velocity γ t)`. Then `MDiffAlongAt.zero_section`, `IsCovDerivAlong.zero`, and **`congr_of_eventuallyEq`** — `D` is **local**, not by fiat: a `ContDiffBump` equal to `1` near `t` and supported where the sections agree makes `f • V = f • W` *globally*, while Leibniz evaluates both sides at `t` (`f t = 1`, `deriv f t = 0`). Gotchas: the bump's support lemma is `ContDiffBump.support_eq` (there is no `support_subset_ball`), and `import Mathlib.Analysis.Calculus.BumpFunction.InnerProduct` is what supplies `HasContDiffBump ℝ`; `contMDiff_zeroSection` needs `(n := 1)` or the level stays a metavariable. **Uniqueness is proved** (`IsCovDerivAlong.eq_of_isCovDerivAlong`), on top of **`exists_frame_expansion`**: near `t` a section along `γ` is `∑ᵢ fᵢ·(Wᵢ∘γ)` with `Wᵢ` *global* `C¹` and `fᵢ` differentiable, so locality + `add` + `leibniz` + `restrict` compute `D V t` with no `D` left in it. Everything is read off **one trivialisation** around `γ t`: the frame is mathlib's `Trivialization.localFrame` for `Module.finBasis`, globalised by `exists_contMDiff_eventuallyEq`; the coefficients are `b.repr (e ⟨γ u, V u⟩).2 i`, differentiable because **`mdiffAlongAt_iff_of_mem`** says differentiability along `γ` *is* differentiability of those coordinates (`Trivialization.mdifferentiableAt_totalSpace_iff`, base component discharged by `hγ`). Same criterion gives `MDiffAlongAt.add`/`.smul`/`.sum`, a trivialisation being fibrewise linear (`continuousLinearEquivAt`, and the `map_add`/`map_smul` go through by **defeq with `exact`** — `simp only [← continuousLinearEquivAt_apply]` makes no progress, the lemma being a function equality). **Take finite sums fibrewise, not in the Pi type**: `Finset.sum` over `Π t, T_{γ t}M` picks an `AddCommMonoid` only *defeq* to the one `Fintype.sum_apply` produces, so `rw` fails on goals that print identically and even `Eq.trans` will not unify — state every sum as `fun u ↦ ∑ i ∈ a, Vs i u` and step the induction with `funext` + `Finset.sum_insert`. **Existence is proved too** (`isCovDerivAlong_covAlong`, `exists_isCovDerivAlong`, `eq_covAlong`) — which is what stops `∇_{γ'}γ' = 0` being *vacuously* true, the repo's worst class of error. `covAlong` is the classical formula in the trivialisation at `γ t`: `∑ᵢ (cᵢ'(t)·Wᵢ(γt) + cᵢ(t)·∇_{γ'}Wᵢ)` with `Wᵢ = canonFrame` (the globalised local frame) and `cᵢ = coeffAlong`. **`add` and `leibniz` are facts about `deriv`** — the coordinates are additive and scale, a trivialisation being fibrewise linear — and `leibniz`'s second term is `f'(t)·∑ᵢcᵢ(t)Wᵢ(γt) = f'(t)·V t` (`sum_coeffAlong_smul_canonFrame`). **`restrict` is the only one with content**: it is `cov`'s own Leibniz rule on `W = ∑ᵢ aᵢ Wᵢ`, transported by mathlib's `IsCovariantDerivativeOn.congr_of_eventuallyEq` (germ-locality of `∇`, already in mathlib — do not reprove it), plus `cov_sum_smul_section_apply` and the chain rule `deriv (a∘γ) t = mvfderiv a (γt) (γ' t)` (`deriv_comp_curve`, from `mvfderiv_comp_apply`; the residual goal closes by `simp only [mvfderiv, mfderiv_eq_fderiv]; rfl`, `fromTangentSpace` being the identity). **`rw` cannot see through `T%`** — it beta-reduces to `fun y ↦ ⟨y, σ y⟩` — so state section rewrites as `T% σ = T% τ` proved by `funext` + `show ⟨y,_⟩ = ⟨y,_⟩`. `W`'s frame coefficients need only `MDifferentiableAt`, so take them as `b.repr (e ⟨y, W y⟩).2 i` off `mdifferentiableAt_totalSpace_iff` rather than via `contMDiffAt_localFrameCoeff`, which wants `C¹`. **`IsParallelAlong`/`IsGeodesicOn`** are defined off `covAlong`, with `isParallelAlong_iff_forall`/`isGeodesicOn_iff_forall` proving them equivalent to vanishing under *every* operator satisfying the axioms — forward by uniqueness, backward by existence; without the latter the quantified form is vacuous. The reusable tool is **`IsCovDerivAlong.eq_sum_of_expansion`** (extracted from the uniqueness proof): `D/dt` is computed by *any* local expansion `V = ∑ᵢ fᵢ(Wᵢ∘γ)`, which is what will read the coordinate ODE off the coordinate frame. `velocity γ` as a *section* needs `velocity (I := I) γ` or instance search sticks on `ChartedSpace ?H M`. **The coordinate form is proved**: `covAlong_eq_sum_of_frame` says `D/dt` may be read in *any* trivialisation around `γ t` (not just the preferred one it is defined through), and `repr_covAlong_eq` is `(D/dt V)ᵏ = (cᵏ)' + ∑ᵢ cⁱ Γᵏᵢ` with `Γᵏᵢ = (∇_{γ'}Wᵢ)ᵏ`. Gotchas there: `b.repr` of a `Finset.sum` needs **`Finset.sum_apply'`** (the Finsupp one) after the two `map_sum`, or the outer `simp only` sees `(∑ …) k` and makes no progress; and `if_pos` is deprecated (a CI error) — use `simp only [Finset.mem_univ, ↓reduceIte]`. **`exists_frame_on_open` is the piece an ODE needs**: globalising the local frame one point at a time gives agreement only *near* that point (`=ᶠ[𝓝 y₀]`), useless along an arc; intersect the finitely many agreement sets, take `interior`, and intersect with `e.baseSet` — `mem_interior_iff_mem_nhds` plus `Filter.eventually_all` does it in four lines, with no change to `GlobalExtension.lean`. `covAlong_eq_sum_of_frame_on` reads the formula off that `U`, and `christoffel`/`repr_covAlong_eq_christoffel` package the correction as `Γ(y)(v)(c) = ∑ᵢ cⁱ ∇_v Wᵢ`, giving `D/dt V = c' + Γ(γt)(γ')(c t)`. **`Γ` is as regular as the connection**: `christoffelCoord` is the classical `Γᵢⱼ(y)` (`e`-coordinates of `∇_{Wⱼ}Wᵢ`), `christoffel_eq_sum` is bilinearity in the two slots, and `contMDiffAt_christoffelCoord` is `contMDiff_cov_apply` (from `Bianchi.lean` — hence the import) followed by mathlib's `contMDiffAt_section_iff`: a `C^k` connection and a `C^{k+1}` frame give `C^k` symbols. **That lemma is the first in the file to use any regularity of `cov` at all**; everything before it holds for a bare `CovariantDerivative`. What is left before `exp` is identifying the tangent-bundle trivialisation with the chart it comes from, which puts the geodesic equation on an open subset of `E` where Picard–Lindelöf applies |
| `BundleCovariantAlongCurve.lean` | **proved**: **`D/dt` for a section of a GENERAL vector bundle along a curve** — `MDiffAlongSectionAt`, `IsCovDerivAlongSection` (the same three axioms), `zero`, `mono`, locality (`congr_of_eventuallyEq`), the closure properties, `exists_frame_expansion_section`, `eq_sum_of_expansion` and **uniqueness** (`eq_of_isCovDerivAlongSection`). **Nothing in the construction is about the tangent bundle**: the *direction* stays there (`velocity γ t` is a tangent vector and `cov σ y` is a map out of `T_yM`), but the section differentiated and the fibres it takes values in are arbitrary — so this is the port `BundleHessian.lean` was, under the same discipline (**no fibre norm is bound**, only mathlib's standard general-bundle binders), and the tangent-bundle statements come back by `mdiffAlongSectionAt_eq_mdiffAlongAt` (`rfl`) and `isCovDerivAlongSection_iff_isCovDerivAlong`. **Why it is wanted**: `BundleMaximumPrinciple.lean` tests against a direction in a *single* fibre; the cross-fibre half compares different fibres, and the comparison carries the direction along a curve **in the fibres of the tested bundle, not of `TM`** — which the repo had only on `TM`. **Gotchas**: `Π t, V (γ t)` is a *dependent* Pi, so `Filter.EventuallyEq` does not typecheck and every germ hypothesis is a bare `∀ᶠ u in 𝓝 t, σ u = τ u` (the `GlobalExtension.lean` adjustment, same reason); **CORRECTED**: an earlier version of this row said the `MDiffAt`/`CMDiff`/`T%` notations cannot infer the model for a bundle other than `TM` — they **can** (`T% σ` finds `F` from the `FiberBundle F V` instance, exactly as `cov_sum_smul_section_apply_of_bundle` uses it), and spelling out `MDifferentiableAt I (I.prod 𝓘(ℝ, F))` is unnecessary. The real fault was elsewhere and is the next gotcha: `I` must be an **explicit** argument of `MDiffAlongSectionAt` (it appears only in the body, so `F V γ σ t` leaves it a metavariable and instance search sticks on `ChartedSpace ?m M`); and the two predicates are distinct *structures*, so the bridge is an **iff** that destructures and reassembles, not a `rfl` — only the `MDiffAlongSectionAt` bridge is `rfl`. **Existence too** (`isCovDerivAlongSection_covAlongSection`, `exists_isCovDerivAlongSection`, `eq_covAlongSection`), on `canonFrameSection`/`coeffAlongSection`/`covAlongSection` — the trivialisation formula read in `V` rather than `TM`; `add` and `leibniz` are facts about `deriv` (a trivialisation is fibrewise linear) and `restrict` is `cov`'s own Leibniz rule on the local-frame expansion, via `cov_sum_smul_section_apply_of_bundle` (already in `BundleNormalSection.lean` — **hence the import**, and no cycle: `NormalSection.lean` imports `CovariantAlongCurve.lean`, not the other way). Then **`IsParallelAlongSection`** with `isParallelAlongSection_iff_forall` — **existence is what stops that quantified form being vacuous**, the repo's worst class of error. Falsification: `covAlongSection_eq_covAlong` is **not** `rfl` and is not asserted by construction — both operators satisfy the axioms and *uniqueness* identifies them — with `isParallelAlongSection_iff_isParallelAlong` on top. **Gotcha**: `variable (I F V) in` on a declaration that does **not mention `I`** (`coeffAlongSection`) reorders the explicit binders — `I` is appended after `F`,`V` — and the error is an application type mismatch reporting `I` where a `Type` is expected. Drop the unused one from the `variable` line rather than guessing the order. Lemmas from `BundleNormalSection.lean` live in namespace **`RicciFlowBlueprint`**, not `CovariantDerivative` |
| `HomBundleAlongCurve.lean` | **proved**: **the Leibniz rule for `D/dt` on a Hom bundle** — `D/dt(Aσ) = (D/dt A)(σ) + A(D/dt σ)` (`covAlongSection_hom_apply`). **No metric is used anywhere**, unlike `TransportIsometry.lean`'s pairing rule, where metric compatibility is what differentiates `⟪Aᵢ,Bⱼ⟫`; here the derivative of `Bᵢ(Wⱼ)` along the curve **is** the definition of `∇A` rearranged. Both are expanded in local frames — not the same frame and neither parallel — and the product `Aσ` is a combination of the **global** sections `y ↦ Bᵢ y (Wⱼ y)` of `V₂` (`ContMDiff.clm_bundle_apply`) indexed by `ι × κ`, so `eq_sum_of_expansion` computes it; the product rule gives the `fᵢ'`/`gⱼ'` terms, which are the coordinate parts of the two right-hand derivatives, and `covHom` gives `∇_{γ'}(BᵢWⱼ) = (∇_{γ'}Bᵢ)(Wⱼ) + Bᵢ(∇_{γ'}Wⱼ)`, which is the connection parts. Nothing is left over, so the frames carry no hypotheses. **Why it is wanted**: the cross-fibre half of the maximum principle carries the tested direction between fibres of the *tested* bundle, in dim 3 `End(TM)`; `IveyParallel.lean` says the pinching set is preserved by every fibre isometry, hence by `TM`-transport, and what was missing is that **conjugating by `TM`-transport IS `End(TM)`-transport** — this rule is what says so. Cashed in: `isParallelAlongSection_hom` (a parallel morphism field carries parallel sections to parallel ones) and `covAlongSection_hom_apply_eq_zero` (the one-section half of the converse). **The full converse needs a family of parallel sections spanning each fibre** — that is `ParallelTransportGlobal.lean` applied to a basis, wants a metric, and is next. `homCov_apply_vector` restates `homCov_apply` at a **bare tangent vector** via `FiberBundle.extend` (`mvfderiv_inner_eq_apply`'s manoeuvre one bundle up; note the call is `FiberBundle.mdifferentiableAt_extend I E v`, models first). The two algebraic helpers (`clm_sum_smul_apply_sum`, `clm_expansion_add`) are stated over bare **topological modules** — a fibre `V₂ x` has no norm and asking for one would break the tangent bridge (corrected belief 4) — and each closes with `rw [Fintype.sum_prod_type]`, a `simp only`, `rw [Finset.sum_comm]` (the double sum comes out **transposed**) and a termwise `module`. Gotcha: `ContinuousLinearMap.sum_apply`/`.add_apply`/`.smul_apply` are all deprecated → bare `sum_apply`, `add_apply`, `smul_apply` |
| `Exponential.lean` | **proved**: **affine reparametrisation of geodesics**, the invariance `exp` is built on. `velocity_comp_mul` is the chain rule `velocity (γ ∘ (·*a)) t = a • velocity γ (t*a)` — via `HasMFDerivAt.comp` on `mfderiv_eq_smulRight_velocity`; state the `HasMFDerivAt` of `s ↦ s*a` by `hasMFDerivAt_iff_hasFDerivAt.mpr` (a `rw` fails on the `TangentSpace` instances) and get the `HasFDerivAt` in the `smulRight 1 a` form from `hasDerivAt_iff_hasFDerivAt.mp`, not from `HasDerivAt.hasFDerivAt` (which produces `toSpanSingleton ℝ (1*a)`); the declaration still needs `set_option backward.isDefEq.respectTransparency false in`. **The geodesic statement is nearly free** (`covAlong_velocity_comp_mul_eq_zero`, `_'`, `isGeodesicOn_comp_mul`): in the chart `p'' = −Γ̃(p)(p')(p')`, reparametrising multiplies the left by `a²`, and `Γ̃` is **bilinear by construction**, so `map_smul` twice on the right matches. **No case split on `a = 0`** — both sides vanish, so the constant curve is a geodesic with no separate argument about `D/dt` of the zero section. **Boundarylessness is not needed** here (unlike existence and uniqueness): the argument reads the equation in a chart but never differentiates the inverse chart. The enabling bridge is `covAlong_velocity_eq_zero_iff_chart` in `GeodesicODE.lean`, which restates the equation against `christoffelChart` (a function of the chart coordinate) rather than `christoffel` (a function of the base point) — the form every in-chart computation wants. **Uniqueness on a whole preconnected open set** (`forall_totalSpace_eq_of_isGeodesicOn`, `eqOn_of_isGeodesicOn`): the clopen argument on the agreement set in `TM`. Open is `eventuallyEq_of_isGeodesic'` — agreeing *near* a point makes the curves the same function there, so the velocities agree by `Filter.EventuallyEq.mfderiv_eq`, whose `tangentSpaceCast` is literally the identity and dies by `rfl`. **Closed is where mathlib is missing a piece: there is no `T2Space` instance on the total space of a bundle**, so the diagonal of `TM × TM` is unavailable; take closedness in two Hausdorff pieces instead — the base in `M`, the fibre coordinate `(e ⟨c u, c'(u)⟩).2` in `E` — and reassemble by `e.toPartialHomeomorph.injOn`. The fibre coordinate is continuous at the point exactly because `mdiffAlongAt_iff_of_mem` turns differentiability *along* the curve into differentiability of that coordinate. Extracted tool: **`eq_of_mem_closure_of_continuousAt`** — two maps agreeing on `A` and continuous *at* a point of `closure A` agree there; the usual `IsClosed {f = g}` argument with continuity assumed only where needed. Upstream candidate. **`exp` is built** — the first object of the comparison-geometry substrate. `IsGeodesicRun cov c s x v` packages exactly what uniqueness asks (open preconnected `s ∋ 0`, `c` and its velocity differentiable along it, the equation, and the initial data as **one** equation in `TM`); `exists_isGeodesicRun` is `exists_isGeodesicOn'` (whose conclusion was strengthened to hand back the differentiability it already proves); `IsGeodesicRun.eq_of_mem` is interval uniqueness on `s₁ ∩ s₂`, **preconnected because in `ℝ` preconnected = order-connected and that survives intersection** (`isPreconnected_iff_ordConnected`, `Set.OrdConnected.inter`); `IsGeodesicRun.comp_mul` reparametrises a run, needing `mdiffAlongAt_velocity_comp_mul` (the reparametrised velocity is differentiable along the curve — its coordinate is `a • v(u·a)`). Then **`expMap`** by choice on runs reaching time `1`, with **`expMap_eq`** (*any* such run computes it — that **is** well-definedness), **`expMap_smul_eq`** (`exp_x(a v) = γ_v(a)`, reparametrisation cashed in, which is what makes `exp` a map near the origin rather than one value) and **`expMap_zero`** free from it. Gotchas: **`dif_pos` is deprecated** (a CI error) — use `rw [expMap]; split` with `next hP =>`, not a `simp` on the condition; the `init` field's goal does not beta-reduce, so `simp only [zero_mul]` makes no progress — `show` the reduced form first; and scaling the fibre of a total-space equality is `congrArg (fun q ↦ ⟨q.proj, a • q.2⟩)`, which avoids ever projecting the dependent second component |
| `GeodesicODE.lean` | **proved**: **the tangent-bundle trivialisation *is* the chart** — the identification that turns `∇_{γ'}γ' = 0` into an ODE. `trivializationAt_snd_eq_tangentCoordChange` is a **`rfl`**: mathlib's `TangentBundle.trivializationAt_apply` already unfolds `(e_{x₀}⟨y,v⟩).2` to the `fderivWithin` that `tangentCoordChange` is. `hasDerivAt_extChartAt_comp` then gives `p'(t) = tangentCoordChange I (γt) x₀ (γt) (γ'(t))` for `p u = extChartAt I x₀ (γ u)`, so **`deriv_extChartAt_comp_eq_trivializationAt`**: the chart position's derivative *is* the velocity's fibre coordinate, and `coeffAlong_velocity_eq` says `coeffAlong` for `V = γ'` is `p'` in the model basis — so `D/dt V = c' + Γ(γ)(γ')(c)` at `V = γ'` is an equation in `p'` and `p''`. **The chart-side manoeuvre is mathlib's own**, from `IntegralCurve/Basic.lean`: compose the curve's `HasMFDerivAt` with the chart's, then `mfderiv_chartAt_eq_tangentCoordChange`. The only adjustment is dropping the integral-curve hypothesis, for which `mfderiv_eq_smulRight_velocity` (a curve's differential is the `smulRight` of its velocity) holds with **no** hypothesis — a CLM out of `ℝ` is determined at `1`, so both sides are junk together. **Gotcha, load-bearing**: `rw [hasDerivAt_iff_hasFDerivAt, ← hasMFDerivAt_iff_hasFDerivAt]` fails on the `TangentSpace` instances unless the declaration carries **`set_option backward.isDefEq.respectTransparency false in`** — mathlib guards its own copy the same way. Also `ContinuousLinearMap.one_apply` is deprecated (→ `one_apply_eq_self`), and `(1 : ℝ →L[ℝ] ℝ).smulRight v` *prints* as `toSpanSingleton ℝ v`, so `rw` with `smulRight_apply` misses; finish with `rfl`. **The ODE is assembled**: `trivializationAt_covAlong_eq` pushes `covAlong_eq_sum_of_frame` through the trivialisation to give `(D/dt V)ᵉ = q' + Γ(γ)(γ')(q)` with `q u = (e⟨γu,Vu⟩).2` — **a vector equation in `E` with no basis in the statement**, the basis eliminated by `sum_deriv_repr_smul` (the coefficients of `q'` *are* the derivatives of the coefficients of `q`). At `V = γ'` the identification turns `q` into `p'`, giving **`covAlong_velocity_eq_zero_iff`**: `∇_{γ'}γ' = 0 ↔ p'' = −Γ(γt)(γ')(p')`. The *iff* is free because a trivialisation is a linear **equivalence** on each fibre, so vanishing may be tested in coordinates (`continuousLinearEquivAt.map_eq_zero_iff`). Gotchas: `∀ᶠ u in 𝓝 t, f u = g u` is `Filter.Eventually`, **not** `EventuallyEq` — `.deriv_eq` needs the type stated as `f =ᶠ[𝓝 t] g`; and `.self_of_nhds` leaves a beta-redex that `rw` cannot match, use `.eq_of_nhds` bound to a `have` with the reduced statement. **`Γ` is packaged for Picard–Lindelöf**: `christoffelB y : E →L[ℝ] E →L[ℝ] E`, built from the scalar symbols by two `ContinuousLinearMap.smulRightL`s so linearity and continuity in both slots hold **by construction** — no `mk₂`, no bilinearity proof. `christoffel_eq_christoffelB` bridges, and `contMDiffAt_christoffelB` carries `contMDiffAt_christoffelCoord` across two fixed CLMs. Gotchas: the `simps` lemma is `smulRightL_apply_apply` (not `_apply`); `ContinuousLinearMap.sum_apply`/`.smul_apply`/`if_pos` are all deprecated (→ `sum_apply`, `smul_apply`); `LinearMap.coe_toContinuousLinearMap'` is the applied form; **`ContMDiff` unfolds to `∀ x, ContMDiffAt`**, so `hL.comp_contMDiffAt` resolves to `Function.comp_contMDiffAt` and fails — use `ContMDiffAt.comp y (hL _) h`; and the nested-CLM section needs `set_option maxSynthPendingDepth 3`. **Geodesics exist** (`exists_isGeodesicOn`, `exists_isGeodesicOn'`). `christoffelChart` is `Γ̃ = christoffelB ∘ (extChartAt I x₀).symm`, and `contDiffAt_christoffelChart` is **where `[I.Boundaryless]` enters and the only place it does**: the inverse chart is smooth on `range I`, which is everything only when `range I = univ`, so the composite is `ContMDiffAt` and hence ordinary `ContDiffAt` — for a model with corners the chart target is not open in `E` and `ContDiffAt` is the wrong predicate. `geodesicField z = (z.2, −Γ̃(z.1)(z.2)(z.2))` is the first-order system, `C^k` by `ContDiffAt.clm_apply` twice, and `exists_solution_geodesicField` is mathlib's `ContDiffAt.exists_forall_mem_closedBall_exists_eq_forall_mem_Ioo_hasDerivAt₀` — **no Lipschitz estimate is done here**, `IsPicardLindelof.of_contDiffAt_one` does it. The transport back sets `c = (extChartAt I x₀).symm ∘ p` and shrinks the interval so that `p` stays in the (open, by boundarylessness) chart target and `c` in the frame's `U`; then every manifold-side hypothesis of `covAlong_velocity_eq_zero_iff` is read off the solution — `c` differentiable because the inverse chart is, and `MDiffAlongAt c (velocity c)` because `mdiffAlongAt_iff_of_mem` turns it into differentiability of the fibre coordinate, which **is** `(z ·).2`. **The initial condition is one equation in the total space** (`⟨c 0, velocity c 0⟩ = ⟨x₀, v₀⟩`), which packages `c 0 = x₀` and `c'(0) = v₀` with no dependent-type cast; it follows from `e.toPartialHomeomorph.injOn` plus `e.coe_fst`. Gotchas: mathlib has **no** `HasDerivAt.fst`/`.snd` — use `(ContinuousLinearMap.fst ℝ E E).hasFDerivAt.comp_hasDerivAt`; `DifferentiableAt.congr_of_eventuallyEq` takes the equality in the **goal's** direction, not `.symm`; and after `e.toPartialHomeomorph.injOn` the goal prints with `↑e.toPartialHomeomorph`, so `e.coe_fst` will not `rw` until a `show` restates it at `⇑e`. `exists_frame_on_open` was generalised from `C¹` to any `n : ℕ∞` (one extra `ContMDiffVectorBundle n` binder) so that `exists_isGeodesicOn'` needs no frame data. **Geodesics are unique** (`eventuallyEq_of_isGeodesic`, `eventuallyEq_of_isGeodesic'`): the same identification run backwards — `hasDerivAt_geodesicField_of_isGeodesic` says a geodesic *solves* the system in the chart — plus mathlib's `ODE_solution_unique_of_eventually`, with the Lipschitz constant from `ContDiffAt.exists_lipschitzOnWith`. **The hypotheses are the ones the equation already needs**: ODE uniqueness wants `p'` *differentiable*, not merely `p''` to have a value, and the chart coordinate of the velocity **is** `p'`, so `MDiffAlongAt c (velocity c)` — already required for `D/dt γ'` to mean anything — is exactly that. No extra regularity. Gotchas: `congrArg TotalSpace.proj hinit` extracts the base equality from a total-space initial condition, but `simp only [hinit]` alone will not close `z₁ t₀ = z₂ t₀` (it rewrites the fibre term and leaves the base), so split with `Prod.ext` and a `show`; and the `∀ᶠ`-hypotheses have to be *nested* (`eventually_eventually_nhds.mpr`) before `filter_upwards`, since the per-point lemma itself takes `∀ᶠ` hypotheses |
| `ParallelTransport.lean` | **proved**: **parallel transport exists on the whole interval, and is unique** (`exists_isParallelAlong`, `eqOn_of_isParallelAlong`), on `transportCoeff` (= `A(t) = −Γ(γt)(γ'(t))`) and `covAlong_eq_zero_iff_deriv` (`D/dt V = 0 ↔ q' = A(t)q`). **Why this is reachable and geodesics were not, and it is the whole point of the file**: `Γ` is bilinear by construction, so in the unknown `q` the transport equation is **LINEAR**, with all the curve's data inside the coefficient — where the geodesic equation `p'' = −Γ̃(p)(p')(p')` is *quadratic*. So `LinearODE.lean`'s Dyson series gives the solution on the **entire** interval with no smallness hypothesis, where `exists_isGeodesicOn` had to take whatever Picard–Lindelöf gave; and uniqueness is mathlib's `ODE_solution_unique_of_mem_Ioo` with a Lipschitz constant compactness supplies free. **CORRECTED BELIEF: parallel transport is NOT behind the ODE-regularity gap `exp` sits behind** — the gap list said it was. `exp` needs smooth dependence of a *nonlinear* flow on initial conditions; transport needs a linear equation along a *fixed* curve. The coefficient is pushed through **`Set.projIcc`** before the series sees it (Dyson wants a continuous globally bounded coefficient on all of `ℝ`; the curve gives one only on `[a,c]`; the extension is invisible there, `projIcc` being the identity). Hypotheses live on an **open** `s ⊇ Icc a c` so every point has a two-sided neighbourhood — no endpoint special-casing. **`exists_parallelTransportEquiv` makes transport a linear ISOMORPHISM of fibres** — a `≃L[ℝ]` with `V t = P (V t₀)` for *every* parallel `V` — and **invertibility is not extra work**: `P` is trivialisation, Dyson propagator, inverse trivialisation, and the middle factor is `bijective_dysonSum`. **No metric appears**: `≃L[ℝ]` needs only the fibres' topology and module structure, and transport is not an isometry until `cov` is assumed metric. Everything rests on ONE ODE-uniqueness argument, `coord_eq_dysonFrom` (in coordinates every parallel section *is* the Dyson solution through its own initial value); uniqueness is then three rewrites, and it holds on the **closed** interval because `ODE_solution_unique_of_mem_Icc` wants the equation only on the interior and continuity on the closure, which differentiability along the curve already gives. `transportCoeffExt` names the `projIcc` extension so the existence and uniqueness proofs share it. **Gotchas**: `Trivialization.symmL` **cannot infer its bundle** here (`IsLinear ?m e` stuck) whether under `set`, an ascription, or an inner application — characterise the section by its *coordinate* instead (`∃ V, ∀ u ∈ s, (e⟨γu,Vu⟩).2 = q u`, built with a `dite` on `γ u ∈ e.baseSet` and `continuousLinearEquivAt`, which does resolve); `set` drops the expected type, so anything needing it must be introduced by an **ascribed existential**; `rw [Set.projIcc_of_mem]` misses under a beta-redex — `show` first; and `ContinuousOn.restrict` is deprecated (→ `.domRestrict`) |
| `TransportIsometry.lean` | **proved**: **parallel transport is an isometry** for a metric connection (`exists_parallelTransportIsometry`) — `ParallelTransport.lean` says in so many words that its `≃L[ℝ]` is *not* an isometry until `cov` is assumed metric, and this is that assumption cashed in. **Mathlib names this as future work** (`CovariantDerivative/Metric.lean`'s header: "when Mathlib has a notion of parallel transport, prove the equivalence of `IsMetricCompatible` with the characterisation that parallel transport be an isometry"); one direction is done here. **The whole cost is one Leibniz rule**, `hasDerivAt_inner_along`: `d/dt⟪V,W⟫ = ⟪D/dt V,W⟫ + ⟪V,D/dt W⟫`, by the `exists_frame_expansion` argument used once more — expand both sections in local frames, **not necessarily the same frame and neither orthonormal nor parallel**, so the pairing is `∑ᵢⱼ fᵢgⱼ⟪Aᵢ∘γ,Bⱼ∘γ⟫`; the product rule gives the `fᵢ'`/`gⱼ'` terms, which *are* the coordinate parts of `D/dt`, and metric compatibility gives each frame pairing's derivative, which *is* the connection part. **Nothing is left over, which is why the frames carry no hypotheses.** Then `inner_eq_of_isParallelAlong` (`constant_of_has_deriv_right_zero` on `[a,c]`), `norm_eq_of_isParallelAlong`, and the `≃ₗᵢ[ℝ]` built from `exists_parallelTransportEquiv` plus `exists_isParallelAlong` (the parallel section through each vector). `mvfderiv_inner_eq_apply` restates mathlib's `IsMetricCompatible.mvfderiv_inner_eq` at a **bare tangent vector** — the lemma quantifies over a field `X` but uses only `X x`, so `FiberBundle.extend` discharges it. **Gotchas**: `rwa [FiberBundle.extend_apply_self] at h` fails because the RHS is a beta-redex — `simp only [...] at h` then `exact h`; `HasDerivAt.sum` is the **Pi**-typed sum, so a goal `fun u ↦ ∑ …` needs **`HasDerivAt.fun_sum`**; `convert … using 1` on a `HasDerivAt` descends into the instances and leaves `Real.instAddCommGroup = Real.normedAddCommGroup.toAddCommGroup` — state the value equation as a separate `have` and `rw` it instead; and `sum_inner`/`inner_sum` will not `rw` on a `Finset.sum` of `TangentSpace`-typed terms (it picks `E`'s `AddCommMonoid`), so the two bilinearity steps are **stated over a variable inner product space `F`** (`inner_sum_smul_sum`, `inner_expansion_add`) and applied with `exact` — the documented remedy, and it also fixes the summation *order*, which `simp only [sum_inner, inner_sum]` silently transposes |
| `ParallelTransportGlobal.lean` | **proved**: **parallel transport along an ARBITRARY curve** (`exists_parallelTransportIsometry_global`) — `ParallelTransport.lean` proves everything *inside one trivialisation*, carrying `e`, `b`, `W`, `U ⊆ e.baseSet` and the binding `hγU : ∀ u ∈ s, γ u ∈ U`, so the curve may never leave a chart; here the only hypotheses are that `cov` is metric and `γ` is differentiable on a preconnected open `s`. **Only half of it was ever chart-local**: `TransportIsometry.lean`'s constancy results carry *no* frame hypotheses, so the isometry was global already and what had to be freed was **existence and uniqueness** of the section. **And the mechanism is CONNECTEDNESS, NOT SUBDIVISION** — the expected proof chains local transports along a Lebesgue subdivision and then proves the chain independent of it; none of that is needed. `exists_chart_interval` (a closed interval with the time in its *interior* along which the curve stays in one trivialisation, from `exists_frame_on_open` + continuity of `γ`); `eqOn_of_isParallelAlong_global` (the chart-local uniqueness propagates agreement across a whole subinterval at once, so the agreement set is open **and has open complement**); `exists_transport_interval` (transport from **any** point of the interval, not the centre — that quantification is what makes it usable twice, and costs nothing since `exists_isParallelAlong` already allows any base point); **`exists_glue_isParallelAlong`** (two parallel sections on overlapping opens glue — **this is what global uniqueness buys**: a case split is only sound because they agree on the *whole* overlap, not just where matched, else the result jumps at the boundary and is differentiable nowhere near it); **`exists_isParallelAlong_global`** (reachable times are open with open complement since a transport interval meeting them is swallowed; witnesses assembled **pointwise**, `V r` the value at `r` of its own witness, well defined because two witnesses agree at `t₀` hence throughout their preconnected intersection). Linearity is the two `D/dt` axioms (`isParallelAlong_add`, `isParallelAlong_const_smul` — the Leibniz term dies on a constant scalar, and `isParallelAlong_sum`/`isParallelAlong_sum_smul`, which need **no induction**: `IsCovDerivAlong.sum` already has additivity over finite sums, so each is one rewrite); invertibility is transport back, composing to the identity by uniqueness. **Gotchas**: `Metric.lean` is **not** transitively imported even via `TransportIsometry.lean` — import it explicitly or `IsMetricCompatible` is "not in the environment"; the norm on `T_xM` needs a `[RiemannianBundle …]` binder, so the isometry lives in its own section; `differentiableAt_const` now takes its constant **explicitly**; and `rw` under `TotalSpace.mk` hits the beta-redex — `show ⟨γ y, V y⟩ = ⟨γ y, V₁ y⟩` first. `push_neg` is deprecated → `push Not`; `if_pos`/`if_neg` → `simp only [h, ↓reduceIte]` |
| `CurvatureThree.lean` | **proved**: **the curvature operator in dimension three, from `Ric` alone** — `scal − 2Ric(b₀,b₀) = 2 Rm(b₁,b₂,b₂,b₁)` (`scalarCurvatureAt_sub_two_ricciAt`) and, in a Ricci eigenbasis, `Rm(b₂,b₀,b₁,b₂) = 0` (`curvatureTensorAt_eq_zero_of_ricciAt_eq_zero`). Together: **`scal·Id − 2Ric♯` *is* the Hamilton curvature operator in dimension 3** — diagonal in a Ricci eigenbasis, diagonal entries twice the sectional curvature of the *opposite* plane, which is exactly `Pinching.lean`'s normalisation (`λ,μ,ν` twice the sectional curvatures, `R = λ+μ+ν`; unit `S³`: `6 − 4 = 2 = 2·1`). **NEITHER statement needs the vanishing of the Weyl tensor**, and neither needs a four-linear expansion over a basis — the literature gets both from the dim-3 decomposition `Rm = f(Ric)`, which is a genuine theorem and is **not required**. The dictionary is the trace identity `Ric(bₘ,bₘ) = ∑ₙ K(bₘ,bₙ)` (true in every dimension) plus pair symmetry for `K(bᵢ,bⱼ) = K(bⱼ,bᵢ)`: `r₀ = p+q`, `r₁ = p+r`, `r₂ = q+r`, so `R = 2(p+q+r)` and `R − 2r₀ = 2r`, one `linarith`. The diagonalisation is the *same* identity read off-diagonally: `Ric(b₀,b₁) = ∑ⱼ Rm(bⱼ,b₀,b₁,bⱼ)` has **one** surviving summand in dimension 3, the `j=0` term dying on antisymmetry of the first pair and the `j=1` term on antisymmetry of the second — so the off-diagonal curvature component *is* the off-diagonal Ricci component. **This is what makes the whole dim-3 line cheap, and it is why the `Λ²` wall is not on the critical path.** Bridge: `ricciAt_eq_sum_curvatureTensorAt` (`Ric(v,w) = ∑ᵢ Rm(bᵢ,v,w,bᵢ)`), which is `ricci_eq_sum_inner_frame` read on the `extendTwo` extensions `curvatureTensorAt` is defined through — the two sides are the same expression after `extendTwo_apply_self`, so it closes by `rfl`. Gotcha: `ricciAt_eq`'s `x` is implicit and undetermined in a bare `have` — pass `(x := x)`, same trap as `bianchi_second` |
| `OneFormAlongCurve.lean` | **proved**: **the Leibniz rule for a one-form along a curve** — `d/dt[ω(V)] = (∇_{γ'}ω)(V) + ω(D/dt V)` (`hasDerivAt_oneForm_along`) — and, at `ω = df` along a **geodesic**, **`(f∘γ)''(t) = ∇²f(γ',γ')`** (`hasDerivAt_mvfderiv_velocity_of_geodesic`). **No metric is used anywhere**, unlike `TransportIsometry.lean`'s pairing rule: there metric compatibility is needed to differentiate `⟪Aᵢ,Bⱼ⟫`, here the derivative of `ω(Aᵢ)` along the curve **is** the definition of `∇ω` rearranged. One frame expansion (`exists_frame_expansion`), the product rule contributing the `cᵢ'` terms that are exactly the coordinate part of `D/dt`, and everything matches term for term — so the frame carries no hypotheses. **WHY THIS EXISTS: it takes `exp` out of the cross-fibre maximum principle, which I had just recorded as possibly blocked by it.** Hamilton extends the tested direction `n` to a **parallel** section over a *neighbourhood*, so that `|N|` and the support function `h_{K_y}(N(y))` are constant and a max of `dist(u,K)` is a max of `⟪N,u⟫`; that is radial transport, hence `exp`'s regularity, hence the mathlib gap. But the conclusion is only ever consumed through `Δ`, and **`Δ` at a point is a sum of `n` ordinary second derivatives along `n` geodesics — one curve at a time**. Along a *single fixed* curve, parallel transport is `ParallelTransport.lean` (linear equation, Dyson series, no dependence-on-initial-conditions theory) and it is an isometry (`TransportIsometry.lean`), so `s ↦ ⟪Nᵢ(s), u(γᵢ(s))⟫` has a max at `s=0`, its second derivative there is `≤ 0`, and differentiating twice with `D/dt γ' = 0` gives `⟪n, ∇²_{eᵢ,eᵢ}u⟫ ≤ 0`; summing gives `⟪n, Δu⟫ ≤ 0`. **Nothing ever varies the curve, so the gap is never touched.** This also means the `BundleNormalSection` + Bochner route to the touching-point step, while correct, is not the only way in and asks more. **Remaining cost of the route**: `CovariantAlongCurve.lean` is `TM`-only, so `D/dt` for a section of a general bundle `V` needs the same port `BundleHessian.lean` was. Gotchas: the two bilinearity helpers (`map_sum_smul`, `map_sum_add_smul`) must be stated over a bare **topological module**, not a normed space — `T_xM` has a norm only through a `RiemannianBundle`, i.e. only once a metric is chosen, and this file is metric-free; stating them with `[NormedAddCommGroup F]` fails with `failed to synthesize NormedAddCommGroup (TangentSpace I (γ u))`. And `rw [hz] at h` for `hz : mvfderiv I f x (0 : TangentSpace I x) = 0` misses — the two `0`s are defeq but not syntactic; use `HasDerivAt.congr_deriv` and close the value goal with `rw [hgeo, add_eq_left]; exact map_zero _` |
| `EndMetric.lean` | **proved**: **the Hilbert–Schmidt form on `End(W)`** — `⟨A,B⟩ = ∑ᵢ⟪A eᵢ,B eᵢ⟫` as a continuous bilinear form, with frame-independence, symmetry, definiteness and boundedness of `{⟨A,A⟩ < 1}`. **These are exactly the fields of a `RiemannianMetric` other than smoothness**, so this is the pointwise half of the fibre metric `End(TM)` needs before any maximum principle can run on a section of it — and it has to be built, because mathlib puts **no** inner product on `→L` and its only `IsContMDiffRiemannianBundle` instances are the trivial bundle and index lowering. **Bilinearity and continuity are FREE**: the form is `∑ᵢ (innerSL ℝ).bilinearComp (apply ℝ W (bᵢ)) (apply ℝ W (bᵢ))`, so there is no `mk₂` and no boundedness estimate. **Frame-independence is then immediate** — `(v,w) ↦ ⟪A v, B w⟫` is itself a continuous bilinear form and `OrthonormalBasis.sum_apply_self_eq` traces it, the same mechanism that makes `scal` and `Δ` frame-independent; copy `Bochner.lean`'s `sum_inner_cov_congr` for the exact `(((innerSL ℝ).comp A).flip.comp B).flip` shape. **The only genuinely metric estimate is `‖A‖ ≤ ∑ᵢ‖A eᵢ‖`** (expand `v` in the basis, `|⟪eᵢ,v⟫| ≤ ‖v‖`), and it gives *both* remaining fields: definiteness (`⟨A,A⟩ = 0` ⟹ every `A eᵢ = 0` ⟹ `‖A‖ ≤ 0`) and boundedness (`⟨A,A⟩ < 1` ⟹ every `‖A eᵢ‖ ≤ 1` ⟹ `‖A‖ ≤ dim W`). Gotchas: **`rw [NormedSpace.isVonNBounded_iff]` fails on `W →L[ℝ] W`** — the documented two-topologies problem from `LinearODE.lean`, `ContinuousLinearMap.topologicalSpace` vs `PseudoMetricSpace.toUniformSpace.toTopologicalSpace` — use `refine (NormedSpace.isVonNBounded_iff ℝ).2 ?_`, which is up to defeq; `Finset.single_le_sum` leaves `AddLeftMono ?m` stuck after a `simp only` rewrites the summand, so name the function (`(f := fun j ↦ ‖A (b j)‖ ^ 2)`); set membership in the hypothesis must be unfolded (`simp only [Set.mem_ofPred_eq] at hA`, `Set.mem_setOf_eq` being deprecated) before `nlinarith` can use it; and `ContinuousLinearMap.sum_apply` is deprecated → bare `sum_apply`. The `ContMDiffRiemannianMetric` built on top of this is `EndBundleMetric.lean` |
| `RicciSection.lean` | **proved**: **`Ric` and `scal` are smooth** — `contMDiffAt_ricciForm_apply` (`C¹` on `C²`/`C³` fields), its `C²` twin on `C³`/`C⁴` fields, and **`contMDiff_scalarCurvatureAt_two`**, which is exactly the hypothesis `ScalarPreservation.lean` and `ScalarLowerBound.lean` carry explicitly — `RicciForm.lean` made `Ric` a bilinear form but said nothing about how it varies with the point, and that gap was load-bearing. **The one real obstacle is LOCALITY, and the fix is to globalise the frame first**: `Ric` is a trace, so a frame computes it, but a frame is local while every smoothness lemma in the curvature tower (`contMDiff_curvature`, `_two`) is stated for **globally** `C^k` sections. **`exists_orthonormal_frame_on_open`** globalises the trivialisation's orthonormal frame one section at a time — agreement only *near* the point — then intersects the finitely many agreement sets and takes the `interior`, exactly as `exists_frame_on_open` does in `CovariantAlongCurve.lean`; the result is orthonormal on a whole **open** set, so the frame sum computes `Ric` at every point of it and a germ is all smoothness needs. After that each summand is `ContMDiffAt.inner_bundle'` on `contMDiff_curvature`, and `scal` is the same sum on the diagonal. **Levels**: last slot expensive as always, so `C¹` runs `C²/C²/C³` and `C²` runs `C³/C³/C⁴`, the frame one level above the fields it feeds; twin proofs rather than a variable level, for `CurvatureDeriv.lean`'s reason. Gotchas: **`Module.Basis.orthonormalFrame` applied to `TangentSpace` infers `Bundle.Trivial M E` for the bundle** and reports a *topology* mismatch on the total space (lean4#14949 family) — pin the frame's type with an **ascribed existential** (`∃ fr : Fin _ → Π y : M, TangentSpace I y, fr = bE.orthonormalFrame e`) and rewrite; `exists_contMDiff_eventuallyEq` needs `[T2Space M]`; and `contMDiff_curvature` wants `[ContMDiffCovariantDerivative cov 2]`, `_two` wants `3` — one more than the level of the section they produce |
| `CurvatureOperatorSection.lean` | **proved**: **`Rm₃` is a `C²` SECTION of `End(TM)`**, and of `EndTangent` — so the object dimension-3 Hamilton–Ivey runs on now exists **with all its structure**: a `C²` section of a bundle carrying a connection, a fibre metric and their compatibility. **Two bricks.** **`contMDiffAt_end_section_of_symmL`** — a section of `End(TM)` is `C^n` as soon as its values on the trivialisation's local frame are; this is the **converse** of `ContMDiffAt.clm_bundle_apply` and is what a *constructed* endomorphism field needs. It is cheap because for a `Hom(V,V)` section the `inCoordinates` representative is **literally the trivialisation coordinate of `y ↦ A y (e.symmL y u)`** — no coordinate change survives, so it is one rewrite plus `continuousLinearMapAt_apply_of_mem`. **`contMDiffAt_ricciSharp_apply`** — `Ric♯` on a `C³` field is a `C²` section, and **the Riesz isomorphism is NEVER differentiated**: `contMDiffAt_section_of_inner_localFrame` says a section of a Riemannian bundle is `C^n` once its inner products with a local frame are, and those inner products *are* the Ricci form (`RicciSection.lean`). So the sharp costs nothing. The rest is `ContMDiffAt.smul_section`/`.const_smul_section`/`.sub_section` on `curvatureOperator_apply`. Gotchas: `contMDiffAt_section_of_inner_localFrame` infers **`E`'s own norm** for the fibre unless `(V := fun z : M ↦ TangentSpace I z)` is passed (lean4#14949 family); the local frame is only locally smooth, so globalise with `exists_contMDiff_eventuallyEq` and recover the germ by `congr_of_eventuallyEq`; and `T%` of a Pi-smul needs a `show` before the rewrite lands |
| `CurvatureOperatorThree.lean` | **proved**: **Hamilton's curvature operator in dimension three as an endomorphism**, `Rm₃ = scal·Id − 2Ric♯`. `sharp` turns a continuous bilinear form on a real Hilbert space into the endomorphism representing it (Riesz, `InnerProductSpace.toDual`); `ricciSharp` and `curvatureOperator` are its instances and `inner_curvatureOperator` (`⟪Rm₃v,w⟫ = scal·⟪v,w⟫ − 2Ric(v,w)`) the defining identity; self-adjointness is `ricciForm_symm`. **The dim-3 content is `CurvatureThree.lean` cashed in and it is cheap**: the trace identity plus pair symmetry gives the diagonal, and the *same* identity read off-diagonally gives that a **Ricci eigenbasis diagonalises `Rm₃`** — `Ric(b₀,b₁)` has one surviving summand in dimension three, so the off-diagonal curvature component *is* the off-diagonal Ricci component. **No Weyl vanishing and no four-linear expansion**; the literature's `Rm = f(Ric)` route is not required. **Three checks against `Pinching.lean`'s normalisation**: diagonal entries are twice the sectional curvature of the **opposite** plane (`inner_curvatureOperator_self`), off-diagonals vanish in a Ricci eigenbasis (`inner_curvatureOperator_eq_zero` — needs **neither** metric compatibility **nor** torsion-freeness), and the trace is `scal` (`sum_inner_curvatureOperator_self`, i.e. `R = λ+μ+ν`); unit `S³` gives `6 − 4 = 2 = 2·1`. **Left for the section**: `Rm₃` as a `C²` section of `End(TM)` (which needs `ricciForm` and `scalarCurvatureAt` smooth as sections — not proved anywhere yet) and its evolution equation off `CurvatureEvolution.lean`. Gotcha: a nested `namespace CovariantDerivative` inside `namespace RicciFlowBlueprint` makes a **different** namespace, and the symptom is an unelaborated `cov` with a metavariable binder type reported far from the cause — close the outer namespace first |
| `EndBundleMetric.lean` | **proved**: **`End(TM)` is a `C^n` Riemannian bundle** — the Hilbert–Schmidt form of `EndMetric.lean` turned into a `ContMDiffRiemannianMetric` and registered. Together with `HomBundle.lean` this gives the **first bundle other than `TM` carrying both a connection and a fibre metric**, so the `Bundle*` layer finally has an instantiation at a real tensor bundle. **The `contMDiff` field was NOT the hard part.** It goes through `contMDiffAt_hom_bundle`, and the one new brick is **`inCoordinates_bilin`**: for a *bilinear form on the fibre* the coordinate representative is the form itself on the trivialisation's inverse, `inCoordinates … Φ u u' = Φ (e.symmL x u) (e.symmL x u')` — the nested Hom bundle unfolds twice and the inner unfolding lands on the **trivial** bundle `B × ℝ`, whose trivialisation is the identity, so **no coordinate change survives**; `hom_trivializationAt_apply` is a `rfl`, so it is two rewrites and a `rfl`. Then `contMDiffAt_clm_of_basis` **twice** (the model fibre `E →L E` is finite-dimensional) reduces to the scalars `y ↦ hsFibre y (σ_y u) (σ_y u')`, smooth by `contMDiffAt_hsFibre_apply` — a local **orthonormal frame of `TM`** computes `hsFibre` as a sum of inner products of tangent vectors (`hsForm_congr`), each term `ContMDiffAt.clm_bundle_apply` then `ContMDiffAt.inner_bundle`. Sections `y ↦ σ_y u` come from `contMDiffAt_localFrame_of_mem` plus `localFrame_eq_symmL'` (the general-bundle form of `LeviCivitaSmooth.lean`'s lemma). **THE REAL OBSTRUCTION IS A TYPE-CLASS ONE — see corrected belief 6.** `RiemannianBundle` cannot be used on `fun y ↦ T_yM →L T_yM` at all, so `EndTangent I x` is a **non-reducible type synonym** for it, with nine fibre/bundle instances transported by `inferInstanceAs`; **the metric transports definitionally** (`endTangentMetric := endMetric`), which is what keeps the *proofs* in the raw world, where every mathlib Hom-bundle lemma matches syntactically, while the *statement* lives on the synonym. `isContMDiffRiemannianBundle_endTangent` is the falsification check. Gotchas: `set_option maxSynthPendingDepth 4` (one deeper than the repo's usual `3`) for `(E →L E) →L (E →L E) →L ℝ`; `endTangentRiemannianBundle` must be **`@[instance_reducible]`**, since mathlib's `IsContMDiffRiemannianBundle` instance matches a `RiemannianBundle` of the syntactic shape `⟨g.toRiemannianMetric⟩` and will not see through a semireducible wrapper; the level-`1` Riemannian instance is carried **alongside** the level-`n` one, because the pointwise orthonormal basis comes from `exists_orthonormalBasis_of_isOrthonormalFrameOn` (stated at level `1`) while the frame's smoothness is wanted at level `n`, and `Module.Basis.orthonormalFrame` is level-free so one frame serves both — no level-lowering lemma needed; and `ContMDiffAt.congr_of_eventuallyEq` cannot infer `f'` from a bare `refine`, so prove the frame-sum smoothness as a `have` first |
| `EndMetricCompat.lean` | **proved**: **the induced connection on `End(TM)` is metric for the Hilbert–Schmidt form** (`mvfderiv_hsFibre_eq`), so `End(TM)` is the **first bundle in the repo other than `TM` carrying a *compatible* connection and metric** — which is what `BundleBochner.lean` and `BundleMaximumPrinciple.lean` ask of the pair, and so the `Bundle*` layer's first real instantiation. **The proof is `TraceCov.lean`'s with one new ingredient**: expand `⟨A,B⟩` over a local orthonormal frame of `TM`, differentiate by metric compatibility downstairs, and substitute `∇_X(A eᵢ) = (∇_X A)(eᵢ) + A(∇_X eᵢ)` — which **is `endCov_apply`, the definition of `∇A` rearranged**. Four groups result: two are the HS forms of `∇_XA` and `∇_XB` on the same frame, and the other two cancel because **the frame is NOT parallel and nothing makes it so** — `⟪∇_Xeᵢ,eⱼ⟫` survives but is antisymmetric (`inner_cov_antisymm`) while `⟪Av,Bw⟫ + ⟪Aw,Bv⟫` is symmetric, so `sum_bilin_of_antisymm` kills it. `isMetricCompatible_endTangentCov` packages this as mathlib's predicate on `EndTangent`, against an **ambient `RiemannianBundle` binder** whose inner product is `hsFibre` (the only shape `IsMetricCompatible` elaborates in), and `isMetricCompatible_endTangentCov_hs` instantiates it at the metric actually built — the falsification check. Gotchas: `MDifferentiableAt.inner_bundle'` lives in **`Bochner.lean`** (not transitively imported) and needs its **explicit name**, dot notation resolving through `ChartedSpace.LiftPropWithinAt`; `IsMetricCompatible.mvfderiv_inner_eq` returns a statement full of beta-redexes that `rw` cannot match — restate it with an ascribed `have h' : … := h`; the bilinear form fed to `sum_bilin_of_antisymm` must be **declared** at the `E` type, so introduce it by an **ascribed existential** (`set` drops the expected type); `exists_orthonormalBasis_of_isOrthonormalFrameOn` gives `b i = fr i x`, the *opposite* orientation to `TraceCov`'s own `hb`; and `isMetricCompatible_iff` additionally wants `[IsContMDiffRiemannianBundle I 1 (E →L[ℝ] E) (EndTangent I)]`, carried as a binder alongside the abstract `RiemannianBundle` |
| `HomBundle.lean` | **proved**: **the induced connection on a Hom bundle** — `covHom cov₁ cov₂ A X σ x = ∇²_X(Aσ) − A(∇¹_Xσ)`, and **`homCov`, a genuine `CovariantDerivative I (F₁ →L F₂) (fun x ↦ V₁ x →L V₂ x)`**, with `endCov` the instantiation at **`End(TM) = Hom(TM,TM)`**. **This is the first covariant derivative in the repo on any bundle but `TM`**, and it is exactly what the `Bundle*` layer was missing: `BundleHessian`, `BundleBochner`, `BundleNormalSection` and `BundleMaximumPrinciple` all take `cov : CovariantDerivative I F V` as a *hypothesis*, and until now had nothing but the tangent bundle to feed it — the `rfl`-bridges back to the tangent-bundle files check that those statements are well-*typed* at `V = TM`, **not** that the layer can be fed a real tensor bundle. **Both slots are free of a frame argument, for `covOneForm`'s reason**: `∇A` has *two* terms against `∇Rm`'s four, so the direction is a CLM by construction and the section slot is the plain Leibniz cancellation — the two `(Xf)·Aσ` terms, one from `∇²` and one from `∇¹`, are **literally equal** — and `mkHom₂` applies on merely `MDiffAt` data. The connection's own axioms are additivity and Leibniz **in `A`**, where the correction is `dg ⊗ A`, not `dg ⊗ (Aσ)`: the derivative falls on `A` as a section of `Hom(V₁,V₂)`. **`End(TM)` is the bundle dimension-3 Hamilton–Ivey needs, and it bypasses the `Λ²` wall outright**: the Weyl tensor vanishes in dimension 3, so Hamilton's curvature operator is `scal·Id − 2 Ric♯`, whose eigenvalues are `Pinching.lean`'s `λ,μ,ν` (`λ = R − 2r₁` from `r₁ = (μ+ν)/2`; checked on the unit `S³`, `6 − 4 = 2 = 2·1`) — an **endomorphism** field, not an element of `Sym²(Λ²TM)`. Gotchas: **mathlib's Hom-bundle instances all synthesise over `TangentSpace` out of the box** (`FiberBundle`, `VectorBundle`, `ContMDiffVectorBundle` — probed 2026-09-18, the only failures being `noncomputable` complaints on bare `example`s), so there is no elaboration wall here; but **`T% A` for a Hom-bundle section cannot infer its model fibre**, so the differentiability predicate has to name it — `IsMDiffHomAt F₁ F₂ A x` with the fibres **explicit** (`variable (F₁ F₂) in`, which like `omit` goes **before** the docstring) and `(I := I)` at every use site, `I` being determined by neither `A` nor `x`; `mdifferentiableAt_extend` lives in the **`FiberBundle`** namespace; `mkHom₂` wants `[FiniteDimensional ℝ F₁]` and `[ContMDiffVectorBundle 1 F₁ V₁ I]` on the **second** bundle only; and in the `leibniz` branch `rw [covHomAt_apply_extend, add_apply, smul_apply, …]` pushes the rewrites to the wrong depth — state `hL`/`hR`/`key` as separate `have`s and finish with one `simp only` |
| `HomBundleSmooth.lean` | **proved**: **the induced connection on a Hom bundle is `C¹`** (`contMDiffCovariantDerivative_homCov`), with `contMDiffCovariantDerivative_endCov` the **instance** at `End(TM)`. `HomBundle.lean` built `homCov` as a `CovariantDerivative` but nothing said it was *smooth*, and `laplacianSection` — hence the whole `∇²`/`Δ`/Bochner layer — needs exactly that. **This was found by attempting the composition, not by inspection**: `BundleMaximumPrinciple` applied to `Rm₃` discharged every other hypothesis and left this one. The proof is two applications of one criterion worth isolating, **`contMDiffAt_hom_section_of_symmL`**: *a section of `Hom(W₁,W₂)` is `C^n` as soon as its values on the SOURCE trivialisation's local frame are* — the **converse** of `ContMDiffAt.clm_bundle_apply`, and what a *constructed* morphism field needs. It is cheap because the `inCoordinates` representative is **literally the target trivialisation's coordinate of `y ↦ A y (e₁.symmL y u)`**, so no coordinate change survives. One application per `Hom` reduces the claim to the two terms of `∇A = ∇²(Aσ) − A(∇¹σ)` on a pair of local frames, each `contMDiff_cov_apply_section` composed with `ContMDiff.clm_bundle_apply`. **Frames globalised first**, for `RicciSection.lean`'s reason. `localFrame_eq_symmL'` moved here from `EndBundleMetric.lean` (it is needed lower in the tower now); `EndBundleMetric.lean` imports this file. Gotcha: `contMDiff_cov_apply_section` wants `CMDiff (k+1)`, so a `hcast : ((1:ℕ∞ω)+1) = 2` and `hcast ▸ _` is needed at every use — the same `ℕ∞` vs `ℕ∞ω` bookkeeping `CurvatureDeriv.lean` documents |
| `CurvatureOperatorMaxPrinciple.lean` | **proved**: **the touching-point maximum principle applied to `Rm₃` on `End(TM)`** (`exists_section_inner_laplacian_curvatureOperator_nonpos`). **This is the falsification check for the entire `End(TM)` line**: the `Bundle*` layer is stated for an arbitrary bundle and until now the only thing it could be applied to was `TM` itself, where the `rfl`-bridges check well-*typedness* and nothing more. Here the connection (`HomBundle`), the fibre metric (`EndBundleMetric`), their compatibility (`EndMetricCompat`), the connection's smoothness (`HomBundleSmooth`) and a `C²` section (`CurvatureOperatorSection`) are all supplied and every hypothesis discharged; only the Riemannian structure of `EndTangent` stays a binder, which `isContMDiffRiemannianBundle_endTangent` instantiates. **Gotcha, and it is why the binders are binders**: under a *statement-level* `letI` of the concrete metric, a `letI` in the proof introduces a local instance whose derived `InnerProductSpace` arguments are **not syntactically** the ones the statement's inlined `letI` produced, so a `haveI`-registered `IsContMDiffRiemannianBundle` does not match the goal. State against binders and instantiate once, as `isMetricCompatible_endTangentCov_hs` does |
| `RicciIdentity.lean` | **proved**: **the Ricci identity for a bilinear form field** — `(∇²_{W,X}h)(Y,Z) − (∇²_{X,W}h)(Y,Z) = −h(R(W,X)Y,Z) − h(Y,R(W,X)Z)` (`cov2Bilin_sub_swap`). Commuting the two derivatives costs **one curvature term per slot of `h`**: none for a function (`hessianFun_symm`), one for a section (`hessian_sub_hessian_swap`), two here. This is the commutation rule roadmap item 3 needs — with `h = −2Ric`, turning `∇²Ric` into `Δ Rm + Q` is a matter of moving derivatives past each other. **All the content is in one expansion**, `cov2Bilin_eq_hessianFun`: `∇²h` is the Hessian of the *scalar* `h(Y,Z)` plus terms that are either `∇h` in one slot or `h` applied to a second derivative of a field — and **the first derivative of the scalar cancels** between the leading term and the `∇_{∇_W X}` correction, which is why no first-order term survives. Antisymmetrising then kills the Hessian (a function's is symmetric — torsion-freeness, first use), cancels the `∇h` and cross terms outright, and leaves `h(∇_{[W,X]}Y − ∇_W∇_X Y + ∇_X∇_W Y, Z)` = `−h(R(W,X)Y,Z)` (torsion-freeness, second use). **Gotchas, all the `E` vs `TangentSpace` family and worth reading before the next file of this kind**: `rw` with an `E`-stated `map_sub` will not match a `TangentSpace`-typed `-`, **and a type ascription `(v : E)` does not fix it** — the ascribed term still carries the `TangentSpace` instance and prints identically. The fix that works is to state the whole step over `E`-typed *variables* — `∀ a b c d : E, a - b = c → h x a d - h x b d = h x c d` — prove it there by `rw`, and apply it with `exact`/`_`, which unifies up to defeq. Also: `mvfderiv_fun_sub` returns an equation of *CLMs*, so the applied form needs a trailing `rfl` (`simp only [sub_apply]` fails on the same transparency issue); and `omit [CompleteSpace E]` fails on anything mentioning `curvature`, which references the instance. **And the same identity one level up is proved too** — **the Ricci identity for the curvature tensor** (`cov2Curvature_sub_swap`): `(∇²_{X,Y}Rm)(A,B)C − (∇²_{Y,X}Rm)(A,B)C = R(X,Y)(Rm(A,B)C) − Rm(R(X,Y)A,B)C − Rm(A,R(X,Y)B)C − Rm(A,B)(R(X,Y)C)`, **one curvature term per slot — three inputs and the vector output**. This is what lets the *differentiated* second Bianchi identity be antisymmetrised. The expansion `cov2Curvature_eq_hessian` is the bilinear one one level up (`∇²Rm` against `hessian` of the *section* `Rm(A,B)C`, same first-derivative cancellation) and needs **no regularity of the connection at all**: the only splitting is `∇` over a difference of sections, which is an axiom, where the bilinear case had to differentiate a scalar. Antisymmetrising, the `∇Rm` terms cancel because each appears in both orders and the cross terms `Rm(∇_YA,∇_XB)C` because the swap maps the pair to itself; the output term is `hessian_sub_hessian_swap`, and the three input terms are one lemma each (`curvature_fst/snd/thd_second_deriv_comm`). **The third slot is the expensive one, as everywhere in this tower**: slots 1–2 combine their four second-derivative terms with additivity plus `TensorialAt.pointwise` on `tensorialAt_curvature_fst`/`_snd`, but slot 3 needs `curvature_add_right` and `curvature_congr_third`, which want `C²` sections and — because that lemma argues through an orthonormal frame — a **metric**, so it lives in its own section with `[T2Space M] [RiemannianBundle …] [IsContMDiffRiemannianBundle I 2 …]`. **Do not reach for `mkHom` here**: `rw [← TensorialAt.mkHom_apply …]` sends the unifier through `FiberBundle.extend` and does not return (the documented `whnf` timeout); additivity + `pointwise` + `linear_combination (norm := module)` does the same job in under a minute |
| `BianchiDeriv.lean` | **proved**: **the differentiated second Bianchi identity** — `(∇²_{X,Y}Rm)(A,B)C + (∇²_{X,A}Rm)(B,Y)C + (∇²_{X,B}Rm)(Y,A)C = 0` (`cov2Curvature_cyclic_eq_zero`), the cyclic identity of `bianchi_second` one derivative up. **Nothing is differentiated twice by hand**: `∇²Rm` is `∇Rm` differentiated once with one correction per slot, so the cyclic sum splits into a leading part and four correction groups, and **all five vanish by the *undifferentiated* identity** — the leading parts combine (additivity of `∇` on sections) into `∇_X` of the section `bianchi_second` says is zero, and each correction group is the cyclic sum with one field replaced by its covariant derivative. So the proof is five `bianchi_second` and one `∇0 = 0`. **The corrections cancel three at a time, not in pairs** — one group per differentiated field. **No metric**: all three Riemannian instances are `omit`ted, exactly as for `bianchi_second`; the trace step is where a metric first becomes unavoidable. Regularity one derivative above `bianchi_second`, last slot still the expensive one (`C` at `C⁴` so `∇_X C` keeps the `C³` the spectator slot wants). **Gotchas**: `IsCovariantDerivativeOn.add` produces a *Pi*-typed `+`, so `rw` will not match it against a lambda-typed one — state the additivity steps in **applied** form (`congrArg (fun L ↦ L (X x))`) and let defeq do the rest; `bianchi_second`'s `x` is implicit and undetermined in a bare `have`, so pass `(x := x)`; `simp only [cov2Curvature]` makes no progress, use `unfold`; and `ContinuousLinearMap.zero_apply` is deprecated (a CI error) → bare `zero_apply` . **The trace is taken too** (`curvatureLaplacian_eq_neg_sum`): summing over `X = Y = eᵢ` puts the first cyclic term on the diagonal, where it *is* `Δ Rm` (`curvatureLaplacian_eq_sum_frame`), leaving `Δ Rm(A,B)C = −∑ᵢ(∇²_{eᵢ,A}Rm)(B,eᵢ)C − ∑ᵢ(∇²_{eᵢ,B}Rm)(eᵢ,A)C`. **This is where the metric enters and the only place so far.** It is also what forces the Ricci identity for `Rm`: in both surviving sums the traced index sits in the **outer derivative slot**, whereas `Divergence.lean` traces the slot `∇Rm` is pointwise in — so the two derivative slots must be swapped by `cov2Curvature_sub_swap`, and *that* swap is what produces the quadratic terms . **The reordering is done too** (`curvatureLaplacian_eq_swapped`), with the quadratic terms named: `curvatureCommutator X Y A B C` is the RHS of the Ricci identity for `Rm`, and `Δ Rm(A,B)C = −∑ᵢ(∇²_{A,eᵢ}Rm)(B,eᵢ)C − ∑ᵢ(∇²_{B,eᵢ}Rm)(eᵢ,A)C − ∑ᵢ C(eᵢ,A;B,eᵢ) − ∑ᵢ C(eᵢ,B;eᵢ,A)`. **This is `∂ₜRm = ΔRm + Q` in outline**: the first two sums are `∇` of a divergence of `Rm` and become `∇²Ric` by the contracted second Bianchi identity; the last two **are** `Q`, quadratic because `curvatureCommutator` is `Rm` applied to `Rm`. **`Q` is not bolted on at the end — it is exactly what reordering the derivative slots costs.** `cov2Curvature_sub_swap'` is the usable form of the Ricci identity: all **twenty-five** differentiability hypotheses of `cov2Curvature_sub_swap` follow from uniform `C³` fields with `C` at `C⁴`, which is what the trace already carries — without it the identity cannot be applied inside a frame sum. Note `BianchiDeriv.lean` imports `RicciIdentity.lean` for this, and `IsContMDiffRiemannianBundle I 2` does synthesise from `I 3` . **Step (iii) is CLOSED** (`inner_curvatureLaplacian_eq`): `⟪Δ Rm(A,B)C, D⟫ = −(∇_A div Rm)(C,D,B) + (∇_B div Rm)(C,D,A) − Q`, with `Q` the two commutator sums and `covDivCurvature` naming `∇_A` of the `(0,3)`-tensor `div Rm(C,D,B) = (∇_C Ric)(D,B) − (∇_D Ric)(C,B)`. **Every term is accounted for**: two second derivatives of `Ric`, two quadratic in `Rm`. The chain is `sum_inner_covCurvature_snd_eq` (pair symmetry puts the traced index back in the output slot, where `divCurvature` lives — needs only a `C²` connection) → `sum_inner_covCurvature_frame_deriv_eq_zero` (the two frame-derivative terms cancel; **pair symmetry again**, putting both into `covCurvatureBilinDir` = `innerSL ∘ covCurvatureEndo`, so `sum_bilin_of_antisymm` applies verbatim — its statement is *already* the symmetrised form) → `sum_inner_cov2Curvature_snd_eq_mvfderiv` (`∇_A` outside the trace, **no curvature term survives**) → `sum_inner_cov2Curvature_snd_eq_covRicci` (the leading term needs the identity as a *function* on the frame's domain, so an `OrthonormalBasis` at each `y` plus `EventuallyEq.mvfderiv_eq`) → **`cov2Curvature_antisymm`** (`∇²Rm` inherits `Rm`'s antisymmetry; each of the five terms flips, and the two corrections in `∇_X A`/`∇_X B` **swap with each other**) → the capstone. Regularity peaks at `C⁴` for all four fields and the frame — pair symmetry wants `C³` in the slot `∇_A eᵢ` occupies. **Gotchas**: `MDifferentiableAt.inner_bundle'` needs its explicit name, dot notation resolving through `ChartedSpace.LiftPropWithinAt`; `mvfderiv_fun_sum` lives in `RicciFlowBlueprint`, not `CovariantDerivative`; and in `cov2Curvature_antisymm` a `show _ = -(…)` leaves the **LHS folded**, so `module` sees `1 = 0` — `unfold cov2Curvature` both sides instead |
| `CurvatureFlow.lean` | **proved**: **the first variation of `Rm` with the commutator pair collapsed** — step (iv) started. `inner_curvatureOfTwoTensor_eq` (in `KoszulSecondDeriv.lean`) pairs `∂ₜRm(X,Y)Z = (∇_XA)(Y,Z) − (∇_YA)(X,Z)` against `W` and applies `inner_covTwoTensor_eq` twice, giving **three antisymmetrised pairs of `∇²h`**; `inner_curvatureOfTwoTensor_eq_curvature` evaluates the first. **The three pairs are not alike, and the whole evolution equation is that asymmetry.** Only the first is antisymmetrised in `∇²h`'s two *derivative* slots, so only it is a commutator: `cov2Bilin_sub_swap` collapses it and it **loses its derivatives entirely**, becoming `h` applied to `Rm` — under the flow (`h = −2Ric`) exactly the terms quadratic in the curvature, i.e. `Q`. The other two antisymmetrise a derivative slot against an *argument* slot, so no commutation rule applies; they keep their derivatives and are what `BianchiDeriv.lean`'s trace step turns into `Δ Rm`. Two uniform hypotheses (`∇h` and `A` differentiable on `C²` fields) replace six pointwise ones. **Gotcha, new and nasty**: inserting a declaration between an `omit [..] in` and the docstring it guards **silently reattaches the omit to the new declaration** — the old one then trips the unused-section-variable linter, which is a CI error. Put a new declaration *before* the `omit`, with its own. **Gotcha**: a hypothesis mentioning `x` placed *before* `{x : M}` in the binder list auto-binds a **fresh** `x`, and the error is an `x` vs `x✝` mismatch far away in the proof — declare `{x : M}` first . **AND ROADMAP ITEM 3 IS CLOSED** (`inner_derivCurvature_eq_curvatureLaplacian_add`): `⟪∂ₜRm(X,Y)Z,W⟫ = ⟪Δ Rm(X,Y)Z,W⟫ + Q₁ + Q₂ + Ric(R(X,Y)Z,W) + Ric(Z,R(X,Y)W)`. **Every term on the right is either `Δ Rm` or quadratic in the curvature, and the two sources of quadratic terms are different**: `Q₁+Q₂` is what reordering the derivative slots of `∇²Rm` cost (the trace step), the two `Ric`-against-`Rm` terms are what commuting the derivative slots of `∇²h` cost (the first variation). Neither is bolted on; **each is a commutator defect**. The join needs one bridge, `covDivCurvature_eq_cov2Bilin`: the trace step speaks `covRicci` and the flow side `cov2Bilin ricciForm`, and the three corrections regroup **not termwise** but one group per differentiated slot. **Then four `∇²Ric` terms cancel in pairs by symmetry of `Ric` alone** — the flow side gives `(∇²_{X,W}Ric)(Y,Z)` where the trace step gives `(∇²_{X,W}Ric)(Z,Y)`; `cov2Bilin_symm` from `ricciForm_symm`, free. **What this is and is not**: it is the identity for the Koszul tensor `A` of `h = −2Ric`. Hooking it to an actual `IsRicciFlowAt` is one further specialisation — `isKoszulOf_derivDifference` plus `innerE_deriv_eq_of_isRicciFlowAt'`, under the two-step `letI`, exactly as `ScalarFlow.lean` did for the scalar equation. **Gotcha**: `ricciForm_symm` takes `hmet`/`htor` and is stated at a point, so `cov2Bilin_symm`'s `∀ y v w` form needs `fun y v w ↦ cov.ricciForm_symm (x := y) hmet htor v w`; and neither bare `smul_apply` nor `ContinuousLinearMap.smul_apply` fires on `((-2 • ricciForm x) v) w` — state the two instances as concrete `rfl` equations |
| `CurvatureEvolution.lean` | **proved**: **`∂ₜRm = Δ Rm + Q` for an actual Ricci flow** — the close of roadmap item 3. `CurvatureFlow.lean` proves the identity for *any* `(1,2)`-tensor `A` Koszul for `h = −2Ric`; here `A = ∂ₜ∇` and `h = ∂ₜg`, so both hypotheses become theorems — `isKoszulOf_derivDifference` (`∂ₜ∇` is Koszul for `∂ₜg`) and `innerE_deriv_eq_of_isRicciFlowAt'` (`∂ₜg = −2Ric`), which also *derives* the `hb`/`hf2`/`hd` differentiability hypotheses from Ricci-side ones by `covBilin_smul_form`. `inner_derivCurvature_eq_curvatureLaplacian_add_of_isRicciFlowAt` is the identity; `hasDerivAt_inner_curvatureE_eq_curvatureLaplacian_add_of_isRicciFlowAt` composes it with `hasDerivAt_curvatureE_covTwoTensor` into a genuine **`HasDerivAt` in `t`**. **The pairing is against the metric frozen at `t₀`** (`innerE (g t₀) x`), so the left-hand side is `∂ₜ` of the `(1,3)`-tensor and no `∂ₜg` term of its own appears — and typing it that way is also what keeps `⟪·,·⟫` off an `E`-valued argument. **Three levels are carried as explicit hypotheses under the `letI`** — `IsContMDiffRiemannianBundle I 3`, `ContMDiffCovariantDerivative … 2` and `… 3` — because `g : ℝ → ContMDiffRiemannianMetric I 2` gives only `I 2` and a `C¹` connection, the same reason `ScalarFlow.lean` carries `hlc2`. **Gotchas**: `ContDiffAt.comp_contMDiffAt` takes its point *implicitly* (passing `x` is an application type mismatch), and `fun y ↦ c * f y` is `(contDiffAt_const.mul contDiffAt_id).comp_contMDiffAt hf` — `ContMDiffAt.mul` on `ℝ` still wants the Lie-group instance it lacks; and pushing the CLM through the `TangentSpace`-typed difference `D₁ − D₂` needs the `map_sub` *stated* at the goal's exact form and closed by `exact map_sub _ _ _`, since `rw [map_sub]` will not match it |
| `CurvatureTrace.lean` | **proved**: **the first-slot trace commutes with `∇²`** — `∑ᵢ ⟪(∇²_{U,V}Rm)(eᵢ,X)Z, eᵢ⟫ = (∇²_{U,V}Ric)(X,Z)` (`sum_inner_cov2Curvature_fst_eq_cov2Bilin`), and traced once more **`∑ᵢ ⟪(Δ Rm)(eᵢ,X)Z, eᵢ⟫ = (Δ_g Ric)(X,Z)`** (`sum_inner_curvatureLaplacian_fst_eq_laplacianBilin`). `Divergence.lean`'s `sum_inner_covCurvature_eq` one derivative up, and what tracing `∂ₜRm = ΔRm + Q` in its first slot consumes. **The five corrections of `∇²Rm` meet the three of `∇²h` exactly**: three are `∇Rm` with one of `V`,`X`,`Z` differentiated and trace termwise; the other two — the leading `∇_U` and the correction in the *frame* slot — combine into `mvfderiv` of the traced function up to the frame-derivative residue `∑ᵢ[⟪(∇_V Rm)(∇_U eᵢ,X)Z,eᵢ⟫ + ⟪(∇_V Rm)(eᵢ,X)Z,∇_U eᵢ⟫] = 0`. **`sum_bilin_of_antisymm` does NOT apply and that is the one real obstacle**: it wants a bundled `E →L[ℝ] E →L[ℝ] ℝ`, but the pairing is `∇Rm` in its *second* slot, where tensoriality (`covCurvature_smul_snd`) demands a `C²` coefficient, so `TensorialAt.mkHom` — which quantifies over merely-`MDiffAt` sections — is unavailable (corrected belief 1). **The remedy is to expand the frame derivative with CONSTANT coefficients** `cᵢⱼ = ⟪∇_U eᵢ, eⱼ⟫`, legitimate because slot 2 is pointwise (`covCurvature_congr_snd`) and constants are `C^∞`; the cancellation is then purely combinatorial (`sum_antisymm_pair_eq_zero`, the bilinearity stripped out of `sum_bilin_of_antisymm`) and **no bilinear form is ever bundled**. Lemma B is one `Finset.sum_comm`: both Laplacians come off the same frame and the two traces are independent sums. Regularity: `U`,`V` and the frame at `C³`, the two `Rm` slots `X`,`Z` at `C⁴` — `X` one level above `V` because `∇_U X` must keep the `C³` that `covBilin_ricciForm_eq_covRicci` wants of a form slot. Gotchas: `exists_orthonormalBasis_of_isOrthonormalFrameOn` gives `b i = fr i x`, so `rw [hcdef]` leaves a beta-redex — use `simp only [hcdef]`; and `sum_inner`/`inner_sum` will not `rw` on a `TangentSpace`-typed `Finset.sum` (they pick `E`'s `AddCommMonoid`), so the three bilinearity steps are stated over a variable inner product space (`inner_sum_smul_left/_right`, `sum_inner_fin`) and applied with `exact` |
| `RicciEvolution.lean` | **proved**: **Hamilton's evolution equation for the Ricci tensor** — `∂ₜRic(Y,Z) = (Δ_g Ric)(Y,Z) + Q₁ + Q₂ + ∑ᵢRic(Rm(eᵢ,Y)Z,eᵢ) + ∑ᵢRic(Z,Rm(eᵢ,Y)eᵢ)` (`derivRicciFormOfMetric_eq_laplacianBilin_add_of_isRicciFlowAt`), the **first-slot trace** of `CurvatureEvolution.lean`'s `∂ₜRm = ΔRm + Q`, taken at `X = W = eⱼ` and summed. **Nothing new is computed**: the left-hand side is `derivRicciFormOfMetric_eq_sum` — `∂ₜRic(Y,Z) = ∑ᵢ⟪∂ₜRm(eᵢ,Y)Z,eᵢ⟫`, the **inner half of `metricTraceE_derivRicciFormOfMetric_eq`'s double-trace argument, extracted** (`derivRicciFormOfMetric_apply` + `LinearMap.trace_eq_sum_inner` + `derivCurvatureEndoE_apply_field`) — and the leading term on the right is `CurvatureTrace.lean`'s `sum_inner_curvatureLaplacian_fst_eq_laplacianBilin`. The other four terms carry **no derivatives**: two commutator sums and two `Ric`-against-`Rm` terms. **Together with `∂ₜscal = Δscal + 2|Ric|²` (`ScalarFlow.lean`) this is what `Rm₃ = scal·Id − 2Ric♯` evolves by, since `Rm₃` is built from exactly those two.** The hypothesis block is `CurvatureEvolution.lean`'s with `X`,`W` dropped, `hRic2` ∀-quantified over the fourth slot (it is instantiated at `fr j`), and `hcb` (`IsMDiffCovBilinAt ricciForm Y Z x`) added for `laplacianBilin_eq_sum_frame` — `hdRic` only gives `CMDiff 2` directions and the Laplacian wants `CMDiff 1`. `IsMDiffBilinAt ricciForm x` is free from `hRic` (`fun U V _ _ ↦ hRic U V x`). **What is left for `∂ₜRm₃`**: `Δ` on `End(TM)` of `Ric♯` against `(Δ_g Ric)♯` — the sharp commuting with the Laplacian — and `Δ(scal·Id) = (Δscal)·Id` |
| `SharpLaplacian.lean` | **proved**: **index raising commutes with `∇`, `∇²` and `Δ`** — for an endomorphism field `A` of `TM` and a bilinear form field `h` with `⟪A v,w⟫ = h(v,w)` (i.e. `A = h♯`), `⟪(∇_X A)v,w⟫ = (∇_X h)(v,w)` (`inner_endCov_eq_covBilin`), `⟪(∇²_{X,Y}A)v,w⟫ = (∇²_{X,Y}h)(v,w)` (`inner_hessianSection_eq_cov2Bilin`) and **`⟪(Δ A)v,w⟫ = (Δ_g h)(v,w)`** (`inner_laplacianSection_eq_laplacianBilin`). **This is the bridge the dim-3 maximum principle needs**: `BundleMaximumPrinciple` concludes `⟪n, Δu⟫ ≤ 0` with `Δ = laplacianSection` of `endCov`, while every evolution equation here produces `Δ_g` of a *bilinear form* — without this the two say nothing to each other. **There is no `sharp` in the statements and none in the proofs**: the interface is the hypothesis `⟪A y v,w⟫ = h y v w`, which `inner_ricciSharp` and `inner_curvatureOperator` supply on the nose; carrying Riesz through the rewriting would only cost. **Metric compatibility is the whole content, once per derivative**: `∇A = ∇(Aσ) − A(∇σ)` by definition, and moving `∇` off the first term onto the pairing turns it into `X(h(σ,σ')) − h(σ,∇σ')`, which with the second term *is* `∇h`; `∇²` is that twice with the corrections matching term for term, and the trace is free — both Laplacians come off the same frame. **Falsification check**: `inner_laplacianSection_ricciSharp_eq_laplacianBilin`, `Δ(Ric♯) = (Δ_g Ric)♯`, on `contMDiff_ricciSharp` (new in `CurvatureOperatorSection.lean` — `contMDiffAt_end_section_of_symmL` with only the sharp term, the curvature operator's proof minus the scalar part). The `∀y`/`IsMDiffHomAt` hypotheses of the `Δ` statement are all **derived from one `hAsm : CMDiff 2` of the section** via `isMDiffHomAt_of_section` and `mdiffAt_cov_apply_section`; only the `∇` and `∇²` statements carry them pointwise. Gotchas: `rw` on a `covBilin`-unfolded goal reports an `(h y) (P y)` type mismatch (`TangentSpace` vs `E`) — do not rewrite, collect the pieces as `have`s and close with `linarith`; `mdiffAt_cov_apply_section` does **not** take the `covT` section variable; and `IsContMDiffRiemannianBundle I 4` does not downgrade to `1`/`2`/`3` by search, so every level used needs its own binder |
| `IveyEndo.lean` | **proved**: **Hamilton's pinching set as a closed convex set of ENDOMORPHISMS** — `iveyEndoSet W = {A | −3 ≤ tr A ∧ iveyG (λ_min A) ≤ tr A}`, with `convex_iveyEndoSet`, `isClosed_iveyEndoSet`, and the bridge `mem_iveyEndoSet_iff_isIveyPinched`. **This is the piece CLAUDE.md named as the one still to build**: `IveyConvex.lean` has the set in `ℝ³`, but the tensor maximum principle runs on a *section of a bundle*, and in dimension 3 that bundle is `End(TM)` — so what must be closed and convex is a set of operators, with no eigenvalue coordinates. **The ordering conditions `ν ≤ μ ≤ λ` DISAPPEAR**: they are a labelling of eigenvalues, not a property of an operator, so only two conditions remain. **`λ_min` is a Rayleigh infimum, NOT an eigenvalue**, and that is the whole economy of the file: `lambdaMin A = ⨅_{‖v‖=1} ⟪Av,v⟫` is an infimum of *linear* functionals of `A`, so `lambdaMin_concave` and `lipschitzWith_lambdaMin` (`|λ_min A − λ_min B| ≤ ‖A−B‖`, which is what gives closedness) are two lines each from `le_ciInf`/`ciInf_le` — a spectral definition would first need the eigenvalues to depend continuously on `A`. **No self-adjointness is assumed anywhere.** Convexity is then: trace linear, `λ_min` concave, `iveyG` convex **and antitone** (`antitone_iveyG`, new), so `iveyG ∘ λ_min` is convex. **The bridge is the falsification check** — an unidentified convex set is worth nothing — and both halves rest on one identity, `inner_apply_self_eq_sum` (`⟪Av,v⟫ = ∑ᵢ eᵢ⟪bᵢ,v⟫²` for `A` diagonal in an orthonormal basis); **Parseval is not imported for the normalisation**, since that identity *at `A = id`* gives `∑ᵢ⟪bᵢ,v⟫² = ⟪v,v⟫` (`sum_sq_inner_eq`). Gotchas: `LinearMap.trace_eq_sum_inner` produces `∑ᵢ ⟪bᵢ, T bᵢ⟫`, the *opposite* orientation to the one you write, so `real_inner_smul_right` not `_left`; `monotoneOn_iveyF`'s membership hypotheses need `Set.mem_Ici.mpr` explicitly (a bare `le_max_right` does not unify); and `NormedSpace.sphere_nonempty_rclike` gives the subtype `Nonempty` directly, which is what `le_ciInf` needs. **Naturality**: `endoConj S A = S A S⁻¹` for a `≃ₗᵢ[ℝ]`, and `mem_iveyEndoSet_conj_iff` — **every fibre isometry preserves the set**, because `lambdaMin_conj` (an isometry carries unit vectors to unit vectors and preserves the pairing, so the two Rayleigh *ranges* coincide) and `traceEndo_conj` (mathlib's `LinearMap.trace_conj'` through the coercion, `ext v; rfl`). Gotcha there: `conv_lhs => rw [← S.apply_symm_apply w]` rewrites **both** occurrences of `w`, including the one inside `S.symm w` — state `key : ∀ a b, ⟪a,b⟫ = ⟪S a, S b⟫` and rewrite the RHS instead. **`lambdaMin` and its API are upstream candidates** — mathlib has `ContinuousLinearMap.rayleighQuotient` and the `iSup`/`iInf`-over-the-sphere identities but no `λ_min` with concavity or a Lipschitz bound |
| `CurvatureOperatorPinched.lean` | **proved**: **Hamilton's pinching condition on `Rm₃` itself** — `exists_mem_iveyEndoSet_iff`: at every point of a 3-manifold there are `λ ≥ μ ≥ ν` (the eigenvalues of `Rm₃`) with `Rm₃ x ∈ iveyEndoSet ↔ IsIveyPinched λ μ ν`. **The spectral theorem is used once and mathlib does all of it**: `isSymmetric_curvatureOperator` is `inner_curvatureOperator_comm` restated (and rests on nothing but symmetry of `Ric`), so `LinearMap.IsSymmetric.eigenvectorBasis` gives the eigenbasis — **and mathlib's `eigenvalues` is ALREADY SORTED decreasing** (`eigenvalues_antitone`), which is exactly `λ ≥ μ ≥ ν`, so **no sorting permutation and no `OrthonormalBasis.reindex` appears**; a first plan reached for `Tuple.sort` and `Fin.revPerm` and none of it was needed. **This is the composition the dim-3 route had been claiming**: the operator, its self-adjointness, the endomorphism set and the eigenvalue dictionary were all present and none said anything to the others until composed — and composing them is what checks that `IveyEndo.lean`'s convexity is convexity of *Hamilton's* set. Gotcha: `iveyEndoSet` needs `[Nontrivial W] [FiniteDimensional ℝ W]` in the *statement*, and neither synthesises for `TangentSpace I x` (`TangentSpace` is non-reducible), so both are carried as instance binders after `{x : M}` rather than derived in the proof |
| `IveyParallel.lean` | **proved**: **the pinching set is a parallel subbundle** (`exists_parallelTransport_mem_iveyEndoSet_iff`) — for a metric connection and any differentiable curve on a preconnected open set, the transport isometry `P` satisfies `endoConj P A ∈ iveyEndoSet ↔ A ∈ iveyEndoSet`. **This is the hypothesis the cross-fibre half of the maximum principle asks for, and it costs NO transport computation**: Hamilton requires `x ↦ K_x` to be preserved by parallel transport, and here *every* fibre isometry preserves it (`mem_iveyEndoSet_conj_iff`) while transport is one (`exists_parallelTransportIsometry_global`). **Stated separately from the naturality lemma on purpose**: the naturality knows nothing about a connection and the transport theorem knows nothing about the set, so neither alone is what the principle wants, and a general lemma with no instantiation is a liability — one line joins them. Gotchas: `MDiffAlongAt`/`IsParallelAlong`/`velocity` live in the **`CovariantDerivative`** namespace, so `open CovariantDerivative` even though the file's own namespace is `RicciFlowBlueprint`; `exists_parallelTransportIsometry_global` needs `[T2Space M]`, which the transport files carry but `IveyEndo`'s binder list does not; and the four fibre instances (`Nontrivial`/`FiniteDimensional` at `γ t₀` and `γ t`) must be **statement binders**, neither being synthesisable for `TangentSpace` |
| `MetricParallel.lean` | **proved**: **the metric is parallel** — `covBilin_metricForm` (`∇g = 0`), `cov2Bilin_metricForm` (`∇²g = 0`) and `laplacianBilin_metricForm` (`Δ_g g = 0`), on `metricForm I y = innerSL ℝ` **declared at the `E` type** (the `curvatureBilinFst` rule: at the `TangentSpace` type it is defeq but no later `rw` matches). **`∇g = 0` is metric compatibility rearranged** — `mvfderiv_inner_eq` gives `X⟪Y,Z⟫ = ⟪∇_XY,Z⟫ + ⟪Y,∇_XZ⟫`, which are exactly the two corrections `covBilin` subtracts; then every term of `∇²g` is `∇g`, the leading one because it differentiates the *zero function* (hence `covBilin_metricForm_eq_zero`, the `∀y` form, is what the second-order lemma actually consumes). **This had never been stated**: every earlier use went through `IsMetricCompatible` directly, so the metric itself was not an input to the bilinear-form calculus. **Why it is wanted**: `Rm₃ = scal·Id − 2Ric♯` carries the metric twice, and the index raising is where the moving fibre metric bites (`SharpLaplacian.lean`); the `(0,2)` form `scal·g − 2Ric` has no index raising, and this is the part of its calculus `Ric` does not already supply. Also `isMDiffBilinAt_metricForm` (`MDifferentiableAt.inner_bundle'`) and `isMDiffCovBilinAt_metricForm` (the zero function is differentiable). **And the varying-scalar Leibniz rule is here too** (`covBilin_fun_smul`: `∇(f·h) = df ⊗ h + f∇h`) — the repo had only the *constant* case (`covBilin_smul_form`), which is all a rescaling `-2Ric` needs, and a term like `scal·g` needs the general one. At `h = g` it collapses at every order: `∇(f·g) = df⊗g`, `∇²(f·g) = ∇²f⊗g` (`cov2Bilin_fun_smul_metricForm` — the leading term splits by the product rule, metric compatibility turns `W⟪Y,Z⟫` into the two corrections in `Y`,`Z`, they cancel against the ones `∇²` subtracts, and `hessianFun f W X x · ⟪Y,Z⟫` is left) and **`Δ_g(f·g) = (Δf)·g`** (`laplacianBilin_fun_smul_metricForm`) — **the trace is free because `∇²f` IS `∇` of the one-form `df`**, so `laplacianFun_eq_sum_frame` reads `Δf` off the *same* frame `laplacianBilin_eq_sum_frame` uses and it is the second statement termwise; the two canonical traces run over *different* families (`FiberBundle.extend` vs `exists_contMDiff_two_extension`), so a bare `Finset.sum_congr` does **not** work. The criteria `isMDiffBilinAt_fun_smul_metricForm` and `isMDiffCovBilinAt_fun_smul_metricForm` are derived, not assumed: `∇(f·g) = df⊗g`, so all it asks of `f` is that `df` be a differentiable one-form — the hypothesis `Δf` already carries. Gotchas: passing `_` for `h` in `covBilin_fun_smul` unifies it with `RiemannianBundle.g.inner` off the *inner-product* differentiability argument rather than with `metricForm I` — pass it explicitly; and `hdf X hX` is a beta-redex that `mvfderiv_fun_mul` will not match, so bind `have hdfX : MDiffAt (fun y ↦ mvfderiv I f y (X y)) x := hdf X hX` first. **Linearity of `Δ_g` in the FORM is here too** (`laplacianBilin_smul_form`, `laplacianBilin_add_form`, and the combined `laplacianBilin_fun_smul_metricForm_add`: `Δ_g(f·g + h) = (Δf)·g + Δ_g h`), on `covBilin_add_form`/`cov2Bilin_add_form` (new in `BilinDeriv.lean`/`BilinLaplacian.lean`). **The additivity could NOT be stated like the rescaling**: `cov2Bilin_smul_form` asks for `h(U,V)` differentiable against *arbitrary* fields, which is fine for `Ric` but **false at `f·g`** — and `f·g` is exactly the summand it is wanted for. So `cov2Bilin_add_form` takes the conditional `∀ y, IsMDiffBilinAt h y` plus `CMDiff 2` on `Y`,`Z` and `MDiffAt` on `W`, and the four terms are split separately: the leading one as a *function* of `y` (hence `Y`,`Z` differentiable everywhere), the three corrections only at `x` (hence `mdiffAt_cov_apply` for `∇_W Y`, `∇_W Z`). Gotcha: `[T2Space M]` is needed **only** by `laplacianBilin_metricForm` (through `exists_contMDiff_two_extension` inside `cov2BilinAt`), so it is a `variable` introduced just before that theorem rather than a block binder with four `omit`s |
| `CurvatureFormThree.lean` | **proved**: **`Δ_g(scal·g − 2Ric) = (Δ scal)·g − 2 Δ_g Ric`** (`laplacianBilin_curvatureFormThree`) — the **spatial** half of the dimension-three evolution equation, with the indices down. `curvatureFormThree` is `Rm₃♭ = scal·g − 2Ric`, written as a **sum** `(scal • metricForm) + ((-2) • ricciForm)` rather than a difference so the linearity lemmas apply on the nose. **Why the `(0,2)` form**: `Rm₃ = scal·Id − 2Ric♯` carries the metric twice, once in `scal` and once in the index raising, and the index raising is where a moving fibre metric bites (`SharpLaplacian.lean`); `Rm₃♭` has no index raising at all. Both summands are then the metric calculus of `MetricParallel.lean`: `∇g = 0` collapses `∇²(f·g)` to `∇²f⊗g` whose trace is `(Δf)·g` (free, because `∇²f` **is** `∇` of the one-form `df`, so both sides come off one frame), and `Δ_g` is linear in the form. Supporting: `isMDiffBilinAt_neg_two_ricciForm`, `isMDiffCovBilinAt_neg_two_ricciForm` (the two criteria for `(-2)•Ric`, derived from the `Ric` ones by `covBilin_smul_form`). **What this does NOT do**: the *time* derivative. `Rm₃♭` still depends on `g_t` through `scal` and through `g` itself, so `∂ₜ` of it produces a `∂ₜg` term — algebraic under the flow (`−2Ric`), but the assembly is not done. **Gotcha, and the fix is structural**: the unused-section-variable linter flags `[ContMDiffVectorBundle 1 …]`/`[VectorBundle ℝ E …]` on the helper lemmas, but `omit` **refuses** them (`cannot omit referenced section variable`) — the remedy is to move those binders to a `variable` line *below* the helpers and above the theorem that needs them, as `MetricParallel.lean` does for `[T2Space M]`; `set_option linter.unusedSectionVars false` is not needed. Also: carry `IsContMDiffRiemannianBundle I 2` **only** — adding the level-`1` binder alongside it is what made the omits unsatisfiable, and level `1` synthesises from `2`. `rw [curvatureFormThree]` fails with "failed to rewrite using equation theorems" — `unfold` |
| `TensorPreservation.lean` | **proved**: **the maximum principles with a MOVING metric** — `le_of_laplacian_moving` and `mem_of_laplacian_moving` for `∂ₜu = Δ_{g_t}u + F t (u)` against a family `g : ℝ → ContMDiffRiemannianMetric I 2`. **A moving Laplacian costs nothing**, for the reason `ScalarPreservation.lean` found one level down: the principle inspects `Δ` only through `laplacianFun_nonneg_of_isLocalMin`, which holds for **each metric separately**, so the abstract `le_of_deriv_ge_at_min_time`/`mem_of_deriv_le_at_max_time` apply with the `letI RiemannianBundle` introduced *inside the proof*, one time at a time. Both compiled first try — the pattern is now routine. **Also `mem_of_laplacian_moving_set`** — **metric AND target set both moving**, the widest form here; the two generalisations compose with **no interaction**, the metric being inspected only through `laplacianFun_nonneg_of_isLocalMin` (per-`t`) and the set only through the first-touching-time comparison (per-monotonicity). **What this does NOT buy**: `u` takes values in a *fixed* inner product space — a **trivial bundle**. Hamilton's curvature operator is a section of `Sym²(Λ²TM)`. **The moving fibre metric is no longer the blocker** (`mem_of_deriv_le_at_max_set` handles it by the square-root reduction); **the non-trivial bundle is**, and it gates the Uhlenbeck route and the §9 route alike. **Narrowed 2026-09-16**: the *touching-point* half of that is now done (`BundleMaximumPrinciple.lean` — `⟪n, Δu x⟫ ≤ 0` at a spatial max, for `n` in a single fibre), so what the non-trivial bundle still costs is only the **cross-fibre comparison** of `dist(u(t,x), K_x)` that the first-touching-time argument makes between different fibres. Note `[T2Space M]` is **not** needed (the tensor version's touching-point step is `laplacianFun_nonneg_of_isLocalMin`, not a frame argument) |
| `NormalSection.lean` | **proved**: **a global `C^k` section through a prescribed `v ∈ T_xM` with `∇N(x) = 0`** (`exists_contMDiff_section_cov_eq_zero`, `_two_`) — the primitive a maximum principle on a **non-trivial bundle** needs in place of a constant direction vector. **Why**: the tensor principle quantifies over `n ∈ V` and speaks of `x ↦ ⟪k, u t x⟫`, which only parses because `n` is the same vector everywhere; on a real bundle `n ∈ V x₀` only, so `n` must be spread into a *section*, and `Δ⟪k(x), u(t,x)⟫` then picks up corrections in `∇n` and `Δn` that must vanish at `x₀`. **No parallel transport and NO exponential map** — the textbook radial-transport construction would put this behind `exp`'s regularity (an open ODE gap); only *first-order* agreement at one point is wanted and that is linear algebra. Subtract `∑ᵢ fᵢ·Wᵢ` from any extension, with `Wᵢ` global sections through an orthonormal basis of `T_xM` and `fᵢ x = 0`, `dfᵢ x` the matching component of `−∇Ñ(x)`; Leibniz leaves `∇(fᵢWᵢ)(x) = dfᵢ(x) ⊗ Wᵢ(x)`. **An ORTHONORMAL basis is what makes it cheap**: against a local frame the components of `A` need a basis inversion to be CLMs, against an orthonormal one they are `u ↦ ⟪bᵢ, A u⟫` = `innerSL ∘ A`, a CLM by construction — so no frame, no trivialisation, no `exists_frame_on_open`, just `exists_contMDiff_extension` at each `bᵢ`. Enabling step **`exists_contMDiff_fun_mvfderiv_eq`** (a global `C^k` function vanishing at `x` with *prescribed* differential): the chart's differential at the base point is **invertible** (`isInvertible_mfderiv_extChartAt`), so `φ ∘L L.inverse` solves for the model covector, and `exists_contMDiff_eventuallyEq_fun` globalises without touching the germ. Also **`mvfderiv_clm_comp`** (chain rule through a CLM; mathlib has the `mfderiv` versions but not this shape — the `HasMFDerivAt.comp` route avoids unfolding `fromTangentSpace` by hand, upstream candidate). **The second-order bricks are proved too** — `hessian_smul_of_vanishing`: at a point where `f` AND `df` vanish, `∇²(f·W) = (∇²f)·W`, because Leibniz's three corrections each carry a factor `f x` or `df x`; **and by the same token `∇(f·W)(x) = 0`, which is what lets a second-order correction be added WITHOUT disturbing the first-order condition already arranged**. `hessianFun_half_sq`: `∇²(½a·g²)(x) = a·dg⊗dg` at a zero of `g` — a half-square is the cheapest function with value `0`, differential `0` and a *prescribed* second derivative (every term of `d(g²) = 2g·dg` carries a factor `g`), and its second derivative is rank one, so tracing with `dg = ⟪bᵢ,·⟫` gives `Δ = a` from `∑ⱼ⟪bᵢ,bⱼ⟫² = 1`. **No chart-side second-derivative transport anywhere** — that is what keeps this step the same size as the first-order one; the radial-normal-coordinates route would have needed it. **AND THE FULL STATEMENT IS ASSEMBLED** (`exists_contMDiff_section_normal`): for every `v ∈ T_xM` a global `C²` `N` with `N x = v`, `∇N(x) = 0` **and** `ΔN(x) = 0`. The two stages do not interact — take `N₁` from the first-order statement, then add `∑ᵢ (aᵢ/2)·gᵢ²·Wᵢ` with `dgᵢ(x) = ⟪bᵢ,·⟫` and `aᵢ = −⟪bᵢ, ΔN₁(x)⟫`; each summand vanishes to *second* order at `x`, so it disturbs neither the value nor `∇N₁(x) = 0`, while contributing `aᵢ·⟪bᵢ,·⟫^{⊗2}·bᵢ` to the Hessian, and tracing collapses `∑ⱼ⟪bᵢ,bⱼ⟫²` to `1`. **The frame is the SAME family `Wᵢ` used for the correction**, globalised by `exists_contMDiff_extension` rather than taken as `FiberBundle.extend` — the latter is regular only *near* `x`, while `contMDiff_cov_apply`, which supplies every differentiability hypothesis in the proof, is a **global** statement; reusing the correction's own sections as the frame is what makes that free, and `laplacian_eq_sum_frame` accepts any frame orthonormal at `x`. Supporting: `cov_sum_section_apply` (`∇` over a finite sum — free from `cov_sum_smul_section_apply` at unit coefficients, `mvfderiv_const` killing the Leibniz terms), `hessian_add_section`, `hessian_sum_section`, `mvfderiv_half_sq`. **In the additivity lemmas the `Y` slot is touched only at `x`, so its hypotheses are pointwise; the SECTIONS need `∀ y`**, because `y ↦ ∇_Y Z(y)` must be split as a *function* before `∇` is applied to it. Gotchas: `MDifferentiableAt.mul` produces a **Pi**-mul, so `mvfderiv_fun_mul` picks the wrong higher-order unifier and reports "did not find the pattern" on a goal that prints right — ascribe `have : MDifferentiableAt … (fun z ↦ g z * g z) y := (hg y).mul (hg y)` first (same family as the documented `MDifferentiableAt.add` trap); `IsCovariantDerivativeOn.add`'s `hx : x ∈ s` auto-param cannot fire when `s` is a metavariable — pass `(s := Set.univ)` and `(Set.mem_univ x)` explicitly; `abel` fails on a goal whose RHS still has `hessian` **folded**, so `simp only […, hessian]` first; and the Leibniz split of `cov (f•W)` must hold for **all** `y`, not just at `x`, so the hypotheses are `∀ y, MDifferentiableAt …` rather than pointwise — a germ-locality dance is avoidable when the fields are globally `C^k` anyway. Gotchas: `ContinuousLinearMap.IsInvertible.inverse` is the *theorem* that the inverse is invertible — the inverse **map** is `L.inverse`; `ContMDiffAt.mdifferentiableAt` wants `n ≠ 0` so the level hypothesis is `(hn : n ≠ 0)`, not `1 ≤ n`; `Filter.EventuallyEq.mvfderiv_eq` lives in `TraceCov.lean`, and without that import the error is a misleading "invalid field notation" on a `Filter.Eventually`; and `hessian` lives in the **root** `CovariantDerivative` namespace so `NormalSection.lean` must import `Hessian.lean` explicitly (the error is "environment does not contain `CovariantDerivative.hessian`", which reads like a namespace problem and is an import one); and the unused-section-variable linter **wrongly** flags `[CompleteSpace E]` here — it is needed for `stdOrthonormalBasis` to elaborate, but the `omit` is still required or CI fails |
| `ScalarPreservation.lean` | **proved**: **a lower bound on the scalar curvature is preserved by the Ricci flow** (`scalarCurvatureOfMetricAt_ge_of_hasDerivAt`, and `_of_hasDerivAt_scalarFlowRHS` taking the evolution equation as its single hypothesis) — **the first inequality of Hamilton's pinching set `K`, on the manifold**. **It needs no Uhlenbeck trick, and isolating it is what shows where the trick is actually forced**: the tensor principle is applied to a section of a bundle whose *fibre metric* moves with `t`, which is what has to be trivialised; the scalar equation has no fibre, so the only thing moving is `Δ` — and the maximum principle never looks at `Δ` except to ask `Δu ≥ 0` at a spatial minimum, which `laplacianFun_nonneg_of_isLocalMin` gives for **each metric separately**. So `le_of_deriv_ge_at_min` (metric-free) is applied directly and the `letI` lives only inside the proof, one time at a time — **do not reach for `le_of_laplacian`, which fixes one `cov` for all `t` and cannot state this**. The reaction term is nonnegative by `metricTraceE_comp_sharpE_self_nonneg` (in `MetricTrace.lean`): `⟨B,B⟩_g` is a sum of squares over any `g`-orthonormal basis once `B` is symmetric, and `ricciFormOfMetric_symm` supplies that from `RicciSymm.lean`. So `F = 0` and the comparison function is the constant. **Non-vacuity is checked, not asserted**: `hasDerivAt_scalarFlowRHS_of_hasDerivAt_scalarCurvatureAt` proves `scalarFlowRHS` defeq to `ScalarFlow.lean`'s conclusion **by `:= h`** — the two differ only by the `letI`, and a bridge proved by `:= h` is the cheapest way to make such a claim a checked one |
| `ScalarLowerBound.lean` | **proved**: **Hamilton's lower bound on the scalar curvature** — `R(0,·) ≥ C` with `C < 0` gives `R(t,·) ≥ C/(1 − (2/n)Ct)` (`scalarCurvatureOfMetricAt_ge_hamiltonComparison`, and `_of_scalarFlowRHS` taking `ScalarFlow.lean`'s conclusion directly). `ScalarPreservation.lean` threw the reaction term away; keeping it is the whole difference. `sq_scalarCurvatureOfMetricAt_le` is `R² ≤ n|Ric|²` (Cauchy–Schwarz at `B = Ric`), so `2|Ric|² ≥ (2/n)R²` and the comparison ODE is `φ' = (2/n)φ²`. **The obstruction was that `r ↦ (2/n)r²` is not Lipschitz** while the maximum principle wants a Lipschitz reaction; `reactionTrunc n A r = (2/n)(max (−A) (min r A))²` fixes it, and **no bound on `R` is assumed** — clamping only moves `|r|` down (`abs_clamp_le_abs`), so `reactionTrunc ≤ (2/n)r²` at *every* `r` and the hypothesis at spatial minima survives untouched, while along the comparison solution (which never leaves `[C,0]`, `hamiltonComparison_mem`) the truncation at level `−C` is invisible. **Do not truncate at a bound for `R` read off compactness — that was the first plan and it is unnecessary.** Gotchas: `Module.finrank ℝ (TangentSpace I x) = Module.finrank ℝ E` **is `rfl`**, which is what lets `stdOrthonormalBasis`'s `Fin`-index be traded for `finrank ℝ E`; `abs_add` no longer resolves — get `|a+b| ≤ 2A` from `rw [abs_le] at … ⊢` plus `linarith`; `div_nonpos_iff` splits as `0 ≤ a ∧ b ≤ 0 ∨ a ≤ 0 ∧ 0 ≤ b` (the second disjunct is the one for `C/D` with `C<0<D`); and `Real.coe_toNNReal` needs the `ℝ≥0` ascription dropped unless `open scoped NNReal` |
| `LinearODE.lean` | **proved**: **global existence and uniqueness for a linear ODE** `ẋ = A(t)x` on all of `ℝ`, with no smallness hypothesis and no continuation argument. The Picard–Dyson iterates for `Φ' = A(t)Φ`, `Φ(0) = 1` — `dysonIter`, `continuous_dysonIter`, **`norm_dysonIter_le`** (`‖Iₙ(t)‖ ≤ (Ct)ⁿ/n!`). This is the ODE **Uhlenbeck's trick** needs (`∂ₜι = Ric∘ι`, linear in `ι`). **Why not just use mathlib's Picard–Lindelöf**: `IsPicardLindelof.exists_eq_forall_mem_Icc_eq_picard` does give the whole interval, but is gated on the field `mul_max_le : L * max (tmax−t₀) (t₀−tmin) ≤ a − r`, and for a *linear* field the best `L` is `C(‖x₀‖+a)`, so on `[0,T]` with `r = 0` it reads `a(1−CT) ≥ C‖x₀‖T` — i.e. **`C·T < 1`**. Past that you need continuation, which mathlib lacks. **The factorial is what removes the smallness hypothesis** and so bypasses continuation entirely: it beats the power at every `t`, where the crude bound `(Ct)ⁿ` (estimating the integrand by its endpoint value) dies at exactly `C·t = 1` — the same regime `mul_max_le` dies in. **Do not reach for the crude bound: the induction does not close with it.** Gotchas: `intervalIntegral.continuous_primitive` needs `(μ := MeasureTheory.volume)` given explicitly or the measure stays a metavariable and `IsLocallyFiniteMeasure` is stuck; `F →L[ℝ] F` carries **two defeq-but-not-syntactic topologies** (`PseudoMetricSpace.toUniformSpace.toTopologicalSpace` vs `ContinuousLinearMap.topologicalSpace`), so `simpa` normalises one side and fails — state the function equality as a `rfl` `have`, `rw` it, then `exact`; `Nat.factorial_succ` leaves `↑(n*n! + n!)`, so **`push_cast` before `ring`**. **`dysonSum`** (`Φ = ∑ₙ Iₙ`) with `summable_dysonIter`, `dysonSum_zero`, **`hasDerivAt_dysonSum`** (`Φ' = A(t)∘Φ`, at every `t`) and `norm_dysonSum_le` (`‖Φ(t)‖ ≤ e^{C|t|}`). **The series is differentiated termwise, not swapped with the integral**: `hasDerivAt_tsum_of_isPreconnected` wants a summable bound on the *derivatives* uniform on an open preconnected set, and `C(CR)ⁿ/n!` on `(-R,R)` is one; swapping `∑` with `∫₀ᵗ` would have meant dominated convergence and `ENNReal` bookkeeping for no gain. **The index must be shifted before applying it** — the derivative of `Iₙ₊₁` is `A∘Iₙ`, so summing over `Iₙ₊₁` is what makes the bound's index match the term's, and `I₀ = 1` contributes nothing; reassemble with `Summable.sum_add_tsum_nat_add` (the additive name — `tsum_eq_zero_add` does not exist for general groups). **Uniqueness, by contrast, is mathlib's verbatim**: a linear field is globally Lipschitz with a constant uniform in `t`, so `ODE_solution_unique_univ` applies with no work (`eq_of_hasDerivAt_linear`), and `eq_dysonSum_apply` says every global solution is `Φ(t)x(0)` — **it is only *existence* on a long interval that mathlib cannot supply**. Gotchas: `rw [hsplit, ← hval]` rewrites `dysonSum` inside the *derivative value* too and then the second rewrite misses — do `← hval` **first**; `ContinuousLinearMap.lipschitz` is deprecated (→ `lipschitzWith`); and `Real.coe_toNNReal C hC0` plus `coe_nnnorm` is the way to `‖A t‖₊ ≤ C.toNNReal`. **`Φ(t)` is invertible** (`bijective_dysonSum`, `dysonEquiv`), and **the mirror right-multiplication series is NOT needed** — an earlier note here said it was. Both halves are uniqueness: *injective* is **backward uniqueness** (`s ↦ Φ(s)v` and the zero solution agree at `t`, hence everywhere), which is why `eq_of_hasDerivAt_linear` is stated at an **arbitrary base time** `t₀`; *surjective* uses `dysonFrom` (`U(t,t₀)`, the Dyson sum of the *time-shifted* family `A(·+t₀)`), whose solution through `w` at time `t` is a global solution and so is `Φ(·)` of its own value at `0`. Then `ContinuousLinearEquiv.ofBijective` (open mapping). **This is what Uhlenbeck's trivialising family needs — a bundle *isomorphism*, not just a bundle map.** Gotcha: `HasDerivAt.scomp` takes `x` explicitly and elaborates `hg` first, so the chain rule for `s ↦ Φ̃(s−t₀)` will not unify — use **`HasDerivAt.comp_sub_const`** (`Mathlib/Analysis/Calculus/Deriv/Shift.lean`) instead. **Dependence on a parameter is proved, at `C¹`** — `section Parameter`. `dysonIterDeriv` (`J₀ = 0`, `Jₙ₊₁ = ∫₀ᵗ A∘Jₙ + (A'·)∘Iₙ`), `norm_dysonIterDeriv_le` (**`‖Jₙ₊₁(x,t)‖ ≤ C'|t|(C|t|)ⁿ/n!`** — the factorial bound with one power of `|t|` traded in, the Leibniz term contributing `A'` exactly once, summing to `C'|t|e^{C|t|}`; the induction closes with **equality**, `1/(n+2)! + 1/((n+2)n!) = 1/(n+1)!`), `hasFDerivAt_dysonIter` and **`hasFDerivAt_dysonSum`** (`D_xΦ = ∑ₙ Jₙ`), `differentiable_dysonSum`. **Mathlib has this in no form.** **Two design points carry it**: the bounds `hC`/`hC'` are *global in `x`*, so the dominating function for `hasFDerivAt_integral_of_dominated_of_fderiv_le` is uniform and the parameter set is all of `H` — no neighbourhood bookkeeping anywhere; and **`dysonIterDeriv` is written with its two terms in *mathlib's* order, not the natural one**, because `HasFDerivAt.clm_comp` states the product rule for `y ↦ (c y).comp (d y)` as `compL (c x) ∘ d' + (compL.flip (d x)) ∘ c'` — matching it makes the differentiation step that lemma **verbatim** instead of an `add_comm` at every induction step. Getting the order wrong costs a rewrite of the whole section, so check `clm_comp` *before* defining the recursion. `norm_integral_le_of_norm_le_pow` was extracted from `norm_dysonIter_le`'s proof (the two-sided `‖∫₀ᵗ g‖ ≤ K|t|ⁿ⁺¹/(n+1)` for `‖g s‖ ≤ K|s|ⁿ`) — it is **the only place the sign of `t` is ever split**, and both factorial bounds are one line on top of it; the now-subsumed `norm_dysonIter_le_of_nonneg` was deleted. `norm_compL_comp_le`/`_flip_comp_le` bound the two integrand pieces from `opNorm_le_bound` directly, avoiding any need for `compL`'s own operator norm. **The variational equation is proved** (`hasDerivAt_dysonDerivSum`): `∂ₜ(D_xΦ) = (A'·)∘Φ + A∘(D_xΦ)` — `∂ₜ` and `D_x` commute for this equation, proved rather than assumed, by the same termwise argument one level up (`dysonDerivSum`, `summable_dysonIterDeriv`, `dysonDerivBound_le_of_abs_le` for the uniform bound on `(-R,R)`). **Left**: `C^k` for `k ≥ 2`. **The route is now a checked claim, not an aspiration** — the variational equation is exactly what says `(Φ, D_xΦ)` solves the *linear* block-triangular system: `(Φ, D_xΦ)` satisfies the *linear* block-triangular system `∂ₜ(Φ,Ψ) = (AΦ, A'Φ + AΨ)`, so the `C¹` theorem applies to the augmented system and `k` follows by induction — do not reach for a second-order differentiation-under-the-integral argument. **Gotcha, hit twice**: `rw [hsplit, ← hval]` rewrites the sum inside the *derivative value* too and the second rewrite then misses — always `← hval` **first**. And `tsum_add` is now `Summable.tsum_add`; get the summability of a CLM image from `(L.hasSum h.hasSum).summable`, stating it as a `have` at the goal's exact shape so the `rw` matches |
| `LinearODEAugmented.lean` | **proved**: **`Φ` is `C¹` in the parameter, and `D_xΦ` is itself differentiable** — the `k = 2` case `LinearODE.lean`'s own docstring named as next, and the first thing done on this line in 41 commits. **The whole content is ONE identification.** `augField a a' (u,v) = (a∘u, a∘v + (·∘u)∘a')` on `G = (F→LF) × (H→L(F→LF))`, and **`dysonSum_augOp_apply`**: `Φ_aug(x,t)(1,0) = (Φ(x,t), D_xΦ(x,t))` — both sides solve `ẇ = augOp(x,t)w`, the right one by `hasDerivAt_dysonSum` and `hasDerivAt_dysonDerivSum` **paired**, whose two values are *literally* the two components of `augField` (so the match is `rfl` and `HasDerivAt.prodMk` is the entire step), and both are `(1,0)` at `t = 0`; `eq_of_hasDerivAt_linear` does the rest. **So `D_xΦ` is a Dyson sum IN ITS OWN RIGHT**, read through the fixed CLM `augRead` (evaluate at `(1,0)`, take `.2`), and every theorem `LinearODE.lean` proves about Dyson sums now applies to it. **The design point is `augCLM`**: `augOp` is a continuous **linear** function of the pair `(A,A')`, so `hasFDerivAt_augOp` is the chain rule through a CLM — one line — and the `C¹` theorem applies to the augmented system **verbatim**, with no second-order differentiation under the integral sign anywhere. Linearity and continuity in `(u,v)` are **by construction** (`compL`/`prod`/`fst`/`snd` — the `christoffelB` lesson: no `mk₂`, no boundedness estimate), and the one estimate, `norm_augField_le` (`≤ ‖a‖+‖a'‖`), reuses `LinearODE.lean`'s own `norm_compL_comp_le`/`norm_compL_flip_comp_le` **unchanged** — the two summands of the second component are exactly the two shapes the iterate recursion already estimates, which is not a coincidence but the same Leibniz rule. **`contDiff_one_dysonSum` is FREE from the second derivative**: `C¹` asks for `D_xΦ` continuous, and a differentiable map is continuous — the first statement in the repo putting the fundamental solution in a `ContDiff` class at all. **The induction to general `k` is `LinearODESmooth.lean`**, which iterates exactly this construction; the one obstruction is a LEAN one, not a mathematical one — the induction replaces `F` by `G` at each step, and `G` lives in universe `max u_F u_H`, so `Nat.rec` would have to produce statements in a *growing* universe. Constraining `F H : Type u` fixes it (`max u u = u`) and costs nothing at the application site. **And note what this does NOT close**: Uhlenbeck's trick needs the parameter to range over a **manifold**, not a normed space, so chart and bundle transfer — with globally-bounded extensions off each chart, since the bounds here are global in `x` — remain after the ODE theory is in place. Gotchas: `augCLM.hasFDerivAt.comp` leaves the CLM a **metavariable**, higher-order unification being unable to match `⇑?m ∘ f` against `fun y ↦ augCLM (f y)` — pass `(augCLM (F := F) (H := H))` and finish with `exact`; every `compL` needs its **third type argument written out**, a `_` there sends `isDefEq` through `augCLM`'s `mkContinuous` body and times out at 200k heartbeats; `map_smul'` leaves a `RingHom.id ℝ c` residue, so `RingHom.id_apply` must be in the `simp only` set; and there is **no `opNorm_prod`** — bound `‖p.prod q‖` with `opNorm_le_bound` + `prod_apply` + `Prod.norm_def` directly |
| `LinearODESmooth.lean` | **proved**: **`Φ(·,t)` is `C^k` in the parameter, for EVERY `k`** (`contDiff_dysonSum`) — the induction `LinearODEAugmented.lean` set up, carried out, and the close of the `C^k`-parameter-dependence gap that CLAUDE.md's gap list carried for months. **The hypothesis class is itself recursive and that is what makes the induction cheap**: `IsBddFamily (k+1) f` says `f` is continuous and bounded in `t` uniformly in the parameter **and** has a parameter derivative family that is an `IsBddFamily k`. **Two closure properties carry the whole file** — stability under **post-composition with a fixed CLM** (`IsBddFamily.comp_clm`) and under **pairing** (`IsBddFamily.prodMk`). **The first one's induction step is THE SAME LEMMA AT A DIFFERENT CLM**: the derivative family of `L ∘ f` is `(compL L) ∘ f'`, so nothing is differentiated by hand at any level; the second reduces to the first through **`prodCLM`**, the bundled pairing map built from `inl`/`inr` so linearity and continuity hold by construction (mathlib bundles `prod` only in shapes that do not fit). With those two, `isBddFamily_augOp` is one line — the augmented coefficient is `augCLM` applied to the pair `(A,A')`, and `augCLM (A x s, A' x s) = augOp A A' x s` is **`rfl`**. **The step**: `Φ` is `C^{k+1}` iff differentiable with `C^k` derivative (`contDiff_succ_iff_fderiv`); `D_xΦ` **is** the Dyson sum of the augmented system read through `augRead`; and that Dyson sum is `C^k` by the IH at `F := G`. **No new analysis at any level** — every derivative traces back to the single `C¹` theorem of `LinearODE.lean`. **The statement is universe-monomorphic (`F H : Type u`) and that is the ONLY reason**: `G` lives in `max u_F u_H`, so iterating in general universes would ask `Nat.rec` for statements in a *growing* universe. It costs nothing at the application site, where the spaces are concrete. **Two checks, because an uninstantiated general theorem asserts nothing**: `isBddFamily_const` (a parameter-independent family is an `IsBddFamily k` for every `k`, so the hypothesis class is non-empty at every level — without it the theorem could be quantifying over nothing at large `k`) and `contDiff_one_dysonSum_of_isBddFamily` (at `k = 1` it reproduces `LinearODEAugmented.lean`'s conclusion from the same data). **The class is an ALGEBRA and it is CHECKABLE** (added the same day): `IsBddFamily.add`, `IsBddFamily.bilin` (a bounded **bilinear** map — the Leibniz induction, the only product rule in the file, each summand of the derivative being this same statement one level down at `B.precompR`/`B.precompL`), `IsBddFamily.comp_op` (operator composition, the falsification instance), and the bridge to mathlib **`isBddFamily_of_contDiff`** (a `C^k` map with bounded iterated derivatives is a bounded `C^k` family, constant in `t`) with **`isBddFamily_of_hasCompactSupport`** on top — so bump functions are available. **`contDiffOn_dysonSum`** localizes the conclusion in five lines, `Φ` depending on the parameter only through `A x`. **A SCALAR family is NOT expressible at a general universe and this is worth not rediscovering**: the recursion is `W ↦ (H →L W)`, stable only when `W` and `H` share a universe, and `ℝ : Type 0` — so `IsBddFamily k (χ : H → ℝ → ℝ)` does not typecheck. Nothing is lost: a bump multiplication is `fun x s ↦ (χ x • 1) (A x s)`, whose left factor is *operator*-valued and stays in `Type u`. **What it does NOT close**: Uhlenbeck's trick needs the parameter to range over a **manifold**, and the bounds here are global in `x`, so chart-and-bundle transfer with globally-bounded extensions off each chart remains — at every `k`. The ODE-theoretic half is closed; the geometric half is not. Gotchas: **dot notation resolves on the type's head symbol as WRITTEN**, so `hf.comp_clm` on `hf : IsBddFamily 0 f` picks `IsBddFamily.comp_clm` (the theorem being defined) rather than `IsBddCts.comp_clm`, and the error is a `ℕ`-vs-CLM application mismatch — write `IsBddCts.comp_clm L hf`; `induction ... generalizing` cannot generalise instance binders, so state the theorem as `∀ (k : ℕ) {W : Type u} [..], …`, `intro k` **alone**, then `induction k`, leaving everything else in the goal; and `omit … in` goes **before** the docstring (the documented trap, hit again) |
| `ParallelFrame.lean` | **proved**: **a parallel orthonormal frame along a curve** — transport an orthonormal basis of one fibre along `γ` and it is orthonormal in **every** fibre (`exists_parallel_orthonormal_frame_along`), hence an orthonormal basis of each `T_{γu}M` (`exists_parallel_orthonormalBasis_along`); and the transport isometry of `ParallelTransportGlobal.lean` **is** the map carrying the one basis to the other (`exists_parallelTransportIsometry_frame`). **NOTHING NEW IS TRANSPORTED**: the frame is `exists_isParallelAlong_global` applied once per basis vector, and orthonormality is `TransportIsometry.lean`'s constancy of the pairing, which carries no frame hypotheses and so was global already. The one adjustment is `inner_eq_of_isParallelAlong_global` — the constancy is stated there on a **closed interval based at its LEFT endpoint** and is wanted on a preconnected open set based at an **arbitrary** time; in `ℝ` preconnected is order-connected (`isPreconnected_iff_ordConnected`), so the closed interval between the two times lies in `s` and it is two cases, not a new argument. **The *same* constancy serves the diagonal and the off-diagonal pairings**, so orthonormality is preserved as one condition (`orthonormal_iff_ite`) rather than a norm statement and an angle statement proved separately. **Why it is the next brick on the cross-fibre maximum principle**: the touching-point half is done (`BundleMaximumPrinciple.lean`) and what remains compares `dist(u(t,x), K_x)` between *different* fibres — a parallel orthonormal frame makes that a statement about a **fixed** inner product space, trivialising `TM` isometrically along the curve, hence `End(TM)` isometrically for the HS metric, with `IveyParallel.lean` carrying the pinching set across; so along each curve the comparison reduces to the trivial-bundle case `TensorMaximumPrinciple.lean` already handles. `inner_eq_of_isParallelAlong_frame` is the sense in which the trivialisation is isometric — **in frame coordinates a parallel section is a fixed vector**. **The purpose is CHECKED, not asserted**: `exists_parallelTransport_frame_mem_iveyEndoSet_iff` (in `IveyParallel.lean`) joins the frame to `mem_iveyEndoSet_conj_iff`, so the isometry that carries the pinching set is identified as the frame change. **What it does NOT do**: it does not build transport on `End(TM)` as a bundle in its own right, and it does not carry out the first-touching-time argument; it supplies the frame those need. Gotchas: `orthonormal_iff_ite` wants `DecidableEq ι` — `classical` first; `Module.Basis.card_eq_finrank` does not exist, use `(Module.finrank_eq_card_basis b.toBasis).symm` and let `finrank ℝ (T_{γt₀}M) = finrank ℝ (T_{γu}M)` close by defeq (both are `finrank ℝ E`); `basisOfOrthonormalOfCardEqFinrank` needs `[Nonempty ι]`; the coe lemma is **`Module.Basis.coe_toOrthonormalBasis`**, not `Basis.coe_…`; and `omit` needs the binder spelled `fun (x : M) ↦ TangentSpace I x` or instance search stalls on `TopologicalSpace (TotalSpace …)` |
| `EndTransport.lean` | **proved**: **parallel transport on `End(TM)`** (`exists_isParallelAlongSection_end`) — for every `A₀ ∈ End(T_{γt₀}M)` a field along `γ`, differentiable along it and **parallel for `homCov cov cov`**, with `A t₀ = A₀`. `ParallelFrame.lean` transports a basis; this transports an *operator*, which is what the cross-fibre comparison actually tests against. **An ORTHONORMAL frame is what makes it cheap**: against a general transported frame the coordinates of an endomorphism need the inverse of the frame matrix, which then has to be differentiated; against an orthonormal one they are `mᵢⱼ = ⟪bᵢ, A₀(bⱼ)⟫` and `endoAlong u = ∑ᵢⱼ mᵢⱼ ⟪frⱼ u,·⟫ frᵢ u` — **no inversion anywhere**. **THE INDEX ORDER IS LOAD-BEARING**: written `⟪A₀(bⱼ), bᵢ⟫`, the outer sum is `∑ᵢ ⟪A₀ v, bᵢ⟫ • bᵢ`, equal over `ℝ` to `OrthonormalBasis.sum_repr'` but **not syntactically**, and unification then descends into the metric's construction — a `whnf` timeout at 200k heartbeats, corrected belief 5 one type former down. With `⟪bᵢ, A₀(bⱼ)⟫` the expansion matches `sum_repr'` on the nose and the file compiles in seconds. **Parallelism costs NO transport computation**: `endoAlong_apply_frame` says the value on `frₖ` is a *constant* combination of the frame (orthonormality collapses the inner sum), hence parallel by `isParallelAlong_sum_smul`; `covAlongSection_hom_apply_eq_zero` then gives `(D/dt A)(frₖ u) = 0` for every `k`, and the frame is a **basis of the fibre** — which is exactly the "family of parallel sections spanning each fibre" `HomBundleAlongCurve.lean` names as the missing half of its own converse, **supplied by the metric**. **The one genuinely new brick is `mdiffAlongSectionAt_hom_of_symmL`**: `HomBundleSmooth.lean`'s criterion (a `Hom(W₁,W₂)` section is `C^n` once its values on the source trivialisation's local frame are) transposed to differentiability **along a curve**, and it transposes because the `inCoordinates` representative is literally the *target* trivialisation's coordinate of `u ↦ A u (e₁.symmL (γ u) w)` — no coordinate change survives. Supporting: `differentiableAt_clm_of_basis` (a CLM-valued curve is differentiable once its values on a basis are, via `smulRightL`), `mdiffAlongAt_symmL` (**the inverse frame is differentiable along γ for a trivial reason** — in *that* trivialisation's own coordinate it is the constant `v`), `mdiffAlongAt_congr` (germ-locality, stated because `MDifferentiableAt.congr_of_eventuallyEq` wants the *total-space* equality while every use has it fibrewise), `inner_sum_smul_right_aux` (the `sum_inner`/`inner_sum`-on-`TangentSpace` remedy). **What it does NOT do**: the first-touching-time argument. It supplies the transport that argument compares fibres with |
| `TraceCov.lean` | **proved**: **the metric trace commutes with `∇`** (roadmap Next 1, the gate for every Laplacian identity). `mvfderiv_sum_eq_sum_covBilin`: for a metric connection, a bilinear form field `B` and a local orthonormal frame, `X(∑ᵢ B(eᵢ,eᵢ)) = ∑ᵢ (∇_X B)(eᵢ,eᵢ)`; `_of_frame` is the same with the hypotheses read off an `IsOrthonormalFrameOn`. `B` need not be symmetric and the frame need not be parallel: `inner_cov_antisymm` (metric compatibility + locally constant `⟪eᵢ,eⱼ⟫` ⟹ the coefficients `aᵢⱼ = ⟪∇_X eᵢ, eⱼ⟫` are antisymmetric) and `sum_bilin_of_antisymm` (antisymmetric against symmetric is `0`, by `Finset.sum_comm`). `exists_orthonormalBasis_of_isOrthonormalFrameOn` turns a frame into an `OrthonormalBasis` of each fibre. Two Mathlib gaps filled on the way: `Filter.EventuallyEq.mvfderiv_eq` and `mvfderiv_fun_sum`/`mdifferentiableAt_fun_sum` |
| `RicciVariation.lean` | **proved**: `CommutesWithCov` (∂ₜ/∇_X commute on the difference-tensor sections, all fields), `curvatureEndoE` (`v ↦ R(v,X)Y` via `mkHom`, typed on `E` by ascription — an expected type `E →L E` on a bare `mkHom` leaves `?V x =?= E` unsolved), `hasDerivAt_ricciOfMetric` (**∂ₜ Ric = tr ∂ₜ[v ↦ R(v,X)Y]**, any manifold). **`section FlowMetric` — the whole scalar line on a general `M`** (was model-space-only): `ricciFormOfMetric` (`ricciForm` for `leviCivitaConnection`, on global `C²` extensions rather than `constField`) and `ricciFormOfMetric_apply_field`; `innerE_deriv_eq_of_isRicciFlowAt'` (`∂ₜg = −2 Ric`); `scalarCurvatureOfMetricAt` + `scalarCurvatureOfMetricAt_eq_metricTraceE` (`scal = tr_g Ric`); `hasDerivAt_ricciFormOfMetric_apply`/`derivRicciFormOfMetric`/`hasDerivAt_ricciFormOfMetric` (∂ₜ of the Ricci form, coordinatewise then as a CLM via `exists_hasDerivAt_clm₂_of_apply`); `hasDerivAt_scalarCurvatureOfMetricAt` (`∂ₜ R = tr_g Ṙic − ⟨h,Ric⟩`) and `hasDerivAt_scalarCurvatureOfMetricAt_of_isRicciFlowAt` (**∂ₜ R = tr_g Ṙic + 2\|Ric\|²**). `section ModelSpace` keeps the same theorems where `Scalar.lean` first phrased them, needing no extension lemma: `constField`, `ricci_add_right_const`/`ricci_smul_right_const`, `ricciE`, `scalarCurvatureOfMetric'`, `hasDerivAt_scalarCurvatureOfMetric'`, `innerE_deriv_eq_of_isRicciFlowAt`, `hasDerivAt_scalarCurvatureOfMetric'_of_isRicciFlowAt`. Gotcha: after `ext v w` on an `E →L E →L ℝ` the variables are `E`-typed, so `exists_contMDiff_two_extension` needs `(x := x) (show TangentSpace I x from v)` or its `M` is a metavariable. Never `local notation` over a section variable: hygiene hides `E` and everything downstream is silently auto-bound |
| `Homogeneous.lean` | **branch closed**: `koszul`, torsion/compat, Levi-Civita uniqueness, `contDiffAt_ricciField`, `ricciFlow_leftInvariant` |
| `Milnor.lean` | **branch closed**: Koszul formula, Ricci in structure constants, diagonal Ricci `rᵢ = 2μⱼμₖ`, Heisenberg, Isenberg–Jackson ODE |
| `MilnorFrame.lean` | **proved**: **Milnor's Lemma 4.1** — every 3-dimensional *unimodular* metric Lie algebra admits a Milnor frame (`exists_milnorFrame`), which **discharges the `IsMilnorFrame` hypothesis `Milnor.lean` assumes** and closes that file's one soft spot. The crux is that **unimodularity is self-adjointness**: in dimension 3 the alternating bracket factors through the coordinate cross product as `[x,y] = L(x × y)` (`bracket_eq_crossL`, both sides bilinear and agreeing on basis pairs), and `IsUnimodular` — every `ad x` trace-free — says exactly that `L` is symmetric (`isSymmetric_crossL`), the trace of `ad eᵢ` being the difference of the two off-diagonal entries. The spectral theorem (`LinearMap.IsSymmetric.eigenvectorBasis`) then hands over the frame. `exists_orthonormalBasis_isMilnorFrame` is the abstract form on an inner product space; `IsInnerProduct.exists_orthonormal_basis` transports it to a metric carried as a value, and `exists_milnorFrame_ricci_diagonal`/`_scalar` cash it into `Milnor.lean`'s diagonal Ricci |

## Seven corrected beliefs — do not re-derive these wrong

0. **Levi-Civita uniqueness is NOT `∃!`.** A `CovariantDerivative` is
   unconstrained on sections not differentiable at the point (every law and
   predicate quantifies over differentiable sections), so two Levi-Civita
   connections can differ there. Mathlib's `IsLeviCivitaConnection.uniqueness`
   is pointwise on differentiable sections, and that is all curvature needs.

1. **`mkHom₃` was never needed and would not have worked.** The trace is in the
   first slot only, so one-slot `TensorialAt.mkHom` suffices. A `mkHom₃` copied
   from `mkHom₂` quantifies over merely-`MDiffAt` sections, whereas first-slot
   tensoriality of `R` needs the third-slot field to be `C²`.
2. **Ricci is NOT symmetric for a general torsion-free connection.** Tracing
   first Bianchi gives `Ric(X,Y) − Ric(Y,X) = −tr R(X,Y)`; that vanishes for a
   *metric* connection, where `R(X,Y)` is skew-adjoint. Symmetry is the
   corollary, not the theorem — and it is now proved unconditionally on a
   general manifold (`RicciSymm.lean`), both hypotheses of
   `ricci_sub_ricci_swap` having become theorems.
3. **The tangent-bundle "diamond" is not a diamond.** The fibre instances are
   definitionally equal (`rfl` succeeds for `AddCommGroup` and
   `TopologicalSpace`; `#synth` picks `instAddCommGroupTangentSpace`). What
   fails is elaboration over defeq terms — see below.
4. **A general-bundle theorem DOES specialise to the tangent bundle — provided it
   never binds a fibre NORM.** An earlier version of this file recorded the
   opposite as unavoidable; it was wrong, and the fix is a one-line change to
   the binders, not a workaround. The real obstruction is narrow and worth
   stating exactly: `TangentSpace I x` reduces to `E`, so `[∀ x,
   NormedAddCommGroup (V x)]` at `V := TangentSpace I` is matched by `E`'s own
   norm under the typing rules while instance search returns the Riemannian
   metric's, and **those are genuinely different norms on one type** — mathlib's
   scoped priority-80 `RiemannianBundle` instances exist precisely to keep them
   apart (lean4#14949). So:
   - state the norm-free layer (`∇²`, `Δ`, tensoriality) with mathlib's standard
     general-bundle binders — `AddCommGroup`, `Module`, `TopologicalSpace`,
     `IsTopologicalAddGroup`, `ContinuousSMul` — and the bridge is `rfl`
     (`BundleHessian.lean`);
   - state the metric layer against **`[RiemannianBundle V]`**, not plain norm
     binders, and the bridge still works because at `V = TM` that *is* the
     instance the tangent-bundle files use (`BundleBochner.lean`,
     `BundleNormalSection.lean`). Mathlib's advice against `RiemannianBundle`
     for general bundles is about bundles with no preexisting fibre topology;
     ours have one, so it does not apply.
   Two enabling facts: `OrthonormalBasis.sum_apply_self_eq` needs **no norm in
   its codomain** (it is an algebraic rearrangement of a finite sum, nothing is
   estimated), so the trace defining `Δ` survives; and `CovariantDerivative I F
   V` already demands `IsTopologicalAddGroup`/`ContinuousSMul` on the fibres, so
   those binders cost nothing.
   **Why this matters beyond deduplication**: a general theorem with no
   instantiations is unfalsified. Deriving `Bochner.lean` and
   `NormalSection.lean` from the bundle versions — with
   `example : @f = @g := rfl` forcing the two *types* to agree — is what checks
   that the general statements say what they are meant to say.

5. **The `Λ²` elaboration wall is the METRIC, not the nested codomain and not `Λ²`.**
   `CurvatureLambda.lean` recorded the opposite for a week; it was a guess from one
   failure, and three checks refute it. A variable of type
   `⋀[ℝ]^2 E →ₗ[ℝ] (⋀[ℝ]^2 E →ₗ[ℝ] ℝ)` applies to two wedges by `rfl` under this
   repo's full instance pile; mathlib's own `LinearMap.BilinForm.exteriorPower`
   has that type and a `simp` lemma applying it to two `ιMulti`s; and a map whose
   codomain is a plain `AlternatingMap` — not nested at all — fails the same way.
   The abstract second factorisation (`sndForm`) defines, applies and is
   characterised. **`set_option diagnostics true` is what settles it**: on the
   smallest failing application, `isDefEq` unfolds `RiemannianBundle.g` and
   `RiemannianMetric.inner` ~202k times each, via `innerSL` and
   `mkContinuousOfExistsBound`. `curvatureTensorAt a b c d` *is* `⟪Rm(a,b)c,d⟫`,
   so any term built from it drags the metric's construction into every
   definitional comparison. **Reach for `set_option diagnostics true` before
   theorising about a `whnf` timeout** — it took one build to answer what three
   redesigns could not. `irreducible` does not help (tried on the inner
   definitions, the outer definition, and `curvatureTensorAt`).

6. **`RiemannianBundle` CANNOT be used on `fun y ↦ T_yM →L[ℝ] T_yM`, and the
   priority-80 trick does not save it.** That fibre already carries the **operator
   norm** at default instance priority (`ContinuousLinearMap.toNormedAddCommGroup`),
   while the norm a `RiemannianBundle` induces is a *scoped* instance at priority
   `80`. Search for `InnerProductSpace ℝ (T_xM →L[ℝ] T_xM)` therefore fails
   outright: the operator-norm `NormedAddCommGroup` wins the first search and no
   `InnerProductSpace` matches it. This is not a wrong-instance hazard — it is a
   hard failure, and it is **corrected belief 4 one type former up**: a fibre norm
   really does break the bridge, and here the fibre has one whether or not you bind
   it. `TangentSpace` escapes only because it is a **non-reducible type synonym**
   with no competing head symbol; mathlib says so in as many words ("The definition
   of `TangentSpace` is not reducible so that type class inference does not pick
   wrong instances"). So the fix is the same synonym one level up — `EndTangent`
   in `EndBundleMetric.lean` — and it is cheap: nine `inferInstanceAs` transports,
   and the metric itself transports by `:= endMetric`, since `EndTangent I` and the
   raw bundle are equal by `delta` and `eta` and every transported instance is
   literally the raw one. **Keep the proofs on the raw bundle and only the
   statements on the synonym**, or every mathlib Hom-bundle `rw` stops matching.

## Stating anything where the metric VARIES

Two conditions are **jointly necessary** for `cov.IsMetricCompatible` to elaborate:

1. the `RiemannianBundle` instance must be an ambient **binder**
   (`variable [RiemannianBundle …]`), not introduced by `letI`/`haveI` in a term;
2. `cov` must be bound by an **existential** — not an explicit parameter, not a
   structure field, and not a parameter in *hypothesis* position.

So: elaborate the predicate ONCE in a section satisfying both, then only *apply*
that constant under `letI := ⟨g.toRiemannianMetric⟩`. See `Hamilton.lean`'s header
(`HasPositiveRicciLC` → `AdmitsPositiveRicciMetric`) and `Flow.lean`.
**Corollary:** converse lemmas must be iffs between existentials. Ten variants
were tested to establish this. It is a workaround, not a fix — open Zulip question.

## Roadmap (revised 2026-09-16)

**Statement audit against Chow–Liao–Qin**: `notes/hamilton-statement-comparison.md`
(nine divergences, classified). The two class-(c) hazards — vacuity of the test
class, and the junk branch in the sectional quotient — are **closed** (PRs #16–#19).
What remains are class-(b) genuine differences: connectedness absent, conclusion
stops at metric existence (no Killing–Hopf), `ω` vs `∞`, `C²` vs smooth, and
Ricci as a raw section rather than a tensor-bundle element.

The dated diary that used to sit here is condensed; the Lean-level lessons it
carried are now in "Lean gotchas". Chronology, for the record: mathlib bump and
Levi-Civita (09-04), its smoothness and the textbook flow equation (09-04), the
curvature ODE invariants and both abstract maximum principles (09-05/06), the
second-derivative test (model space, then manifold), the variations of the
connection, curvature, metric trace, Ricci and scalar curvature (09-05/06),
second Bianchi (09-05), the principles on a manifold and CI hardening (09-06).

**Scooped, 2026-08-21.** Chow–Liao–Qin, `arXiv:2608.21502`, "A Lean
Formalization of Hamilton's Three-Manifold Theorem". Repo
`github.com/qinz1yang/differential-geometry`, tag `arxiv-v1-preview`,
Apache-2.0, **1.9M lines of Lean, zero `sorry`, zero project axioms** (audited
locally 2026-09-06). Top-level `hamilton_positive_ricci` takes exactly
`isClosedThreeManifold` + `admitsPositiveRicci` and concludes
`admitsConstantPositiveSectionalCurvature ∧ isSphericalSpaceForm`; the
definitions are honest. They also have short-time existence (DeTurck +
spectral/Galerkin), forward uniqueness, maximal continuation, Shi, Uhlenbeck,
both maximum principles, F- and W-entropy with the cutoff argument, Perelman
κ-noncollapsing, and Cheeger–Gromov–Hamilton compactness. **Both analytic gaps
this project had flagged as blocking are closed in the literature.**

Do not race them on parabolic PDE. They have a 266:1 line advantage and Bennett
Chow. Race where their library is empty.

**What their repo does NOT contain** (grepped, 2026-09-06): `Real.log` anywhere
in `Preservation/` — so **no Hamilton–Ivey**; no reduced volume, no `L`-length,
no `L`-geodesics; no κ-*solutions* (their `kappa` is all noncollapsing) and no
canonical *neighborhoods* (their `canonical` is all Moser-iteration constants);
no surgery, no solitons, no finite extinction, no harmonic map flow, no
curve-shrinking flow, no cut locus, no Toponogov. They do have `Geometry/
Exponential`, `Comparison/Volume`, `Comparison/Variation`, Jacobi fields,
Bishop, index form, injectivity radius — the substrate `L`-geometry needs, and
which this repo has none of.

**Also found: the blueprint chart was missing Hamilton–Ivey entirely.** It is
the fifth correction to the chart and a load-bearing one: without it no blow-up
limit is ever known to have non-negative curvature, so `def:kappa-solution`'s
standing hypothesis is never verified for anything that actually arises.
Now `thm:hamilton-ivey` at the head of `chap:kappa`, with a
`State of the art, September 2026` section in the Overview.

**Next, in order.**
1. **Hamilton–Ivey** — **the ODE half is done** (`IsCurvatureODE.hamiltonIvey`,
   no auxiliary hypotheses: ordering + `ν(0) ≥ -1` ⟹ `R ≥ (-ν)(log(-ν) - 3)`
   wherever `ν < 0`). Source is Cao–Zhu Thm 2.4.1, whose LaTeX is on arXiv
   (`math/0612069`, grep `Hamilton-Ivey Curvature Pinching`).
   **The key simplification, which the literature does not state:** Cao–Zhu's
   two boundary cases are *the same inequality* — case (ii)'s hypothesis
   `λ = -μ + N(L-2)` is case (i)'s `λ+μ = N(L-2)` — and eliminating `L` leaves
   the same polynomial `iveyE = N(λ²+μ²) + N³ + λμ(λ+μ+N)`, nonnegative under
   the ordering alone (`iveyE_nonneg`; two sign cases, the `μ<0` one via
   `iveyE = (λ²+λμ+μ²)(N+μ) + N³ - μ³`). So the boundary argument becomes a
   linear Grönwall: with `Ψ = R - f(-ν)` and `N = -ν`,
   `Ψ' = iveyE/N - Ψ(N²+λμ)/N` identically (`field_simp; ring`, the `log N`
   terms cancel), so `nonneg_of_mul_le_deriv` applies directly
   (`iveyPsi_nonneg`). `hamiltonIvey_boundary_of_nonneg`/`_of_neg` are the
   literature's form and are now redundant; kept for the correspondence.
   `ν < 0` on all of `[0,t]` is free: `nonneg_preserved` (`ν̇ ≥ λν` under the
   ordering) plus `shift`/`restrict` (time translation of `IsCurvatureODE`)
   give `neg_of_neg`.
   **Left:** ~~(a) `f⁻¹` on `[-e²,∞)` and its concavity~~ — **closed, and it was
   never needed**: `IveyConvex.lean` shows the disjunction cutting out `K` is a
   single inequality against `iveyG n = f(max(-n, e²))`, which is convex because
   `f` is convex and increasing on `[e²,∞)` and `n ↦ max(-n,e²)` is convex and
   lands there. `K` is closed and convex (`convex_iveyPinchedSet`,
   `isClosed_iveyPinchedSet`), which is exactly what `thm:max-tensor` consumes.
   Invariance of `K` under the ODE is also packaged in that language
   (`IsCurvatureODE.isIveyPinched`, `IsCurvatureODE.mem_iveyPinchedSet`), so
   **both** things `thm:max-tensor` asks about `K` are proved.
   **Item 3 below is now closed**, so the equation the principle is applied to
   exists. **And the first of `K`'s inequalities is already proved on the
   manifold** (`ScalarPreservation.lean`), and **sharpened to Hamilton's
   `R ≥ C/(1 − (2/n)Ct)`** (`ScalarLowerBound.lean`): a lower bound on `R` is
   preserved and improves, with **no Uhlenbeck trick** — the scalar equation has no fibre, so a moving
   `Δ` costs nothing (see that file's row). What the trick is actually needed
   for is the *tensor* inequalities, where the bundle's fibre metric moves. (b) the time-dependent `K_t` form of
   `thm:max-tensor` is needed only for Hamilton 1999's `log(1+t)` improvement,
   not for the basic estimate; an earlier version of this list said otherwise.
   Watch out: `Real.log (-n) = Real.log n` is a simp lemma
   (`Real.log_neg_eq_log`), so `simpa [iveyF]` rewrites under you — use a
   `calc` with `rfl`, or `set L := Real.log (-n t)` before `field_simp`.
   **Scope audit, 2026-09-18 — the gate is NOT the `Λ²` wall, and it is NOT
   `exp`.** Carrying Hamilton–Ivey from the ODE to a flow on a closed 3-manifold
   needs a *section of a bundle* to run `thm:max-tensor` on. In dimension three
   the Weyl tensor vanishes, so Hamilton's curvature operator is
   `scal·Id − 2 Ric♯` — an **endomorphism field**, with eigenvalues exactly the
   `λ,μ,ν` of `IsCurvatureODE` (`λ = R − 2r₁` from `r₁ = (μ+ν)/2`; unit `S³`:
   `6 − 4 = 2 = 2·1`). So `Sym²(Λ²TM)` is not on the critical path and neither is
   `curvatureForm`; `End(TM)` is. **Convexity transfers, and that step is now DONE** (`IveyEndo.lean`): `trace` is linear and `iveyG` is *non*increasing composed with the concave `λ_min`, so the spectral set is convex for the same reason the triple set is. `λ_min` is defined as a Rayleigh infimum, not spectrally, so no eigenvalue continuity is needed; the bridge `mem_iveyEndoSet_iff_isIveyPinched` identifies the two sets on a diagonalised operator.
   **What WAS missing is a connection on a tensor bundle, and that is now built**
   (`HomBundle.lean`: `homCov`, `endCov`) — the repo's first `CovariantDerivative`
   on any bundle but `TM`, and the first instantiation the whole `Bundle*` layer
   has ever had. **The fibre metric is DONE** (`EndBundleMetric.lean`):
   `End(TM)` — as the type synonym `EndTangent`, which corrected belief 6 says is
   forced — is a `C^n` Riemannian bundle, **and `endCov` is metric for it**
   (`EndMetricCompat.lean`), so connection and metric sit on the same non-tangent
   bundle and agree. **`Rm₃ = scal·Id − 2Ric♯` exists pointwise**
   (`CurvatureOperatorThree.lean`), diagonal in a Ricci eigenbasis with
   `Pinching.lean`'s normalisation checked three ways. **Left on this line**, in
   order: **`Rm₃` IS a `C²` section** (`CurvatureOperatorSection.lean`), of
   `End(TM)` and of `EndTangent` alike, so what is left on it is its **evolution
   equation**, read off `CurvatureEvolution.lean` — **and the first brick of that
   is in** (`CurvatureTrace.lean`): the first-slot trace commutes with `∇²`, hence
   `∑ᵢ ⟪(Δ Rm)(eᵢ,X)Z, eᵢ⟫ = (Δ_g Ric)(X,Z)`, so tracing `∂ₜRm = ΔRm + Q` in its
   first slot turns the right-hand side into `Δ_g Ric` plus the trace of `Q`. What
   that still needed was the *left*-hand side and the trace of `Q`, and **both are now
   in** (`RicciEvolution.lean`): `∂ₜRic = Δ_g Ric + Q_Ric` with `Q_Ric` carrying no
   derivatives. **So `∂ₜRm₃` reduces to two bridges**, since `Rm₃ = scal·Id − 2Ric♯` and
   `∂ₜscal = Δscal + 2|Ric|²` is already proved: the sharp commuting with the Laplacian
   and `Δ_{End}(scal·Id) = (Δ scal)·Id` — **and the first is done**
   (`SharpLaplacian.lean`: `⟪(Δ A)v,w⟫ = (Δ_g h)(v,w)` whenever `⟪A v,w⟫ = h(v,w)`,
   instantiated at `Δ(Ric♯) = (Δ_g Ric)♯`). **But note what `∂ₜRm₃` still faces**: `Rm₃`
   depends on `g_t` through the index raising as well as through `Ric`, so the *time*
   derivative is where the moving fibre metric bites; the spatial half is what that file
   settles;
   the **cross-fibre** half of the maximum principle (`BundleMaximumPrinciple.lean`
   has the touching-point half, and **the touching-point half is now actually
   applied to `Rm₃`** — `CurvatureOperatorMaxPrinciple.lean`); and the moving
   fibre metric. **The transport half of the cross-fibre piece is now DONE**:
   a parallel orthonormal frame along a curve (`ParallelFrame.lean`), the
   pinching set carried by every fibre isometry hence by transport
   (`IveyParallel.lean`), and **parallel transport of an ENDOMORPHISM**
   (`EndTransport.lean`, `exists_isParallelAlongSection_end`) — so the tested
   direction can be moved between fibres with its HS norm and its spectrum
   intact. What is left of the cross-fibre half is the **first-touching-time
   argument on a bundle** itself: `TensorMaximumPrinciple.lean` compares
   `infDist (u t x) K` across times in one fixed space, and the bundle version
   must compare it across *fibres*. Note also the design mismatch to resolve
   there: `BundleMaximumPrinciple.lean`'s touching-point lemma is proved for a
   **normal** extension (`∇N(x₀) = 0`, `ΔN(x₀) = 0`), while Hamilton's
   cross-fibre argument turns a maximum of the distance into a maximum of
   `⟪N,u⟫` using a **parallel** extension. **`homCov` is now known `C¹`** (`HomBundleSmooth.lean`), which
   the `∇²`/`Δ` layer needs and which nothing had proved; it was found by
   attempting the composition rather than by inspection.
2. ~~**The manifold trace lemma** `X(tr_g B) = tr_g(∇_X B)`~~ — **done**,
   `TraceCov.lean`, exactly along the sketch that used to sit here (no parallel
   frame; antisymmetric coefficients against a symmetric bracket). **Next on
   this line:** the *contraction* itself. `div Rm` now **exists** and the
   **first contraction is proved** (`Divergence.lean`,
   `divCurvature_eq_covRicci_sub`) **and so is the second**
   (`two_mul_sum_covRicci_eq_mvfderiv_scalarCurvatureAt`: `2 div Ric = d scal`).
   **Roadmap item 2 is closed.** What is left on this line is
   `tr_g Ṙic = Δ scal` under the flow, and hence `∂ₜ scal = Δ scal + 2|Ric|²`.
   `RicciVariation.lean` now has `∂ₜ R = tr_g Ṙic + 2|Ric|²` **on a general
   manifold** (`hasDerivAt_scalarCurvatureOfMetricAt_of_isRicciFlowAt`), so
   `tr_g Ṙic = Δ R` was the only missing piece, on no restricted `M` — and it
   is now proved (`ScalarFlow.lean`), so the whole line is closed.
   **The derivation, and why it is a `∇²h` problem.** With
   `⟪Ȧ(X,Y),Z⟫ = ½[(∇_Xh)(Y,Z) + (∇_Yh)(Z,X) − (∇_Zh)(X,Y)]`
   (`inner_derivDifferenceE_eq`) and `∂ₜRm(X,Y)Z = (∇_XȦ)(Y,Z) − (∇_YȦ)(X,Z)`,
   tracing twice gives, with **no** curvature terms,
   `tr_g ∂ₜRic = div div h − Δ(tr_g h)`: the first trace contributes
   `div div h − ½Δ tr h`, the second `½Δ tr h`. Under the flow `h = −2Ric`, so
   `tr h = −2R` and `div div h = −2 div div Ric = −ΔR` by the second contraction
   (`2 div Ric = d scal`), giving `2ΔR − ΔR = ΔR`. **So everything needed is
   `∇²h` for a bilinear form `h` and its two traces** — `∇h` is done
   (`BilinDeriv.lean`), so are `∇²h` and `Δ_g h` (`BilinLaplacian.lean`), and so
   is `div div h` (`OneForm.lean`) — which turned out **not** to need the
   four-linear packaging of `∇²h` at all: it factors as `div(div h)`, `div h`
   being the 1&2 trace of `∇h` and `div` of a one-form needing no frame argument.
   The two forms of `div div h` are also identified: `divDivBilin_eq_sum` says
   the iterated divergence **is** the double trace `∑_{j,i}(∇²_{eⱼ,eᵢ}h)(eᵢ,eⱼ)`
   that the flow produces, by "the divergence commutes with `∇`".
   **The geometric half of the identity is now proved too**:
   `KoszulSecondDeriv.lean`'s `inner_covTwoTensor_eq` says that differentiating
   the Koszul combination gives the same combination with `∇h` replaced by
   `∇²h` and **no curvature terms**, so `∂ₜRm` becomes a statement about `∇²h`
   alone. **The double trace is proved too** (`sum_inner_covTwoTensor_eq`): for
   symmetric `h`, `∑_{i,j}⟪Rm_A(eᵢ,eⱼ)eⱼ,eᵢ⟫ = div div h − tr_g(Δ_g h)`, with
   no curvature terms anywhere. **So the whole geometric side is done.** What
   is left is the *analytic* plumbing: instantiate `IsKoszulOf` at `A = ∂ₜ∇`
   ~~from `inner_derivDifferenceE_eq`~~ — **done** (`FlowKoszul.lean`:
   `isKoszulOf_derivDifference`, and `hasDerivAt_curvatureE_covTwoTensor`
   delivering `∂ₜRm` as `Rm_A`). What is left is to identify `∑_{i,j}⟪Rm_A…⟫`
   with `tr_g(∂ₜRic)` — **done** (`metricTraceE_derivRicciFormOfMetric_eq_sub`:
   `tr_g(∂ₜRic) = div div h − tr_g(Δ_g h)` for the actual flow) — and
   ~~`tr_g(Δ_g h) = Δ(tr_g h)`~~ — **done** (`laplacianFun_traceBilin_eq`).
   That needed no new packaging of `hessianFun`: `∇²f` **is** `∇` of the
   one-form `df` (`hessianFun_eq_covOneForm`, `rfl`), so `OneForm.lean`'s
   frame-argument-free `covOneFormAt` already is the packaging. And
   **`div div Ric = ½ Δ scal` is done too** (`ScalarEvolution.lean`). What is
   the specialisation `h = −2Ric` is **done** too
   (`sum_cov2Bilin_neg_two_ricciForm_eq`): `tr_g(∂ₜRic) = Δ scal`. **Roadmap
   item 2 is fully closed.** Composing it with
   `hasDerivAt_scalarCurvatureOfMetricAt_of_isRicciFlowAt` gives
   **`∂ₜ scal = Δ scal + 2|Ric|²`** on a general manifold — **done**
   (`ScalarFlow.lean`: `metricTraceE_derivRicciFormOfMetric_eq_laplacian`, then
   `hasDerivAt_scalarCurvatureOfMetricAt_eq_laplacian_add`). **This is the first
   evolution equation of the flow formalised here in full.** The composition was
   bookkeeping, but not free: every hypothesis has to be written under a
   *two-step* `letI` (the `RiemannianBundle` **and**
   `ContMDiffCovariantDerivative (leviCivitaOfMetric (g t₀)) 1`), because
   `ricciForm`/`scalarCurvatureAt`/`divBilinOneForm` need the instance and a
   hypothesis-position `letI` supplies only what it names; and level `2` of that
   instance is carried as an explicit hypothesis, a `C²` metric giving only a
   `C¹` connection.
3. **`∂ₜ Rm = Δ Rm + Q`** — **CLOSED** (`CurvatureFlow.lean`,
   `inner_derivCurvature_eq_curvatureLaplacian_add`):
   `⟪∂ₜRm(X,Y)Z,W⟫ = ⟪Δ Rm(X,Y)Z,W⟫ + Q₁ + Q₂ + Ric(R(X,Y)Z,W) + Ric(Z,R(X,Y)W)`,
   for the Koszul tensor of `h = −2Ric`. **Every term on the right is either
   `Δ Rm` or quadratic in the curvature.** The two sources of quadratic terms are
   different and both are commutator defects: `Q₁+Q₂` from reordering the
   derivative slots of `∇²Rm`, the `Ric`-against-`Rm` terms from commuting those
   of `∇²h`. **The flow specialisation is done too** (`CurvatureEvolution.lean`,
   `inner_derivCurvature_eq_curvatureLaplacian_add_of_isRicciFlowAt` and its
   `HasDerivAt` form): `isKoszulOf_derivDifference` +
   `innerE_deriv_eq_of_isRicciFlowAt'` under the two-step `letI`, the same
   bookkeeping `ScalarFlow.lean` did for the scalar equation, plus three level
   hypotheses (`IsContMDiffRiemannianBundle I 3` and the connection at `2` and
   `3`) that a `C²` metric cannot supply. **So the line is closed end to end, and
   what is left on the Hamilton–Ivey road is item (4)'s Uhlenbeck trick, not the
   equation.** The record of how it was built follows.
   It was the single gate for both (1) and (2), with **both sides existing on a
   general manifold** and **both commutation bricks in** (`RicciIdentity.lean`). **What the remaining work
   decomposes into**, in order: (i) ~~the Ricci identity for `∇²h`~~ — **done**;
   (ii) ~~the same for `∇²Rm`~~ — **done** (`cov2Curvature_sub_swap`: one
   curvature term per slot, three inputs and the vector output), which is what
   lets the *differentiated* second Bianchi identity be antisymmetrised;
   (iii) the trace of that against the metric, turning `∇²Ric` into `ΔRm` plus
   quadratic terms; (iv) the substitution `h = −2Ric` into
   `inner_covTwoTensor_eq` and the bookkeeping to land on `Q`. **Step (iii) is
   the bulk and is started**: its first brick, the **differentiated second
   Bianchi identity**, is proved (`BianchiDeriv.lean`,
   `cov2Curvature_cyclic_eq_zero`) and — like `bianchi_second` itself — **needs
   no metric**. **The shape of what remains**: summing that identity over
   `Y = eᵢ` with `X = eᵢ` gives `Δ Rm(A,B)C` as minus two traces of `∇²Rm`
   whose derivative slots are in the *wrong* order; `cov2Curvature_sub_swap`
   reorders them at the cost of terms quadratic in `Rm`; and the reordered
   traces are `∇` of a divergence, which `divCurvature_eq_covRicci_sub` turns
   into `∇²Ric`. So the remaining work is the frame bookkeeping, and it runs on
   the same orthonormal-frame machinery `TraceCov.lean` and `Divergence.lean`
   use. The metric enters here and only here.
   *Geometry side*: `Δ Rm` is built (`CurvatureLaplacian.lean`), on top of
   `∇Rm` being tensorial **and pointwise** in all four slots
   (`CurvatureDeriv.lean`, `Divergence.lean`) — so `∇Rm` and `∇²Rm` are genuine
   tensors, not operators on fields.
   *Analytic side*: **`∂ₜ Rm` is NOT model-space-only.** Do not re-scope this
   wrongly: `hasDerivAt_curvatureE_leviCivitaOfMetric` (`CurvatureVariation.lean`,
   `section Metric`) is stated for a general `M`, as is `hasDerivAt_ricciOfMetric`
   (`RicciVariation.lean`, `section Ricci`). What *is* model-space-only is the
   **scalar** curvature variation, and that is model-space-only **no longer**:
   `RicciVariation.lean`'s `section FlowMetric` has `ricciFormOfMetric`,
   `scalarCurvatureOfMetricAt` and `∂ₜ R = tr_g Ṙic + 2|Ric|²` on a general
   `M`. `section ModelSpace` keeps the `constField` phrasing as the same
   theorems where `Scalar.lean` first stated them.
   **So what is left here is not a port but an identification.** `∂ₜ Rm` is
   currently delivered as `(∇_X Ȧ)(Y,Z) − (∇_Y Ȧ)(X,Z)` with `Ȧ = ∂ₜ∇` the
   derivative of the difference tensor; under the flow (`h = −2 Ric`) that has
   to be shown equal to `Δ Rm + Q(Rm)`. That is the standard several-page
   computation, and it is the real remaining work on this line.
4. **Perelman's `L`-geometry** (`def:reduced-volume`,
   `thm:reduced-volume-monotone`). The deepest genuinely-open node and the one
   everything downstream of `chap:kappa` consumes. Pure comparison geometry +
   ODE, no parabolic theory — but it needs an exponential-map / Jacobi-field /
   second-variation substrate this repo does not have and theirs does.
   **Decided 2026-09-08: build it ourselves.** The reason is not licensing
   (theirs is Apache-2.0 and `NOTICE.md` already sets the vendoring precedent)
   and not pride — it is that **`L`-geometry does not consume Riemannian
   comparison geometry off the shelf; it redoes it.** `L`-geodesics,
   `L`-Jacobi fields, the `L`-index form and the `L`-exponential map are their
   own objects with their own ODEs and their own second-variation formula.
   Bishop, Toponogov and the injectivity radius — the parts of their
   `Comparison/` that look most valuable — are largely *not* what
   `thm:reduced-volume-monotone` needs. So the reusable surface is much
   narrower than 1.9M lines suggests, while the costs (their mathlib pin
   against our post-#36845 requirement, their build time, their breakage) are
   not. **Build narrowly**: the exponential map, the Jacobi equation as an ODE,
   and enough second-variation scaffolding to imitate on the `L`-side. If a
   specific file turns out to be worth taking, vendor it narrowly with
   attribution as `GramSchmidtOrtho.lean` does — do not take a dependency.
   **Survey, 2026-09-08 — the first brick is NOT `exp`.** Mathlib
   `Geometry/Manifold/` has no geodesics, no pullback connection, no
   Christoffel symbols and no covariant derivative along a curve;
   `CovariantDerivative.toFun` acts only on **global sections over `M`**
   (`(Π x : M, V x) → (Π x : M, T_xM →L V x)`), and its whole API — including
   `IsCovariantDerivativeOn`'s two axioms `add`/`leibniz` — is phrased there.
   It does have Picard–Lindelöf (`Analysis/ODE/`). So the missing primitive is
   **`D/dt` along a curve**, the pullback connection on `γ*TM`: without it one
   cannot even *state* `∇_{γ'}γ' = 0`, parallel transport, the Jacobi equation,
   or the first/second variation of length — let alone their `L`-analogues.
   **Design**: mirror mathlib's own `IsCovariantDerivativeOn` →
   `CovariantDerivative` split. A predicate `IsCovDerivAlong cov γ D` with
   three axioms — additive; Leibniz over `f : ℝ → ℝ` with `deriv f`; and
   agreement with `cov` on restrictions of global sections,
   `D (W ∘ γ) t = cov W (γ t) (γ' t)` — then uniqueness, then existence via
   charts (the difference tensor against a chart-flat connection is the
   Christoffel tensor, and `Variation.lean`'s `differenceE` already builds
   exactly that comparison). A section along `γ` is a lift of `γ` to `TM`, so
   its regularity is ordinary `MDifferentiableAt` into the total space — no
   new bundle structure needed. **Do not do this model-space-first**: the repo
   paid for that once with `∂ₜ scal`, which sat `M = E`-only and then cost a
   full port.
   **Started 2026-09-08** (`CovariantAlongCurve.lean`): `velocity`,
   `MDiffAlongAt`, the predicate `IsCovDerivAlong` with those three axioms,
   `zero`, and **locality** (`congr_of_eventuallyEq`) — `D` depends on a section
   only through its germ, by the bump argument, not by fiat — **and uniqueness**
   (`eq_of_isCovDerivAlong`), via `exists_frame_expansion`. **No metric was
   needed**: an earlier plan reached for `OrthonormalFrame.lean` and
   coefficients `⟪V u, Eᵢ(γ u)⟫`, but a plain trivialisation gives the frame
   (`Trivialization.localFrame`) *and* the coefficients (`b.repr (e ⟨γ u,V u⟩).2`)
   *and* their differentiability, all from the same total-space criterion.
   **Existence is proved too** (`isCovDerivAlong_covAlong`), and it needed **no
   Christoffel symbols and no chart-flat connection**: the classical
   trivialisation formula `∑ᵢ(cᵢ'Wᵢ + cᵢ∇_{γ'}Wᵢ)` is enough, because `cov`'s
   own Leibniz rule on the local frame expansion `W = ∑ᵢaᵢWᵢ` *is* the
   `restrict` axiom. An earlier plan here called for `Variation.lean`'s
   `differenceE` against a chart-flat connection; that was more machinery than
   the job takes. **Geodesics are stated** (`IsGeodesicOn`) and, crucially,
   *non-vacuously*: `isGeodesicOn_iff_forall` says `∇_{γ'}γ' = 0` is equivalent
   to vanishing under every operator satisfying the axioms, forward by
   uniqueness and **backward by existence** — without the latter the quantified
   form is vacuously true, the repo's worst class of error. **`Γ` is built and
   is `C^k`** (`christoffelCoord`, `contMDiffAt_christoffelCoord`), so the
   coordinate equation `D/dt V = c' + Γ(γ)(γ')(c)` is available with the
   regularity Picard–Lindelöf wants.
   **That identification is now done** (`GeodesicODE.lean`): the tangent-bundle
   trivialisation *is* the chart (a `rfl`), and with `p t := extChartAt I x₀ (γ t)`
   the coordinate of `γ'(t)` is `p'(t)`
   (`deriv_extChartAt_comp_eq_trivializationAt`, `coeffAlong_velocity_eq`). The
   chart-side manoeuvre was mathlib's own, from `IntegralCurve/Basic.lean` —
   look there first for anything of this shape, it saved most of the work.
   **The equation is assembled too** (`covAlong_velocity_eq_zero_iff`):
   `∇_{γ'}γ' = 0 ↔ p'' = −Γ(γt)(γ')(p')`, an ODE on an open subset of `E`. The
   vector form `trivializationAt_covAlong_eq` was the right level to work at —
   going componentwise through `b.repr` first was a detour; `sum_deriv_repr_smul`
   eliminates the basis in one line.
   **Picard–Lindelöf is started.** The entry point is mathlib's
   `exists_forall_mem_closedBall_exists_eq_forall_mem_Ioo_hasDerivAt` — a `C¹`
   vector field on a normed space admits an integral curve — so **no Lipschitz
   estimate has to be done by hand**; `IsPicardLindelof.of_contDiffAt_one` does
   it. `Γ` is packaged as the bilinear `christoffelB` with its regularity
   (`contMDiffAt_christoffelB`).
   **The chart transport is done and so is Picard–Lindelöf**
   (`christoffelChart`, `contDiffAt_christoffelChart`, `geodesicField`,
   `exists_solution_geodesicField`), and **geodesics exist**:
   `exists_isGeodesicOn` and, with the frame supplied by `exists_frame_on_open`
   (now stated at any regularity, not only `C¹`), the frame-free
   `exists_isGeodesicOn'` — through every point in every direction there is a
   geodesic on an interval about `0`, with the initial condition stated as one
   equation in `TM` so that no dependent-type cast appears. **`[I.Boundaryless]`
   enters exactly once**, in `contDiffAt_christoffelChart`: the chart target has
   to be open in `E` for `ContDiffAt` to be the right predicate. **No Lipschitz
   estimate is done by hand** — `IsPicardLindelof.of_contDiffAt_one` does it.
   **Uniqueness is proved too** (`eventuallyEq_of_isGeodesic`,
   `eventuallyEq_of_isGeodesic'`), on `hasDerivAt_geodesicField_of_isGeodesic`
   (a geodesic *solves* the system in the chart) plus
   `ODE_solution_unique_of_eventually` — and it needs **no hypotheses beyond
   the ones the geodesic equation already carries**, because ODE uniqueness
   wants `p'` differentiable and the chart coordinate of the velocity *is*
   `p'`. **Affine reparametrisation is proved too** (`Exponential.lean`:
   `velocity_comp_mul`, `covAlong_velocity_comp_mul_eq_zero`,
   `isGeodesicOn_comp_mul`) and cost almost nothing — in the chart the equation
   is `p'' = −Γ̃(p)(p')(p')`, reparametrising multiplies the left by `a²`, and
   `Γ̃` is bilinear by construction; **no case split on `a = 0`** and **no
   boundarylessness**. **Interval uniqueness is proved too**
   (`forall_totalSpace_eq_of_isGeodesicOn`, `eqOn_of_isGeodesicOn`) — the clopen
   argument, with closedness taken in two Hausdorff pieces because mathlib has
   no `T2Space` on a bundle's total space. **And `exp` is built**
   (`IsGeodesicRun`, `expMap`, `expMap_eq`, `expMap_smul_eq`, `expMap_zero`) —
   the first object of the comparison-geometry substrate, well defined rather
   than chosen, with homogeneity `exp_x(a v) = γ_v(a)` from reparametrisation.
   **Left on this line**: regularity of `exp` — and this is **not** a
   manifold-side computation. Mathlib has **no smooth dependence of ODE
   solutions on initial conditions** (checked 2026-09-09; its local flow is
   built by `choose` behind a `dite` and is not even continuous as constructed),
   only Lipschitz and continuous dependence. So `d exp_0 = id` and Jacobi fields
   sit behind a genuine ODE-theory gap — but **NOT parallel transport**, which an
   earlier version of this paragraph wrongly listed with them: its equation is
   *linear* in the unknown, so `LinearODE.lean`'s Dyson series settles it along a
   fixed curve with no dependence-on-initial-conditions theory at all
   (`ParallelTransport.lean`, `TransportIsometry.lean`). Only the *nonlinear*
   geodesic flow is behind the gap; see "Where the real gaps are".


**Upstream candidates** (Mathlib-general, no dependence on the curvature
stack unless noted): `VectorField.lieBracket_apply_fun`; `neg_apply`,
`sub_apply`, `mdiffAt_cov_apply`, `contMDiff_cov_apply` (`C^k` connection ⇒
`C^k` covariant derivative); `mlieBracket_sub_left'`;
`exists_hasDerivAt_clm_of_apply` (coordinatewise ⇒ CLM-valued derivative);
`Filter.EventuallyEq.mvfderiv_eq`, `mvfderiv_fun_sum` and
`mdifferentiableAt_fun_sum` (Mathlib has `mvfderiv_fun_add` but no germ-congruence
and no finite-sum rule; its `MDifferentiableAt.sum` is stated for `ι : Type`, not
`Type*`);
`hasDerivAt_inverse_innerE`'s pattern (derivative of `inverse` from
`contDiffAt_map_inverse` plus `f ∘ f⁻¹ = id`); `hessianFun_neg`; and the
big one, smoothness of `leviCivitaConnection` (`LeviCivitaSmooth.lean`,
which Mathlib's `LeviCivita.lean` lists as future work — weaken
`IsManifold I ω M` to `C^{k+2}` first). Post the Zulip question
(`#mathlib4`, "RiemannianBundle: metrics as instances vs values", tag
`sgouezel`) before upstreaming anything that quantifies over metrics.
Small PRs first; budget the time for conventions, not mathematics.

**Not yet:** upstreaming curvature/Ricci/sectional (wait for the Zulip
answer), anything parabolic, any writing-up.

## Where the real gaps are

Read this list as *gaps in Mathlib and in this repo*. Since 2026-08-21 the
parabolic and Cheeger–Gromov entries are no longer gaps **in the literature** —
Chow–Liao–Qin closed both — but nothing of theirs is in Mathlib, and nothing of
theirs is imported here.

- **Levi-Civita existence is NOT needed to define the flow** — the per-slice
  `∀ t, ∃ cov` is equivalent to the textbook equation, now as a theorem
  (`isRicciFlowOn_iff_ricciOfMetric`, using smoothness of the connection from
  `LeviCivitaSmooth.lean`). Both formulations are kept; the existential one is
  what elaborates under `letI`.
- **Three distinct parabolic theories** are on the road, not one: Ricci flow,
  harmonic map flow (uniqueness of the standard solution), curve-shrinking flow
  (finite extinction). Chow–Liao–Qin have the first; the other two are open
  everywhere.
- **Second independent analytic gap:** Cheeger–Gromov compactness. Mathlib has
  Gromov–Hausdorff for compact metric spaces; pointed smooth convergence of
  manifolds under curvature bounds does not exist. Chow–Liao–Qin built it
  (`Geometry/Compactness/CheegerGromov`), so it is done but not upstreamed.
- **Almost no comparison-geometry substrate here** — `exp` and parallel transport
  now exist (`Exponential.lean`, `ParallelTransport.lean`), and transport is known to
  be an **isometry** for a metric connection (`TransportIsometry.lean`), but there are no
  Jacobi fields, no second variation, no index form, no injectivity radius, no
  cut locus. Perelman's `L`-geometry (Next 4) cannot start without it. This is
  the deciding fact in build-versus-import.
- Mathlib has **no maximal-solution ODE theory** — hence germ uniqueness in
  `Homogeneous.lean`.
- **Uhlenbeck's trick is gated by the SAME mathlib gap as the `L`-geometry line.**
  Checked 2026-09-11, `Analysis/ODE/`: there are six files and **no linear-ODE
  theory at all**, no global existence, and no dependence-on-parameters theorem.
  Every existence result in `ExistUnique.lean` is *local* (on a closed ball, on a
  small interval); the rest is uniqueness. **Refined 2026-09-14**: mathlib *does*
  have a whole-interval statement (`exists_eq_forall_mem_Icc_eq_picard`), but its
  `mul_max_le` field forces **`C·T < 1`** for a linear field — so the first
  obstacle on this line is **continuation**, not the fixed point, and continuation
  is the "no maximal-solution theory" gap listed separately below. **The Dyson
  series bypasses continuation entirely** (`LinearODE.lean`), which makes this
  route cheaper than it looked: the factorial bound converges at every `t` with no
  smallness hypothesis. **That half is now closed**: `hasDerivAt_dysonSum` and
  `eq_dysonSum_apply` give existence *and* uniqueness of a global solution of
  `ẋ = A(t)x` on all of `ℝ`, so the **global existence** obstacle is gone.
  **And `C¹` dependence on a parameter is proved too** (2026-09-14,
  `hasFDerivAt_dysonSum`): differentiation under the integral sign, with the
  bounds taken global in the parameter so the dominating function is uniform.
  **And `k = 2` is now DONE** (2026-09-24, `LinearODEAugmented.lean`): the augmented
  system `∂ₜ(u,v) = (A∘u, A∘v + (·∘u)∘A')` is built as a continuous LINEAR function
  `augCLM` of the pair `(A, A')`, the identification `Φ_aug(x,t)(1,0) = (Φ, D_xΦ)` is
  proved by uniqueness, and the `C¹` theorem applied at `F := G` gives `D_xΦ`
  differentiable — hence also `Φ` genuinely **`C¹`** in the parameter, the continuity
  coming free from the second derivative existing.
  **And general `k` is DONE too** (same day, `LinearODESmooth.lean`): the construction
  iterated, on a hypothesis class `IsBddFamily` that is itself recursive and closed under
  CLM post-composition and pairing. **So the `C^k`-parameter-dependence gap this list
  carried for months is CLOSED.** Its one obstruction was a LEAN one, not a mathematical
  one: `G = (F→LF) × (H→L(F→LF))` lives in universe `max u_F u_H`, so iterating in
  general universes would make `Nat.rec` produce statements in a *growing* universe;
  `F H : Type u` fixes it and costs nothing at the application site.
  **Do NOT record this line as closed for Uhlenbeck on the strength of `k = 2`**: the
  parameter there ranges over a *manifold*, not a normed space, and the bounds here are
  global in `x`, so chart-and-bundle transfer with globally-bounded extensions off each
  chart remains — at every `k`.
  Uhlenbeck's trick needs
  `∂ₜι(t,x) = Ric_{g_t}(x) ∘ ι(t,x)` solved on all of `[0,T]` **and** `C^k` in
  `x`, since the pulled-back curvature has to be a differentiable section.
  **Strategic note, 2026-09-14 — the parameter may be avoidable entirely.** `LinearODE.lean`
  is stated for an arbitrary Banach space `F`, and `ι ↦ Ric_{g_t} ∘ ι` is a *bounded linear
  operator on the Banach space of `C^k` sections* of `Hom(V,TM)` over a compact `M`. Running
  the Dyson series there gives a solution that is a `C^k` section at every `t` with **no
  parameter-dependence theory at all**. The cost moves to constructing that Banach space —
  a `C^k`-norm on sections, which mathlib does not have either (it has
  `BoundedContinuousFunction` and sup norms on `ContinuousMap`, nothing with derivatives).
  Cost the two routes before starting either.
  *Continuous* dependence on `x` is in fact reachable without new theory — fold
  the parameter into the state as a coordinate with `ẋ = 0` and use
  `..._lipschitzOnWith` / `..._continuousOn`, which are dependence on the
  **initial condition** — but `C^k` dependence is not, and `C^k` is what the
  trick needs. So roadmap items 1 and 4 have one common gate, and it is an
  ODE-theory contribution, not a geometry one.
  **The alternative is now started, not just costed** (2026-09-13): the maximum
  principles take a **time-dependent reaction** (`mem_of_deriv_le_at_max_time`,
  `mem_of_laplacian_time`, `le_of_deriv_ge_at_min_time`), which is what a moving
  background produces and cost nothing — `F` is touched only at the touching
  time. **And the moving *Laplacian* is now done too**
  (`TensorPreservation.lean`, `le_of_laplacian_moving`/`mem_of_laplacian_moving`): a family
  `g_t` of metrics costs nothing, because the principle inspects `Δ` only at a spatial
  extremum and that holds per-metric. **What is still missing for §9 is the moving inner
  product on `V`**, and that is genuinely the hard part: the distance to `K` and the nearest-point
  projection are compared *across* times by the first-touching-time argument.
  The route in full: Hamilton 1982 §9
  applies the tensor maximum principle on a bundle whose fibre metric *moves*,
  with the metric's time derivative appearing explicitly. That avoids the trick
  entirely at the cost of generalising `TensorMaximumPrinciple.lean`. The scalar
  analogue of that trade is already settled in this repo's favour —
  `ScalarPreservation.lean` shows a moving `Δ` costs nothing when the principle
  only inspects the operator at an extremum.
- **Mathlib has no *smooth dependence* of ODE solutions on initial conditions.**
  Checked 2026-09-09, `Analysis/ODE/ExistUnique.lean`: the local flow
  `ContDiffAt.exists_eventually_eq_hasDerivAt` is built by `choose` behind a
  `dite`, so as constructed it is not even continuous in the initial point; what
  *is* available is Lipschitz and continuous dependence
  (`IsPicardLindelof.exists_forall_mem_closedBall_eq_hasDerivWithinAt_lipschitzOnWith`,
  `..._continuousOn`). **This is still the gate on the `L`-geometry line**:
  `expMap` is built and well defined (`Exponential.lean`), but nothing yet says
  it is continuous, let alone `C^k`, and `d exp_0 = id` and Jacobi fields both
  need that. **Narrowed 2026-09-14, but NOT closed**: `LinearODE.lean` now has
  `C¹` dependence on a parameter for a *linear* equation, which is the substrate
  the variational-equation route runs on — but the geodesic equation is
  **nonlinear**, so what is proved does not apply to `exp` directly. What is left
  is the step from "the variational equation has a solution" to "the difference
  quotients of the flow converge to it", which is a Grönwall argument against the
  nonlinear field and is not in this repo or in mathlib. Do not record the
  `L`-geometry gate as closed on the strength of the linear result.
  **Parallel transport, however, IS closed** (2026-09-15, `ParallelTransport.lean`), and the
  entry above used to list it alongside `exp` wrongly. The transport equation is **linear** in
  the unknown — `Γ` is bilinear by construction — so it is `LinearODE.lean`'s equation along a
  *fixed* curve and needs no dependence-on-initial-conditions theory at all. Only the
  *nonlinear* geodesic flow sits behind this gap.
- ~~No global `C²` extension of a tangent vector.~~ **Closed 2026-09-07**
  (`GlobalExtension.lean`, PR #16). It was the worst hazard in the repo, not
  merely a gap: `Hamilton.lean`'s predicates quantify over globally `C²`
  fields, so on an `M` carrying no such nonvanishing field they were
  *vacuously true* and `hamilton_1982` was trivially provable and asserted
  nothing. `forall_contMDiff_iff_forall_tangent` is the bridge; the
  predicates are now also stated pointwise
  (`hasPositiveRicciLC_iff_tangent`, `hasConstSecLC_iff_mul`).
- ~~Ricci is not a pointwise bilinear form; the scalar curvature is
  model-space only.~~ Closed the same day by `RicciForm.lean`, on top of the
  extension and of `CurvaturePointwise.lean`'s pointwise third slot: `Ric` is
  a continuous bilinear form (`ricciForm`) and `scal` a genuine metric trace
  (`scalarCurvatureAt`) on any manifold, with frame-independence free.
  ~~**Still model-space only:** `∂ₜ R`.~~ **Closed 2026-09-08**
  (`RicciVariation.lean`, `section FlowMetric`): `ricciFormOfMetric`,
  `innerE_deriv_eq_of_isRicciFlowAt'` (`∂ₜg = −2 Ric`),
  `scalarCurvatureOfMetricAt` (= `tr_g` of the Ricci form),
  `hasDerivAt_ricciFormOfMetric`, and `∂ₜ R = tr_g Ṙic − ⟨h,Ric⟩` with its flow
  corollary `∂ₜ R = tr_g Ṙic + 2|Ric|²`, all on a general manifold. The
  `constField` versions in `section ModelSpace` are kept as the same theorems
  where they were first phrased.
- ~~The metric trace does not yet commute with `∇` on the manifold.~~ Closed
  2026-09-07 by `TraceCov.lean` (PR #20), and the *contraction* is closed too:
  **both** contractions of the second Bianchi identity are proved
  (`Divergence.lean`), so `div Rm(Y,Z,W) = (∇_Y Ric)(Z,W) − (∇_Z Ric)(Y,W)` and
  `2 div Ric = d scal`.
- ~~`∇Rm` is only an operator on fields, and there is no `Δ Rm`.~~ **Closed
  2026-09-08.** `∇Rm` is tensorial and pointwise in all four slots
  (`CurvatureDeriv.lean`, `Divergence.lean`), `∇²Rm` likewise in both derivative
  slots, and `Δ Rm` is the trace of the resulting bilinear map
  (`CurvatureLaplacian.lean`). The Bochner identity for the rough Laplacian is
  proved too (`Bochner.lean`). **What is left on the evolution line is the
  identification `∂ₜ Rm = Δ Rm + Q`** — a computation, not a port; see Next 3.

## Lean gotchas

- Use `[IsManifold I ω M]`. Instance search will not see through
  `minSmoothness ℝ n = n`; `ω` synthesises `3`, `(2:ℕ∞)+1` and `minSmoothness ℝ 2`.
- Metric reaches `TM` via `[RiemannianBundle (fun x ↦ TangentSpace I x)]`, never a
  raw `[∀ x, InnerProductSpace ℝ (TangentSpace I x)]`.
- **lean4#14949 (unification sees through `TangentSpace`)**: applying a lemma
  stated for a general bundle `V` to `TangentSpace I` can infer the wrong
  fibre instances (`fun _ ↦ NormedAddCommGroup E`). Pass
  `(V := fun x : M ↦ TangentSpace I x)` explicitly, or restate the lemma for
  the tangent bundle (`ContMDiffAt.inner_bundle'` in `LeviCivitaSmooth.lean`,
  as mathlib does in `LeviCivita.lean`).
- `omit [..] in` goes *before* the docstring, not between docstring and
  `theorem`. `set x := e with h` then `rw [← h]` fails when `e` sits under
  dependent types (trivialisations); write the term out instead.
- `ContMDiffAt.div_const`/`.mul` on `ℝ` want a Lie-group instance ℝ lacks;
  use `(contDiffAt_id.mul contDiffAt_const).comp_contMDiffAt`.
- ODE comparison: `HasDerivAt.neg` produces `(-A) u`, and `.add`/`.sub` produce
  `(m + n) t` — `simp only [Pi.neg_apply, Pi.add_apply, Pi.sub_apply]` before
  `ring`. `ContinuousOn a (Icc 0 T)` does not give `ContinuousAt` at the
  endpoints; extend by `projIcc` to get a `Continuous` integrand for
  `intervalIntegral.integral_hasDerivAt_right`.
- `Basis` is `Module.Basis`; `Fintype.linearIndependent_iff` gives coefficients
  from `∑ c i • b i = 0`.
- Mathlib's `IsLeviCivitaConnection` spells compatibility as
  `cov.IsMetricCompatible (M := M) (V := TangentSpace I)` with `cov` an explicit
  variable — the named arguments are what lets it elaborate outside an
  existential. Prefer `cov.IsLeviCivitaConnection` in hypothesis position;
  `isLeviCivitaConnection_iff` converts to the `∧` form used in the existentials.
- Building mathlib `master` from source with no cache takes hours on 4 cores;
  `lake exe cache get` needs `mathlib4.lakecache.org`.
- `HAdd (TangentSpace I x) E` never synthesises: keep algebraic identities on
  tangent spaces (`A : Π y, T_yM →L T_yM →L T_yM`) and analytic statements on
  `E` (`covE`, `curvatureE`, `covEndE`), and bridge with `exact` by defeq.
- `simp only` zeta-reduces a `show T from v` ascription and then
  `rw [extend_apply_self]` fails (`?v : E` vs `T_yM` at instances
  transparency) — use `unfold`. Same family: `LinearMap.trace ℝ E` and
  `LinearMap.trace ℝ (TangentSpace I x)` are defeq but `rw` will not cross;
  `change` to the `TangentSpace` form before `trace_eq_sum_inner`, and state
  sum-expansion facts for plain `E`-vectors (`key : ∀ c e w, …`) then `exact`
  them at `b i`.
- A bare `TensorialAt.mkHom …` elaborated against an expected type `E →L E`
  leaves `?V x =?= E` unsolved; ascribe `(… : TangentSpace I x →L[ℝ]
  TangentSpace I x)` first, then use it at type `E →L E`.
- **Never `local notation` over a section variable.** Hygiene hides `E`, the
  next `variable (g : … E …)` silently fails, and everything downstream is
  auto-bound with stuck `IsManifold ?I ω ?M` instances. Write the lambda out.
- The `RiemannianBundle` instance for a metric `g` is introduced by
  `letI : RiemannianBundle … := ⟨g.toRiemannianMetric⟩` in defs and
  `let _ : … := …` in proofs (the `haveI`/`letI` linter); a hypothesis that
  needs it is written `(h : letI := …; P)`, as `IsRicciFlowOn` does.
- **`TangentSpace I x` has no norm of its own** — the norm comes from a
  `RiemannianBundle` instance, i.e. from a metric. `HasDerivAt` into
  `TangentSpace I x` or `TangentSpace I x →L[ℝ] …` fails to elaborate with
  `failed to synthesize NormedAddCommGroup (TangentSpace I x)`. Type such data
  on `E` via a wrapper def whose signature says `E` (`innerE`,
  `leviCivitaOfMetricE` in `Variation.lean`); a type ascription `(v : E)` does
  **not** change the inferred type. Pass `hasDerivAt_const (F := E)`. Then
  `map_sub`/`map_zero` will not fire on a `TangentSpace`-typed `a - b` fed to an
  `E`-typed CLM: state the equation as a `have … := map_sub _ _ _` and rewrite.
- `set_option maxSynthPendingDepth 3` for nested CLM spaces
  (`E →L[ℝ] E →L[ℝ] E →L[ℝ] ℝ`), or norm instances silently fail to synthesise and
  `simp` lemmas quietly no-op.
- `[LieRing 𝔤]` **cannot** carry a norm — `LieRing` and `NormedAddCommGroup` both
  extend `AddCommGroup`, an unresolved diamond with no mathlib precedent. Carry the
  bracket as an explicit `β : E →L[ℝ] E →L[ℝ] E` and transport.
- Carry a varying metric as a map into the dual (`g : E →L[ℝ] E →L[ℝ] ℝ`); then
  nondegeneracy is `ContinuousLinearMap.IsInvertible` and smoothness is
  `contDiffAt_map_inverse`, with no coordinates.
- Argument order is nonstandard: `cov σ x (X x)` is `(∇_X σ) x` on paper.
- `torsion_eq_zero_iff` takes `cov` explicitly. `ContMDiffAt.mdifferentiableAt`
  wants `n ≠ 0`, not `1 ≤ n`.
- `real_inner_comm x y` orients the *other* way than reading suggests: `rw` with
  it hunts for the pattern you were trying to produce. State the swap as a
  `have e : ⟪a, b⟫ = ⟪b, a⟫ := real_inner_comm _ _` and rewrite with `e`.
- `refine ⟨fun i j ↦ e, …⟩` leaves the later goals beta-unreduced, so `rw`
  fails on `(fun i j => e) i j`; put a `show` first.
- "Prints identically but won't unify" is usually a substitution made mentally
  under a binder, not a diamond. `simp only` with collapse lemmas *and* pointwise
  `∀ y` facts in one set; `rw` cannot reach under a binder.

## Blueprint / CI gotchas

- **CI (`.github/workflows/blueprint.yml`) runs three jobs.** `lean`: mathlib cache
  (a miss fails, never builds from source), `lake build` with warnings as errors,
  then `scripts/check_axioms.py` (`#print axioms` on every `\lean{}` name in the
  blueprint; only `propext`, `Classical.choice`, `Quot.sound` allowed; no `axiom`
  declarations). `blueprint`: **on PRs too** — `leanblueprint pdf` with the log
  checked for `^!` and `Missing character`, then `web` and `checkdecls`; uploads
  the site only on `main`. `deploy`: Pages, `main` only. TeX is a pinned TinyTeX
  bundle (`TINYTEX_URL`, xelatex + latexmk included, ~200 MB, verified locally),
  not `texlive-full`.
- The axiom script is the local pre-PR check too: `python3 scripts/check_axioms.py`
  after `lake build` (~3 min, mostly loading oleans).
- **PDF builds locally**: basictex + `sudo /Library/TeX/texbin/tlmgr install latexmk`.
  The config uses **xelatex** (`$pdflatex = 'xelatex -synctex=1'`), not pdflatex.
  Fallback without latexmk: `xelatex` twice from `blueprint/src`.
- **Grep the log for `Missing character`, not just errors.** A literal `₃` inside
  `\texttt{}` has no glyph in `lmmono10-regular` and was silently dropped from the
  PDF for several commits. Use `$_3$`.
- Every macro must be declared in `macros/common.tex` — an undefined `\Z` broke CI.
  And never write a bare `\lean` in prose; it takes an argument.
- `checkdecls` verifies every `\lean{}` name. `proof_wanted` produces a **private**
  declaration that it cannot resolve — name such statements in prose instead.
- `latexmkrc` lists `print.tex` only; `print.tex` must not load `blueprint.sty`.
- **Concurrency, corrected 2026-09-14.** An older note here said pushing while a run is in
  flight cancels it, and that batching pushes was therefore necessary. Read the workflow: the
  `blueprint` job's group is **`blueprint-${{ github.ref }}`** with `cancel-in-progress: true`
  — *ref-keyed*, so a push to one branch can never cancel another branch's run — and `deploy`
  uses group `pages` with **`cancel-in-progress: false`**, which queues. So the only thing a
  push cancels is a superseded run on the *same* ref, which is what you want. The three runs
  cancelled on 2026-08-13 predate that config. Do not reinstate the batching rule without
  re-reading `.github/workflows/blueprint.yml`.
- `gh run watch` exits 1 on transient API errors and its shell exit code does not
  reflect the CI conclusion. Poll `gh run view --json status,conclusion` in a loop.
- **Never `git add -A` while a subagent is writing** — it swept 500 lines of
  in-progress work into an unrelated commit (`d43dd54`). Stage explicit paths.
